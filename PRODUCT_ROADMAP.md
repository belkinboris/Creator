# Создатель — источник правды

Этот файл — общая память между автономными сессиями. Каждая сессия начинается
с чистого листа и не помнит предыдущие, поэтому: **сначала читаем этот файл,
потом работаем, в конце дописываем журнал.**

Порядок работы в сессии:
1. Прочитать принципы (ниже) — они не обсуждаются.
2. Взять задачу из бэклога сверху вниз: **блок A всегда вперёд остальных**.
3. Сделать ОДНО осмысленное улучшение с тестами, все тесты зелёные.
4. Дописать запись в журнал работ (сверху) и обновить статус задачи.

---

## Что мы строим

Сервис, который помогает человеку в России пройти этап **0.1** — от «есть идея
в голове» до «знаю, стоит ли в неё вкладываться». Бесплатно: реальный спрос по
Вордстату и конкуренты из выдачи. Платно: бизнес-план с расчётами и живой тест
на реальных людях.

Две аудитории, которые нельзя мерить одной линейкой:
- **фаундер** — проверяет идею до вложения сил и денег;
- **получатель соцконтракта** — самозанятый или безработный, готовит
  обоснование для комиссии соцзащиты под выплату ~350 000 ₽.

Аналогов, которые делают это честно и на реальных данных, в России нет.
Дженерик-генераторы бизнес-планов пишут «рынок огромен» из воздуха — мы
считаем по цифрам, которые собрали сами.

---

## Принципы, которые нельзя нарушать

**Если задача противоречит принципу — неверна задача, а не принцип.**

1. **Честность данных.** Цифры Вордстата и выдачи показываем как есть. Если
   частотность получена по другой формулировке, чем показана, — говорим об
   этом прямо. Никогда не выдумываем числа «чтобы красивее выглядело» и не
   приписываем одной фразе счёт другой.

2. **Вердикт имеет право сказать «нет».** Смысл сервиса — чтобы человек НЕ
   потратил деньги зря. Разбор, который всем говорит «отличная идея», не стоит
   ни рубля. Плохая новость, поданная до траты денег, — это и есть польза.

3. **Продаём только то, что реально отдаём.** Если витрина обещает смету —
   движок обязан её посчитать. Если тариф не содержит расчётов — это написано
   на карточке тарифа. Не обещаем услуг, которых не оказываем.

4. **Одна аудитория — одна оптика.** Венчурные критерии («не масштабируется»)
   для самозанятой, которая шьёт шторы на дому, не просто бесполезны — вредны.
   Устойчивый доход одной семьи — нормальная цель, а не низкий балл.

5. **Пользователь не обязан знать наш внутренний язык.** «Оффер», «лендинг» —
   запрещены (тесты проверяют). «Живой тест», «трафик», «конверсия», «гипотеза»
   — либо раскрываются понятной фразой рядом, либо заменяются. Человек должен
   видеть слова, которыми он сам думает.

6. **Один экран — один фокус.** Прогрессивное раскрытие: открыли следующее —
   свернули пройденное в одну строку итога. Не даём пользователю выбора, куда
   смотреть, и не показываем всё полотном сразу.

7. **Деградация вместо ошибки.** Упал Вордстат, LLM или почта — показываем
   максимум из доступного и объясняем словами, что именно недоступно. Пятисотая
   ошибка на пути пользователя недопустима.

8. **Всё работает из России.** Инфраструктура Yandex Cloud, 152-ФЗ, чек 54-ФЗ.
   Claude/Anthropic в продукте не используется в принципе.

9. **Каждое изменение — с тестом.** `python -m pytest tests/` зелёный до сдачи.
   Комментарии объясняют «почему», а не «что».

10. **Катит и деплоит владелец.** Сессия не мержит PR и не жмёт ▶ в Timeweb.
    Работа сессии заканчивается пушем и понятным описанием.

---

## Бэклог

Статусы: `[ ]` не начато · `[~]` в работе · `[x]` сделано (переносится в журнал).

### A. Сломанное и денежно-рискованное — вперёд всего остального

- [x] **A1. Оплаченный отчёт не собрался — никто не узнал.** Сделано
  2026-07-26: `mailer.notify_owner` + `ReportPurchase.gen_error/owner_notified`,
  письмо владельцу один раз на заказ, покупки отчётов появились в `/desk`
  отдельной секцией со статусом «сбой доставки». Требует
  `SOZDATEL_OWNER_EMAIL` в окружении — **без неё уведомления молча не уходят**.
- [x] **A2. Владелец не знает о новых оплатах и заявках.** Сделано
  2026-07-26: `_notify_owner_order` зовётся из `yookassa_webhook`
  (подтверждённая оплата, оба вида) и из `/api/live-test` + `/api/report`
  (заявки без оплаты). Флаг `paid_notified` разведён с `fail_notified`,
  иначе письмо об оплате гасило бы письмо о сорванной доставке. Так же
  требует `SOZDATEL_OWNER_EMAIL`.
- [x] **A3. `/p/{idea_id}` обращается на «ты» и говорит по-владельчески.**
  Сделано 2026-07-26: вся инструкция запуска переписана на «вы», заодно убран
  внутренний жаргон — «smoke-тест», «РСЯ», «трафик» (принцип 5). Тест
  `TestProjectPageSpeaksToCustomer` не даёт «ты»-формам вернуться.
- [x] **A4. `_verdict()` в `app/demand.py` говорит «на живом трафике».**
  Сделано 2026-07-26: переписаны все четыре текста вердикта. Правило на
  будущее — **вердикт сообщает находку и НЕ предписывает следующий шаг**:
  шаг у аудиторий разный и его подбирает CTA страницы по `purpose`. Тесты
  `test_verdict_has_no_internal_jargon` / `test_verdict_states_finding_not_next_step`.
- [x] **A5. `static/portfolio.html` — мёртвый файл** с третьей, устаревшей
  шкалой этапов (Оффер · Активация · Мост · Оплата · Масштаб). Сделано
  2026-07-26: файл удалён, имя убрано из прогрева шаблонов в `_lifespan`.
  Роут `/portfolio` остаётся редиректом на `/desk` — старые ссылки не ломаем.
  Тест `test_dead_portfolio_file_is_gone` не даёт файлу вернуться.
- [x] **A6. `chosen_offer` игнорируется в отчёте.** Сделано 2026-07-26: выбор
  хранится на `DemandCheck.chosen_offer` (не на заказе — отчёт заказывают с
  `/report/{check_id}`, который про заказ не знает), пишется по
  `POST /api/demand/{id}/chosen` при клике на карточке, доезжает до
  `generate_report` и видён покупателю строкой «Разбираем формулировку».

- [x] **A7. Воронка обещала, что рекламу запустим мы.** Сделано 2026-07-27.
  Исправлены все три места (крошка на `/r/`, сообщение после оплаты, ответ
  на заявку без оплаты). Главное сверх исходной задачи: **до оплаты нигде не
  говорилось про отдельный рекламный бюджет**, хотя в оферте это записано, а
  на деле он больше самой услуги. Оговорка встала под кнопкой оплаты, цифра
  живёт в `AD_BUDGET_HINT` и подставляется и туда, и в плейбук.
- [x] **A9. Оплаченный отчёт открывался любому по перебору номера.** Найдено
  кастдев-проходом 2026-07-27 и в тот же день закрыто. `/report/{check_id}`
  адресуется порядковым номером бесплатной проверки: 42 → 41 отдавал чужой
  бизнес-план за 2990 ₽ целиком, вместе с чужой сметой. Хуже чтения была
  запись: `POST /api/report/{id}/section` пускал постороннего **запускать
  генерацию по чужой покупке**, то есть тратить наши деньги на модель.
  Теперь у покупки есть `access_token`; отчёт открывают только владелец по
  ключу, ссылка с токеном (уходит в `return_url` оплаты и в кабинет) и
  сессия кабинета с той же почтой, что в заказе. Посторонний видит тизер и
  объяснение, как попасть к своему отчёту, — не 403 (принцип 7).
- [x] **A10. Покупателю после оплаты не приходило ничего.** Сделано
  2026-07-27. При оплате письмо уходило владельцу (A2), а человеку,
  отдавшему 990–2990 ₽, — только фискальный чек от ЮКассы, то есть чек, а
  не ссылка на продукт. `mailer.notify_buyer` (fail-soft, как владельческий)
  + `buyer_notified` на обеих моделях заказа — флаг **разведён** с
  `paid_notified`, иначе успех одного письма погасил бы второе (урок A2).
  Письмо отвечает на три вопроса: что куплено, где оно лежит (прямая ссылка
  с токеном из A9) и как вернуться, если ссылка потеряется. Уходит и на
  заявку без оплаты. В письме про живой тест повторена оговорка про
  отдельный рекламный бюджет (A7). Контакт-телефон писем не получает —
  `mailer.looks_like_email` спрашивает заранее, а не падает. На странице:
  строка сборки говорит, что вкладку можно закрыть, а плашка возврата с
  оплаты — где разбор будет лежать потом; **про письмо страница говорит
  только если оно действительно ушло**.
- [x] **A11. Слабый спрос не мешал продавать то, что по нашим же цифрам не
  сработает.** Найдено кастдев-проходом 2026-07-27, закрыто в тот же день.
  Идея с вердиктом «в поиске эту идею почти не ищут» и оценкой 1/10 вела
  ровно к той же кнопке, что идея на 6/10. Живой тест здесь особенно
  сомнителен: он гоняет рекламу по ТЕМ ЖЕ запросам, а вывод мы делаем по
  `CLICK_TARGET` визитам — при частотности ниже `THRESHOLD_NICHE` (300/мес)
  их неоткуда взять. Теперь при `verdict.level == "weak"` главным действием
  становится бесплатное («попробуйте другую формулировку» с арифметикой,
  почему), оба платных блока опускаются в альтернативы, у кнопки живого
  теста стоит прямая оговорка, а шапка перестаёт обещать следующий этап.
  **Кнопки не спрятаны** — наше дело сказать правду, а не решить за человека.
- [x] **A12. Ненайденную частотность выдавали за приговор идее.** Найдено
  кастдев-проходом и закрыто 2026-07-27. `count = None` означает «оба пути
  Вордстата не дали числа» (так и написано в докстринге `wordstat_best`), а
  страница писала напротив каждой фразы **«почти не ищут»** — вывод о рынке
  на месте отсутствующего числа. Это самое вероятное состояние прода
  сегодня: OAuth-токена Вордстата ещё нет, значит **каждый** посетитель
  видел три строки «почти не ищут» и уходил хоронить живую идею. Теперь
  там «нет данных у Яндекса», а при `verdict.level == "unknown"` финал
  говорит «Проверка не состоялась», главным действием становится бесплатное
  «Проверить ещё раз», оба платных блока опускаются в альтернативы и несут
  оговорку, что цифр спроса нет. Кнопки не спрятаны.
  **Отменено прежнее решение:** когда-то «нет данных» заменили на «почти не
  ищут», потому что первое читалось как «сайт сломан». Жалоба верная, но
  лечится словами («нет данных **у Яндекса**» называет источник), а не
  враньём в другую сторону — принцип 1 не обсуждается.
- [x] **A13. Оплаченный проект показывался как «Этап 1 — Идея».** Найдено
  кастдев-проходом по платному пути и закрыто 2026-07-27. Правило этапа было
  `1 if views > 0 else 0` — оно осталось от старой шкалы, где 0 назывался
  «Оффер». По нынешней шкале 0 это «Идея», 1 — «Спрос», и человек сразу
  после оплаты 1490 ₽ видел свой проект в самом начале пути, а на `/p/`
  ещё и с подписью «Идея — набираем первые визиты», противоречащей самой
  себе. Проект существует только потому, что проверка спроса пройдена и
  тест оплачен: теперь это этап 2 «Тест на реальных людях», а с набором
  визитов до цели — 3 «Заявки». Правило переехало в `_smoke_stage` —
  **вторая копия жила в `static/project.html` и уже разъехалась**, теперь
  страница берёт и этап, и длину шкалы с сервера.
- [x] **A14. Проверочная страница обещала от имени покупателя.** Найдено
  кастдев-проходом по `/l/` и закрыто 2026-07-27. В шаблоне были зашиты три
  вещи: **«Ранним — 50% на первый месяц»** (скидку мы придумали за владельца
  идеи и показывали живым людям), **«Запускаемся скоро» / «продукт готовится
  к запуску»** (предполагало, что продукта ещё нет — для услуги вроде пошива
  штор это отпугивает человека, пришедшего заказать сегодня, и портит ту
  самую конверсию, по которой мы выносим вердикт) и слово **«лендинг»** в
  подвале. Тексты вокруг формы стали нейтральными и ничего не обещают за
  владельца: «Оставить заявку», «напишем вам с условиями и сроками».
  **Сторожа на запрещённые слова для этого файла не было вовсе** — тесты
  сканировали `static/`, а шаблон лежит в `app/`; теперь покрыт.
  **Важно:** `landing_html` рендерится и сохраняется в момент запуска, так
  что уже запущенные проекты остаются со старым текстом — обновится только
  при повторном запуске того же `idea_id`.
- [x] **A15. Почтовый сканер съедал ссылку входа раньше человека.** Найдено
  кастдев-проходом и закрыто 2026-07-27. Токен гасился прямо в GET-обработчике
  `/account/verify`, а почтовые провайдеры и антивирусы (mail.ru, Яндекс,
  Kaspersky) открывают ссылки из писем сами, до адресата, чтобы проверить их
  на вредоносность. Последствий было два. Первое: **человек не мог войти
  вообще** — кликает, видит «Ссылка недействительна», просит новую, сканер
  съедает и её; замкнутый круг, в котором покупатель не открывает то, за что
  заплатил 990–2990 ₽. Второе: **сессия выдавалась сканеру** — GET отвечал
  307 с `Set-Cookie` на 180 дней. Вход перенесён на `POST /account/verify`,
  GET только показывает страницу с кнопкой и называет почту, в чей кабинет
  человек входит; сканеры ходят GET и HEAD, форму не отправляют. Заодно
  страница отказа перестала быть голым `<p>` без стилей.
  **Проверено воспроизведением:** до правки второй запрос по той же ссылке
  давал 400, после — вход проходит.
- [ ] **A16. Бесплатная проверка и платный отчёт дают РАЗНЫЕ баллы одной
  идее, рядом на экране.** Найдено 2026-07-28 на скриншоте владельца с живого
  прогона (груминг). Тизер сверху: «Общая оценка идеи — **4/10**, самое слабое
  место — спрос». Сразу под ним платный блок: «**65**/100». Разные шкалы и
  разный вывод (4/10 = 40/100) без единого слова о том, почему они расходятся.
  Человек заплатил 2990 ₽ и не знает, какому числу верить, — а это ровно то,
  что мы продаём. **Сейчас это видно на опубликованном примере `/example`,
  то есть на витрине.**
  Причина: `overall` бесплатной проверки детерминирован и **ограничен баллом
  спроса** (см. `check_demand`, задача про «балл не выше спроса» уже
  решалась), а `viability_score` — независимая оценка модели, которая про этот
  потолок ничего не знает, хотя `_VIABILITY_SPEC` описывает ей ровно ту же
  величину: «честная оценка жизнеспособности идеи в текущем виде».
  **Чинить нельзя, не выбрав — это решение владельца, не сессии:**
  (а) ограничивать ли балл отчёта спросом так же, как бесплатный. Для фаундера
  логично; для соцконтракта — вряд ли: `_VIABILITY_SPEC` там прямо говорит
  «крепкое локальное дело без роста — это высокий балл», а груминг на 480
  запросов/мес и есть такое дело, и потолок в 40/100 будет враньём в другую
  сторону (принцип 4);
  (б) либо это две РАЗНЫЕ величины — и тогда их надо назвать по-разному и
  объяснить прямо на экране, а не оставлять два числа спорить молча.
  Сессия может сделать любой из вариантов за цикл, как только выбор сделан.
- [x] **A17. Карточка проекта диктовала покупателю следующий шаг
  по-владельчески.** Найдено кастдев-проходом по платному пути и закрыто
  2026-07-28. `_smoke_card` собирает строку «следующий шаг» один раз для обеих
  панелей — владельческой `/desk` и покупательской `/account` (так и задумано,
  один язык на двоих), но написана она была не для покупателя:
  «Запустить Директ на страницу — инструкция: **/guide/direct**» (путь
  показывали текстом, кликнуть нельзя — надо перепечатывать руками),
  «идея в очередь на **MVP**», «идею **в архив**» (очередь и архив
  владельческие), «второй **оффер** на том же **трафике**» — а «оффер»
  запрещён во всех текстах для пользователя (принцип 5).
  **Сторож на запрещённые слова этого не ловил:** он сканирует `static/`, а
  строка живёт в `app/main.py` — ровно та же дыра, что в A14 с «лендингом» в
  `app/landing_template.html`. Новый тест проверяет не файл, а вывод
  `_smoke_card` по всем пяти ветвям вердикта. Ссылка на инструкцию уехала в
  отдельное поле `next_link`: строка попадает в разметку экранированной, и
  HTML в неё класть нельзя.
- [x] **A18. Проверочная страница ждала шрифты с чужого домена.** Найдено
  кастдев-проходом и закрыто 2026-07-28. B6 убрала эту зависимость со ВСЕХ
  страниц сайта — и промахнулась мимо одной: шаблон проверочной страницы лежит
  в `app/`, а сторож смотрел `static/`. **Третий раз тот же слепой угол**
  (A14 — «лендинг» в этом же файле, A17 — запрещённые слова в `app/main.py`).
  Промах пришёлся ровно на ту страницу, куда идёт ПЛАТНЫЙ трафик.
  **Замер 2026-07-28** (Chromium, шрифтовой хост «молчит» 8 с): заголовок
  появлялся через **8,07 с**, `first-contentful-paint` не наступал вовсе —
  всё это время экран белый. После правки — **0,72 с** и **ноль** внешних
  запросов. `display=swap` тут не помогает: он про подмену шрифта, а рендер
  блокирует сам тег стилей в `<head>`.
  Цена ошибки не косметическая: человек платит нам 1490 ₽ и 3–5 тысяч Яндексу,
  посетитель уходит с белого экрана, а мы по этой конверсии выносим вердикт
  «спроса нет» и говорим это заказчику как факт о рынке (принципы 1 и 8).
  Manrope/Onest/JetBrains Mono заменены на IBM Plex, который мы и так отдаём
  со своего роута — новых файлов не добавилось. Это заодно закрывает часть C4
  (страница переехала в шрифты дизайн-системы), но не всю: цвета и
  бейдж-пилюля на месте.
  **Важно для прода:** `landing_html` рендерится и сохраняется В МОМЕНТ
  ЗАПУСКА проекта. Уже запущенные страницы останутся со старым тегом, пока их
  не перезапустят под тем же `idea_id`. Проверено на стенде: до перерендера
  страница из БД по-прежнему тянула чужой домен.
- [x] **A19. Технические сообщения движков доезжали прямо до человека.**
  Найдено 2026-07-28 сплошным просмотром строковых литералов в `app/*.py` —
  там, куда сторожа по `static/` никогда не заглядывали. **Четвёртый дефект
  из того же слепого угла** (A14, A17, A18).
  Обе ошибки движков попадают на экран дословно: `/api/sharpen` отдаёт
  `str(e)`, `result.html` показывает `data.error`; у отчёта тот же путь через
  `__GEN_ERROR__`. При этом докстринг `sharpen_idea` прямо обещал «бросает
  OfferEngineError с человеческим текстом» — а проверки структуры бросали
  сырьё: «нужно ровно 3 **оффера**» (запрещённое слово на бесплатном шаге
  прямо перед решением платить), «в **оффере** нет поля demo_left_label»,
  «direct_queries: 5-12 фраз». У отчёта — «недостаточно top_risks», «пустой
  раздел finance»; это читает человек, который уже отдал 990–2990 ₽.
  Теперь у обоих исключений есть `.tech`: техническая причина уходит в лог,
  на экран идёт одна человеческая фраза. **Заодно неполный ответ модели стал
  поводом переспросить** — ровно как испорченный JSON и таймаут, которые
  движок уже переспрашивал; пропущенное поле такая же случайная осечка, а
  человек до этой правки упирался в тупик с первого раза.
  Сторож `_sources()` из B5 расширен на `app/landing_template.html`: цен и
  названий тарифов он там не нашёл, но дыра закрыта на будущее.
- [x] **A20. Правки шаблона не доходили до уже запущенных страниц, и владелец
  об этом не знал.** Найдено и закрыто 2026-07-28. `landing_html` рендерится и
  кладётся в БД **в момент запуска** проекта, поэтому правка шаблона действует
  только на будущие запуски. Само по себе это защитимо: менять страницу под
  работающей рекламой значит менять то, что измеряешь, — плейбук ровно об этом
  просит и самого покупателя. Не защитимо другое: **владелец не мог ни узнать,
  что страница отстала, ни обновить её** — перезапуск требовал руками собрать
  POST `/api/launch` с полным JSON варианта.
  Цена не теоретическая: A18 убрала со страницы рендер-блокирующий запрос
  шрифтов (замер — 8,07 с белого экрана при молчащем хосте), C4 убрала
  пилюлю — и **ни то, ни другое не доходило** до страниц, за трафик которых
  уже платят. Теперь `SmokeProject.template_hash` хранит отпечаток шаблона,
  `/api/cabinet` отдаёт `landing_stale`, `/desk` показывает предупреждение с
  кнопкой, а `POST /api/projects/{id}/refresh` (owner-only) пересобирает
  страницу из сохранённого варианта. Пересбор — **действие владельца, а не
  тихий автопересбор**: он должен видеть, что вмешивается в идущую проверку.
  Записи без отпечатка (всё, что запущено до этой правки) считаются
  устаревшими — молчать о них нельзя. Переименование проекта переживает
  пересбор: имя живёт на проекте, а не в варианте.
- [x] **A8. Порог «сигнала» 8% был завышен.** Решено владельцем 2026-07-28,
  сделано в тот же цикл. Было `SIGNAL_RATE=8%`/`DEAD_RATE=4%`; стало
  `SIGNAL_RATE=4%`/`DEAD_RATE=2%` — серая зона той же относительной ширины
  (была 4-8%, стала 2-4%), а не схлопнута в одну точку. Правка в одном месте
  (`app/main.py`), все витрины и подписи берут числа оттуда же (`__SIGNAL_PCT__`
  /`__DEAD_PCT__`, следствие B5) — обновились сами. Заодно у пресетов-примеров
  (`PRESET_OFFERS`) и у дефолта в `offer_engine._validate`/`create_project`
  жёстко зашитые 0.08/0.04 заменены на новые числа (были дублем ровно того
  класса ошибок, который правило B5 и должно было предотвратить — не поймало,
  потому что это были литералы-примеры, а не ссылки на константу; теперь
  `create_project` берёт дефолт из `SIGNAL_RATE`/`DEAD_RATE` напрямую).
  Проверять на первых живых кампаниях по-прежнему нужно — это была
  теоретическая калибровка, не измерение.

### B. Понятность и первый вход

- [x] **B1+B2. Пустой кабинет был тупиком, экрана первого входа не было.**
  Сделано 2026-07-27 одной правкой — это один экран и одна проблема. Пустые
  разделы не выводятся вовсе; когда пусто всё — экран первого входа с одним
  действием; когда есть содержимое — разделы от самого продвинутого к
  раннему плюс тихое «Проверить ещё одну идею →», ведущее на свою витрину
  по `purpose`.
- [x] **B3. Числа без объяснения.** Сделано 2026-07-27. Под «6/10» появилась
  строка, откуда взялось число (и отдельно — когда итог опущен до балла
  спроса). Вердикт теста называет порог, с которым сравнил. Главное: пороги
  переехали в константы `CLICK_TARGET/SIGNAL_RATE/DEAD_RATE` и подставляются
  в витрины — раньше главная обещала «выше 2,5%» и «~100 визитов», а движок
  считал по 8% и 40.
- [x] **B4. Мобильная вёрстка проверялась точечно**, скриншотами руками.
  Сделано 2026-07-27: `tests/test_mobile.py` — прогон 13 страниц на 390px в
  настоящем браузере, три проверки (страница не едет вбок, ни один элемент не
  шире экрана, плейсхолдеры не обрезаны). Входит в обычный
  `python -m pytest tests/`, ~16 секунд; без Playwright или Chromium модуль
  пропускается целиком. Нашёл и починил обрезанный плейсхолдер на главной.
- [x] **B5. Копии названий и чисел в статике разъезжались с сервером.**
  Сделано 2026-07-27: `_fill_server_values()` подставляет цены, названия
  тарифов, пороги вердикта и рекламный бюджет во все страницы, включая
  оферту. Главное — сторож: тесты падают, если сумма с «₽», название тарифа
  в кавычках-ёлочках, порог или бюджет снова окажутся зашиты в HTML, и
  отдельно — если слот доедет до браузера неподставленным.

- [x] **B6. Шрифты грузились с `fonts.googleapis.com`** рендер-блокирующим
  тегом в `<head>`. Сделано 2026-07-27: IBM Plex Sans/Mono (подмножества
  cyrillic+latin, 341 КБ) лежат в `static/fonts/`, отдаёт роут `/fonts/{name}`
  по белому списку. Оценка вреда в исходной записи была занижена: `display=swap`
  спасает текст, только если стили УЖЕ загружены; при недоступном домене
  блокируется отрисовка всей страницы. Замер: 0,13 с вместо ~13 с, ноль
  внешних запросов.

- [x] **B7. Состав тарифа «Бизнес-план» стал нечитаемым.** Найдено кастдев-
  проходом и закрыто 2026-07-27. C2 поставила состав тарифов к кнопке, когда
  разделов было 8; после посекционной переработки (E5) их 21, и полный тариф
  выглядел как строчная простыня из 16 фрагментов через запятую. Теперь
  полный тариф разложен по пяти группам `SECTION_GROUPS` — имя группы держит
  взгляд, а «Финансовая модель» находится в «Деньгах», а не тонет в середине
  списка (для соцконтракта это та самая строка, ради которой платят).
  Дешёвый тариф остался перечислением: пять пунктов читаются. Секция, не
  попавшая ни в одну группу, всё равно выводится — иначе раскладка умела бы
  ронять раздел с витрины. Второй половины задачи — **описывать тарифы
  выдачей, а не объёмом** («3 названные компании-аналога», «риск-реестр»,
  как у dimeadozen) — здесь нет: она пересекается с остатком E5 и делается
  вместе с ним.

- [x] **B8. «Цифра справа» — на телефоне она снизу.** Найдено кастдев-проходом
  2026-07-27, закрыто 2026-07-28. Когда Вордстат подсказывал более ходовую
  формулировку, чем угадала LLM, число в строке относилось к НЕЙ, а подпись
  объясняла это через сторону экрана: «цифра справа про неё». На узком экране
  `.freq-row` перестраивается в столбик, число уходит вниз — и подпись
  показывала не туда. Трафик из Директа преимущественно мобильный, то есть
  неверный вариант видело большинство. Чинилось не вёрсткой, а порядком:
  подпись стала отдельной строкой ПОСЛЕ числа (`.freq-item` держит рамку
  ряда) и не называет сторон вообще — «Число посчитано по более ходовой
  формулировке, которую подсказал Вордстат: "…"».
  **Тот же обман нашёлся на втором экране:** в ветке слабого спроса текст звал
  максимальную частотность «самой популярной из ВАШИХ формулировок», хотя она
  могла быть подсказкой Вордстата. Теперь «из проверенных».

- [~] **B9. «Убедиться, что всё единообразно, ровно и красиво».** Задача
  владельца от 2026-07-28, со скриншотом блока рисков. Смысл не в разовой
  вычитке глазами — так уже пробовали, и дефект дожил до прода, — а в том,
  чтобы «ровно» стало машинной проверкой, как это сделала B4 для узкого
  экрана. **Первый шаг сделан:** `test_card_grids_stay_even` находит сетки
  карточек на странице сам (display:grid + три и более ребёнка одной ширины)
  и не даёт им иметь карточку-сироту в последнем ряду или разные высоты в
  одном ряду; гоняется на четырёх ширинах. Им же и починен блок рисков.
  **Второй шаг сделан 2026-07-28:** подписи под шкалами оценки приведены к
  одному виду, и заодно закрыта дыра — у шкалы «Спрос» подписи не было вовсе
  (`check_demand` кладёт ей `note: ""`, три остальные пишет модель). Из
  четырёх ячеек одна стояла голой, причём самая важная. Теперь подпись
  дописывается из её же частотности, а пунктуация нормализуется на выдаче:
  в ячейке это подпись под числом (без точки), в тизере отчёта те же заметки
  идут абзацами (с точкой). Правится в `_polish_scores`/`_as_sentence` на
  выдаче, а не при записи — чинятся и уже сохранённые проверки.
  **Третий шаг сделан 2026-07-28:** карточка заострения склеивала два поля
  движка через « — », и объяснение боли приносило свою заглавную букву —
  «Ателье срывают сроки — Обещают три недели». Теперь название боли и
  объяснение стоят разными строками, объяснение нормализуется до предложения
  на сервере (`_polish_offers`, там же где `_polish_scores`).
  **Пункт про типографику снят:** проверено 2026-07-28 сканом всех страниц —
  кавычки-ёлочки и длинное тире единообразны, короткое тире встречается
  только в числовых диапазонах (`15–20 минут`, `3–5 тысяч ₽`), где оно и
  уместно. Чинить нечего, сторож заводить не стал.
  **Четвёртый шаг сделан 2026-07-28:** моно-шрифт вернулся к своей роли.
  Зонд по отрисованным страницам (а не по CSS) нашёл 24 места, где моно был
  набран обычный текст, в том числе **всю страницу `/contacts`** — имя ИП,
  адрес почты, названия документов. Сторож `test_mono_font_is_only_for_numbers`
  смотрит вычисленный `font-family` каждого текстового узла и пропускает
  только числа, метку «Этап N из 7» и код.
  **Пятый шаг сделан 2026-07-28:** под сторож встали последние две страницы —
  публичный пример `/example` (оказался чистым) и панель владельца `/desk`
  (восемь нарушений: название этапа рядом с меткой, подписи
  «визитов/заявок/конверсия», вердикт «СЕРАЯ ЗОНА», «следующий шаг», статус
  «оплачено», тип проекта). Правило проверяется на всех страницах сервиса без
  исключений.
  **Осталось:**
  · разнобой длины абзацев в карточках рисков: рамки ровные, но текст внутри
    заканчивается на разной глубине. Лечится не вёрсткой, а рамкой длины в
    промпте (`top_risks`) — проверить в dev нельзя, нужны ключи LLM, поэтому
    идёт вместе с E5.

### F. Аудитории: одна платформа — несколько витрин

Замысел владельца (2026-07-28): платформа умеет больше, чем соцконтракт, и
разным аудиториям нужны разные витрины под свои группы объявлений в Директе.
Не два разных сайта — иначе человек с соцконтрактом попадёт на общую версию
и не поймёт, как это про него. **Цены единые для всех аудиторий** — решение
владельца: разные цены обидят тех, кто не попал в льготную группу.

Состав: фаундеры (есть), соцконтракт (есть), студенты (следующая),
возможно гранты. Больше трёх-четырёх не планируем: каждая аудитория — это
своя персона, свой критерий балла, свои названия разделов, своя витрина и
своя реклама, а качество разбора не обкатано даже на двух.

- [x] **F1. Аудитория была размазана по четырём файлам.** Сделано 2026-07-28.
  Персона и критерий балла жили в `report_engine`, переопределения разделов —
  ключом `"social"` внутри `SECTION_SPECS`, ветка `IS_SOCIAL_CONTRACT` — в
  скрипте страницы результата, русская метка — прямо в `desk.html`. На двух
  аудиториях держалось, на трёх разъехалось бы: тот же дефект-класс, что уже
  дал расхождения в ценах (B5), названиях тарифов (B7), правиле этапа (A13) и
  адресе отчёта (E6). Появился `app/audiences.py`: одна запись описывает
  адрес витрины, метку, кто читает результат, персону, что означает балл и
  обязательна ли смета. Движок отчёта, воронка и панель владельца спрашивают
  у реестра; переопределения разделов теперь по ключу аудитории, а не
  «соцконтракт или нет». **Поведение обеих существующих аудиторий не
  изменилось** — это перенос, а не правка оптики.
- [x] **F2. Переключатель аудитории; `/social-contract` перестал быть
  тупиком.** Сделано 2026-07-28. Витрина была доступна ТОЛЬКО по ссылке из
  объявления: с сайта на неё не попасть, а пришедший по объявлению не мог
  уйти обратно, если ошибся, — единственной ссылкой был логотип, молча
  уводивший на «другой» сайт. Теперь на обеих витринах строка «Вы к нам
  зачем» со ссылками на остальные; собирается сервером из реестра
  (`_audience_switch_html`), в страницах только слот — копия в каждой витрине
  разъехалась бы, как разъезжались цены (B5). Текущая аудитория показана, но
  не ссылка. Плюс `POST /api/demand/{id}/purpose`: сменить оптику на уже
  посчитанной проверке, не проходя её заново.
  **Отменено прежнее решение** «страница не должна светиться на главной
  вовсе»: опасение сузить позиционирование верное, но запрет был слишком
  широким. Компромисс закреплён тестом — заголовок и описание пути на главной
  по-прежнему не упоминают соцконтракт, ссылка живёт только в переключателе.
  **Не сделано:** общая вёрстка витрины из реестра. Сливать две страницы в
  шаблон, чтобы потом добавить третью, дороже, чем добавить третью и
  вынести в шаблон то, что реально повторится. Единственное, что уже
  повторялось бы — сам переключатель, — вынесено.
- [~] **F3. Аудитория «студент».** Витрина и оптика сделаны 2026-07-28;
  **качество разбора на живой модели не проверено** — то же ограничение, что
  у двух других аудиторий (E1). Появились: `/students`, запись в реестре со
  своей персоной («научный руководитель, который готовит к защите» — не
  венчурный аналитик) и своим критерием балла, общий шаблон витрины
  `static/audience-landing.html` (тексты из реестра, три копии HTML
  разъехались бы, как разъезжались цены — B5).
  **Нулевой спрос решён по правилу A4:** вердикт у всех один и тот же, но
  следующий шаг разный и переехал в реестр (`Audience.weak_demand`). Фаундеру
  — «переформулируйте, не тратьте бюджет»; студенту — «это тоже результат для
  работы, если сказать о нём прямо». Ветка `IS_SOCIAL_CONTRACT` в скрипте
  страницы результата убрана: аудиторий больше не две.
  **Переключатель оптики на `/r/` сделан 2026-07-28:** строка «Разбор под
  задачу: … / Не то?» над заголовком; смена не пересчитывает спрос (цифры
  Яндекса от аудитории не зависят), меняется только оптика. Блока нет вовсе,
  если аудитория одна.
  **Осталось:** проверить разбор по студенческой идее на живых ключах — это
  за владельцем.
- [x] **F3-решения (для истории).** Оптику включаем ПОСЛЕ того, как владелец
  посмотрел живой отчёт по ней (E1 открыт даже для двух текущих). Решено с
  владельцем: студенту сильнее продаётся не бизнес-план, а **тест на реальных
  людях** — «я не придумал, я проверил, вот цифры» — это то, что отличает его
  работу на защите. Бизнес-план для студента переформулировать: не венчурный
  разбор, а «как это запустить и обо что ты споткнёшься».
  **Нерешённый вопрос снят** (замечено 2026-07-28 при повторном чтении):
  витрина `/students` прямо отвечает на него в FAQ («Мы меряем именно
  поисковый спрос — часть идей живёт вне его»), и `STUDENT.weak_demand`
  говорит то же самое на `/r/`. Решение уже реализовано в F3, просто не было
  отмечено здесь.
- [x] **F4. Кабинет вёл студента не на его витрину.** Найдено и закрыто
  2026-07-28 кастдев-проходом (после того как A16/A8 упёрлись в решения
  владельца, а остальной бэклог — в LLM-ключи и внешние токены). В
  `static/account.html` адрес «Проверить ещё одну идею» решался бинарной
  веткой `purpose === 'social_contract' ? '/social-contract' : '/'` —
  ровно тот же класс дефекта, что и F1 (аудитория размазана и хардкожена в
  файле, который её не должен знать), только в файле, который F1 не
  трогала. Студент, сохранивший проверку в кабинет, при клике «Проверить
  ещё одну идею» попадал на витрину для фаундеров, а не на `/students`.
  Тест `test_cabinet_routes_social_contract_back_to_its_own_landing`
  прямо проверял наличие старой бинарной строки — то есть закреплял баг.
  Правка: `/api/account/me` отдаёт готовый адрес `home` из реестра
  (`audiences.for_page(purpose)["home"]`), кабинет использует `d.home`
  и больше не знает слова `purpose` вовсе.
  **Побочно найдено и закрыто в этом же цикле:** прогрев статики при
  старте (`_lifespan`) читал `social-contract.html` — файл, который F2
  удалила, заменив на общий `static/audience-landing.html`. Исключение
  поймано и только залогировано (принцип 7), поэтому сервис не падал, но
  каждый рестарт писал в лог трассировку, а витрина, которая реально
  обслуживает `/social-contract` И `/students` (обе новые, под рекламу с
  завтрашнего дня), прогревом вообще не покрывалась. Список прогрева
  вынесен в константу `PREWARM_STATIC_PAGES`, добавлен тест, что каждое
  имя в ней существует на диске — тот же класс бага молча не повторится.
  **Проверено:** оба новых теста падают на старом коде (проверено откатом
  правки), 622 теста в сьюте зелёные, `tests/test_mobile.py` (22 браузерных
  теста) зелёные отдельным прогоном, живой запуск `uvicorn` — прогрев
  больше не пишет исключение в лог.
- [x] **F5. Пример отчёта (C1) мог утечь не в свою аудиторию.** Найдено и
  закрыто 2026-07-28 в той же кастдев-сессии, что F4 — тот же класс: пример
  один на весь сервис (F2/C1, «публикация нового снимает старый»), а слот
  `__EXAMPLE_LINK__` подставлялся ОДНОЙ и той же ссылкой на `/social-contract`,
  `/students` и `/r/` при любой оптике, не спрашивая, для какой аудитории
  пример собран. Если бы владелец опубликовал пример по своей идее
  (`business`), получатель соцконтракта или студент увидели бы венчурный
  разбор как «вот что вы получите» — обещание не той оптики, что мы реально
  отдадим по их заявке (принцип 4), и это разошлось бы с тем, что мы
  фактически продаём (принцип 3). Сейчас безвредно только потому, что C1
  ещё не наполнен — владелец не публиковал пример; баг ждал первой
  публикации.
  Правка: `_example_link` сравнивает аудиторию текущей страницы с
  аудиторией проверки, из которой пример собран (join через
  `ReportPurchase.check_id -> DemandCheck.purpose`), и возвращает пустую
  строку при несовпадении. `_fill_server_values` и `_audience_landing`
  теперь принимают `purpose`/`audience` и передают его дальше;
  `/r/{id}` — свежую (возможно, только что переключённую) оптику `rec.purpose`.
  **Проверено:** `test_examples_do_not_leak_across_audiences` и
  `test_example_link_appears_for_its_own_audience` падают на откаченном коде
  (проверено), существующий `test_showcases_link_the_example_once_it_exists`
  пришлось сузить — он раньше проверял именно протекание в `/social-contract`,
  закрепляя баг как ожидаемое поведение. 624 теста в сьюте зелёные (было 622).
- [x] **F6. Бесплатное заострение идеи всегда писалось «за фаундера».** Найдено
  и закрыто 2026-07-28 сплошным чтением `app/offer_engine.py` — единственного
  движка, который F1-F3 не тронули, когда разводили аудитории. `SYSTEM`-промпт
  открывался буквально «Фаундер приносит сырую идею», а пользовательское
  сообщение — «Идея фаундера: …», НЕЗАВИСИМО от того, кто на самом деле принёс
  идею. `IdeaIn.purpose` доезжал до `/api/demand` и `/r/`, но `/api/sharpen` и
  `/api/offers` звали `sharpen_idea(data.idea)` без него, а `result.html`
  вообще не клал `purpose` в тело запроса — три независимых места, где
  аудитория терялась. Это первый (и для многих единственный до оплаты) вызов
  модели в воронке: получатель соцконтракта и студент видели венчурную рамку
  заострения ровно там, где остальной сервис уже умеет говорить на трёх языках.
  Правка: `sharpen_idea(idea, *, purpose="business", …)`, системный промпт
  строится через `_system_prompt(purpose)` с подстановкой `Audience.switch_label`
  («Проверяю идею для своего дела» / «Готовлю обоснование для соцзащиты» /
  «Делаю студенческий проект») — переиспользует уже существующее поле реестра,
  не новую копию текста. `/api/sharpen` и `/api/offers` передают `data.purpose`,
  `result.html` кладёт в запрос `purpose: PURPOSE` (уже читаемую переменную
  страницы, использовавшуюся только для цели Метрики).
  **Проверено:** `test_purpose_changes_who_the_model_thinks_it_is_writing_for`
  и `test_sharpen_passes_purpose_through` падают на откаченном коде (`git
  stash`). Живьём на 390px: `#sharpen-btn` реально шлёт `{"purpose":"student"}`
  на `/r/` со студенческой оптикой (перехват `page.route` в Playwright, dev без
  LLM-ключей — дальше самого запроса проверить нечем, см. E1). 626 тестов в
  сьюте зелёные (было 624).
  **Чего не проверял:** различается ли реально КАЧЕСТВО заострения по трём
  промптам на живой модели — только то, что персона доезжает до промпта
  (тот же принцип, что и в F3 про отчёт).
- [x] **F7. Состав тарифа на `/r/` называл секцию не так, как сам отчёт.**
  Найдено и закрыто 2026-07-29 кастдев-проходом по ПЛАТНОМУ пути (F4-F6
  смотрели только бесплатный). `_tier_summary_html()` — блок «Что входит в
  тариф» у самой кнопки оплаты (C2) — строил названия секций из статического
  `ALL_SECTIONS`, то есть ВСЕГДА показывал дефолтную (business) формулировку.
  Для финансового раздела это «Финансовая модель» — а `SECTION_SPECS.by_audience`
  прямо переопределяет её для соцконтракта в «Смета и расчёты для комиссии»,
  и именно так секция называется в самом отчёте после оплаты. Получатель
  соцконтракта на `/r/` искал глазами «смету» (ради которой и платит,
  см. docstring `_tier_summary_html`), видел «Финансовая модель» — не
  опознавал в списке то, за чем пришёл, хотя после оплаты получил бы ровно
  нужное. Витрина и отчёт обязаны называть секцию одинаково.
  Правка: `_tier_summary_html(purpose="business")` берёт названия через
  `section_title(key, purpose)` вместо статического словаря; `_fill_server_values`
  (уже принимает `purpose` с F5) передаёт его дальше.
  **Проверено:** `test_tier_breakdown_names_sections_the_way_this_audience_will_see_them`
  падает на откаченном коде (`git stash`). Два существующих теста
  (`test_summary_follows_the_engine_not_a_copy`,
  `test_section_without_a_group_still_reaches_the_showcase`) патчили только
  `ALL_SECTIONS`, а не `report_engine.SECTION_SPECS` — с новой реализацией
  название читается из второго, пришлось обновить оба патча. Живьём (свежая
  БД, без LLM-ключей): `/r/` для `business` показывает «финансовая модель»,
  для `social_contract` — «смета и расчёты для комиссии» в той же группе
  «Деньги». 627 тестов в сьюте зелёные (было 626).
  **Осталось непроверенным:** такие же ли расхождения витрины и отчёта есть
  у двух других секций с `by_audience`-переопределением титула («Почему
  клиенты вернутся именно к вам», «Когда пересмотреть план») — фикс общий
  и должен закрыть все три сразу, но глазами конкретно эти две не смотрел.
- [x] **F8. Бесплатный образец отчёта кэшировался навсегда одной записью —
  не под аудиторию.** Найдено и закрыто 2026-07-29, тот же кастдев-проход
  платного пути, что F7. `_ensure_sample()` (C1/E5 — балл, объяснение и один
  настоящий раздел бесплатно, чтобы человек мог оценить качество до оплаты)
  считался «ОДИН раз на проверку и кэшировался навсегда», а не на аудиторию:
  человек, открывший `/report/{id}` под одной оптикой (образец сгенерировался
  и лёг в `DemandCheck.sample_json`), а потом переключивший оптику на `/r/`
  (`POST /api/demand/{id}/purpose` — F3), при повторном визите видел ЗАСТРЯВШИЙ
  старый образец — например, венчурный разбор фаундера на витрине, куда он
  зашёл как получатель соцконтракта. Образец существует РОВНО затем, чтобы
  убедить купить (принцип 3, докстринг функции) — показать не ту персону
  работает против собственной цели. Тот же класс дефекта, что и F5
  (пример отчёта), только кэш другой.
  Правка: `sample_json` теперь хранит словарь `{purpose: sample}` вместо
  одного плоского сэмпла; `_ensure_sample` проверяет кэш ПО ТЕКУЩЕЙ
  `rec.purpose`, генерирует заново только если для этой аудитории ещё
  ничего не было. Аудиторий всего три — переключение туда-обратно не может
  стоить владельцу больше одной генерации на каждую (не пересчитывается
  снова и снова при повторном переключении на уже виденную).
  Старые записи с плоским (домиграционным) форматом `sample_json`
  автоматически не совпадают ни с одним ключом-аудиторией — деградируют
  безопасно: один раз перегенерируются под текущую оптику вместо крэша
  (**это утверждение было в журнале непроверенным** — закрыто в цикле (49),
  см. ниже, отдельным тестом на старом формате).
  **Проверено:** новый тест
  `test_sample_regenerates_after_a_switch_then_stays_cached_per_audience`
  падает на откаченном коде (`git stash`) — старый образец бизнес-аудитории
  утекал в текст после переключения на соцконтракт. Все 7 существующих
  тестов класса `TestFreeSampleSellsTheReport` (включая «строится и
  кэшируется один раз», «покупатель не платит за образец») остались
  зелёными без изменений — поведение «один вызов модели на ту же
  аудиторию» не сломано. 628 тестов в сьюте зелёные (было 627).
  **Чего не проверял:** живую генерацию образца настоящей моделью — в dev
  нет LLM-ключей, путь проверен инъекцией `generate_core`/`generate_section`,
  как и весь остальной класс тестов.

### C. Доверие и клиентский путь

- [~] **C4. `/l/` живёт в старой дизайн-системе.** Замечено попутно с A14.
  Страница использовала оранжевый акцент и зелёную плашку из прежней
  «чертёжной» системы, а также пилюлю-бейдж, прямо запрещённую в `CLAUDE.md`.
  Отчасти это защитимо — страница представляет чужой бизнес, а не Создателя, и
  не обязана выглядеть как мы. Поэтому сначала решить: это сознательный выбор
  или наследство.
  **Сделано без владельца то, что решения не требует.** Шрифты — вместе с A18
  (внешний хост убран, страница на IBM Plex). 2026-07-28: убран
  **бейдж-пилюля** («результат · черновик готов» стоял скруглённым по высоте
  прямоугольником с цветной рамкой) — он запрещён прямым текстом, тут решать
  нечего; и снят моно-шрифт с текстовых подписей («Семьи 30–45 лет», «Ваше
  окно», «готово за секунды») — правило про моно одно на весь сайт. Обе
  проверки теперь машинные и покрывают `/l/`.
  **Осталось за владельцем:** цвета. Оранжевый акцент и зелёный «ответ» —
  единственное, что ещё отличает страницу от системы, и это ровно тот случай,
  где «страница представляет чужой бизнес» может быть верным ответом.

- [~] **C1. Примера отчёта не существует нигде.** Механика готова
  2026-07-27: страница `/example` показывает настоящий собранный отчёт
  целиком и бесплатно; владелец публикует его кнопкой на странице отчёта
  (`POST /api/example/publish`). Ссылки на витринах появляются сами, только
  когда пример опубликован. **Осталось за владельцем:** собрать хороший
  отчёт владельческим прогоном на проде и нажать «Опубликовать как пример» —
  в dev нет LLM-ключей, взять настоящий текст неоткуда. Писать пример руками
  нельзя: рукописный текст всегда глаже машинного, и человек заплатит за
  одно, а получит другое (принцип 3).
- [x] **C2. Разница тарифов не видна там, где принимается решение.** Сделано
  2026-07-27: на `/r/` у самой кнопки — оба тарифа с ценами и составом,
  собранным из `ALL_SECTIONS`/`QUICK_KEYS`. Видно, что «Финансовая модель»
  (та самая смета) есть только в полном тарифе — для соцконтракта это была
  прямая ловушка.
- [x] **C3. Политика возврата не сформулирована.** Сделано 2026-07-27, но
  задача оказалась шире: **вся оферта описывала только живой тест** — отчёта
  за 990/2990 ₽ в договоре не было вовсе. Добавлены предмет, цены (из
  `REPORT_PRICES`), сроки, возврат и ответственность по отчёту; условия
  возврата вынесены к кнопке оплаты. **Юридический текст должен прочитать и
  утвердить владелец — это его ИП и его договор.**

- [x] **C5. Распечатанный бизнес-план не выглядел документом.** Найдено
  кастдев-проходом и закрыто 2026-07-28. Для соцконтракта кнопка «Скачать PDF»
  не украшение, а способ доставки: человек несёт разбор в комиссию НА БУМАГЕ.
  Сама печать работала (`@media print` прячет шапку, оглавление и витрину
  тарифов, все 21 раздел уходят на лист), но лист был озаглавлен «Отчёт по
  идее» независимо от купленного тарифа — включая тот, что мы сами называем
  «Бизнес-план» и под этим именем продаём. Комиссия получала документ, чьё
  название не совпадает ни с чеком, ни с тем, зачем его принесли. И на листе
  не было даты. Теперь заголовок берётся из `REPORT_PRICES` (третья копия
  названий разъехалась бы, как уже разъезжались цены — B5), а под ним
  печатная строка выходных данных: тариф, дата оплаты, адрес сайта. На экране
  строка скрыта — там она лишняя. Тизер до оплаты по-прежнему «Отчёт по идее»
  и без даты: называть его купленным тарифом значит обещать то, чего человек
  ещё не получил (принцип 3).

### D. Измеримость — до того, как тратить на рекламу

- [x] **D4. Почта ломалась молча, и настраивалась вслепую.** Сделано
  2026-07-27. Владелец задаёт четыре переменные окружения в чужой панели и
  до этой правки узнавал результат только по тому, пожаловался ли покупатель.
  Появились: блок «Почта» в `/desk` (что задано, чего не хватает, кнопка
  «отправить проверочное письмо») и ручка `GET /api/diag/mail` (owner-only,
  `mailer.diagnose`), которая называет причину отказа человеческими словами,
  а техническую строку даёт следом. Пароль не возвращается никогда. Заодно
  убрана ловушка: код умел только порт 465, а провайдеры дают на выбор ещё
  587 — теперь способ шифрования выбирается по порту (`mailer._transmit`).
- [ ] **D5. У домена нет DKIM.** Проверено владельцем 2026-07-27: SPF есть
  и верный (`v=spf1 ... include:_spf.hosting.reg.ru ~all` — путь через SMTP
  reg.ru покрыт), DKIM-записи (`*._domainkey`) нет. Без подписи письма с
  нового домена системно рискуют попасть в спам у mail.ru и Яндекса, а от
  этого страдает всё сразу: вход в кабинет, письмо об оплате, уведомления
  владельцу. **Это настройка в панели reg.ru, не код** — включается в
  разделе почты либо одной заявкой в поддержку. Сессии тут делать нечего,
  задача висит как напоминание. Проверять результат — блоком «Почта» в
  `/desk` (D4) и через mail-tester.com.
- [x] **D1. Середина воронки невидима.** Сделано 2026-07-27: целей стало
  десять, реестр `METRIKA_GOALS` в `app/main.py` — единственный источник
  правды. Добавлены `demand_done`, `sharpen_used`, `check_saved`,
  `report_viewed`, `example_viewed`, `report_order_started`,
  `live_test_ordered`. Каждая цель несёт `purpose` — это же закрывает
  клиентскую половину D3. **Владельцу нужно завести цели в интерфейсе
  Метрики руками**, имена см. в `METRIKA_GOALS`.
- [x] **D2. У владельца нет вида на воронку.** Сделано 2026-07-27:
  `GET /api/funnel` (owner-only) считает восемь шагов из нашей же БД —
  проверок спроса → заострили → сохранили в кабинет → дошли до витрины
  отчёта → заказали отчёт → оплатили → заказали тест на людях → оплатили.
  Блок воронки в `/desk` показывает число, разбивку по аудиториям, «дошли
  N% с прошлого шага» и полосу доли от первого шага; период — за всё
  время / 7 / 30 дней. Владельческие прогоны отчёта (`preview`) в воронку
  не попадают, деньги считаются только по `status == "paid"`.
- [x] **D3. Нет разбивки по `purpose`** (business / social_contract).
  Клиентская половина закрыта 2026-07-27 вместе с D1: каждая цель Метрики
  несёт параметр `purpose`. Серверная закрыта в тот же день вместе с D2:
  каждый шаг воронки в `/desk` разложен на «фаундеры» и «соцконтракт»,
  заказы отчёта и живого теста наследуют аудиторию своей проверки.

### E. Ценность продукта

- [~] **E5. Отчёт не стоит своих денег.** Кастдев владельца 2026-07-27 со
  сравнением с dimeadozen.ai: «не стоит и 10 рублей». Причина архитектурная —
  весь отчёт был ОДНИМ вызовом с бюджетом 12 000 токенов на всё. **Шаги 1, 2 и 4 сделаны:**
  генерация посекционная, 8 разделов → 21, у каждого свой вопрос, страница
  собирается на глазах; бесплатно открыт настоящий кусок разбора — балл с
  объяснением, названные риски и раздел «Резюме» целиком. **Осталось:** шаг 3 —
  усилить голос по результатам живого прогона (нужны ключи LLM, только у
  владельца). И описать тарифы выдачей, а не объёмом, как у конкурента
  («3 названные компании-аналога», «риск-реестр»).
- [~] **E1. Качество платного отчёта не проверено на живой модели.** В dev нет
  LLM-ключей, промпт правился «вслепую». **Инструмент готов (2026-07-27):**
  владелец открывает `/report/{id}?key=<ключ>&preview=full` и получает полный
  тариф без оплаты — та же генерация, тот же промпт, та же оптика по
  `purpose`. Панель со ссылками видна на странице отчёта при входе с ключом.
  **Сама проверка — за владельцем:** прогнать на эталонных идеях (в т.ч.
  «пошив штор на дому» для соцконтракта) и оценить глазами. В dev это
  сделать нельзя, ключей нет.
- [x] **E2. Заострение идеи не влияет на отчёт** — закрыто вместе с A6
  2026-07-26 (дубль записи, оставлен для истории).
- [ ] **E3. Автопилот Яндекс Директа** — спека готова
  (`docs/SPEC_DIRECT_AUTOPILOT.md`), ждёт OAuth-токен с правом `direct:api`.
- [x] **E6. Результат бесплатной проверки читался по перебору.** Сделано
  2026-07-27. Вопрос «чем жертвуем» решился: **ничем.** Требовать вход
  действительно сломало бы обмен ссылкой, но обмену не мешает неугадываемый
  адрес — это и есть стандартный приём «ссылка знает того, кто её открыл».
  `DemandCheck.public_id` (случайный, с бэкфиллом при старте), страницы
  переехали на `/r/{public_id}` и `/report/{public_id}`. Порядковый номер в
  адресе принимается только у того, кто и так имеет право видеть проверку —
  владельца по ключу или её хозяина по сессии кабинета, — и им отдаётся
  редирект на канонический адрес, чтобы старая закладка сама починилась.
  **Дыра была на ДВУХ страницах:** тизер `/report/{id}` показывал текст
  чужой идеи ровно так же. Номер записи остался внутри API (заказы,
  сохранение, дозаказ разделов) — там свои проверки доступа.

- [x] **E4. Идеи нельзя было сравнить между собой.** Сделано 2026-07-27.
  Пять проверенных идей выглядели пятью одинаковыми строками, и понять,
  какая сильнее, можно было только открыв каждую. Теперь строка проверки
  несёт цифры, по которым её уже судили: общий балл, частотность и слабое
  место, — а сами строки отсортированы от сильной идеи к слабой. Порядок и
  есть ответ на вопрос «во что вкладываться». Ничего нового не считаем: всё
  это посчитано на бесплатной проверке. Проверки без балла (Вордстат
  промолчал) уходят вниз, но остаются видимыми. Битый `result_json` не
  прячет карточку — без цифр, но строка на месте (принцип 7).
  **Ограничение:** сравнение охватывает только «просто проверенные» идеи;
  те, из которых уже вырос отчёт или заявка, показаны выше своими
  разделами и в сортировку не попадают.

- [x] **E7. Битая ссылка на проверку приводила на сломанную главную.**
  Сделано 2026-07-27. Оба 404-пути (`/r/` и `/report/`) отдавали
  `_static("index.html")` в обход `_fill_server_values` — и человек читал на
  главной буквально «Больше __SIGNAL_PCT__ — идея живая, меньше __DEAD_PCT__».
  Первое, что он узнавал о сервисе, — что сервис сломан. Ссылками на
  результат делятся намеренно (это и был смысл E6), поэтому обрезанная
  мессенджером или устаревшая ссылка — обычное дело, а не экзотика.
  Второе следствие того же места: главная подменяла запрошенную страницу
  молча, и человек не понимал, куда попал. Теперь 404 идёт через
  `_lost_page()`: слоты подставлены, сверху записка о том, что проверка не
  найдена и ссылка могла быть неполной, форма бесплатной проверки — тут же.
  Сторож `_slots()` в тестах ловит любой новый неподставленный слот на
  главной.
- [x] **E8. `Audience.estimate_required` было полем без действия.** Найдено
  и закрыто 2026-07-29 после того, как систематический прогон прошлого цикла
  (по классу «кэш не знает про аудиторию») не нашёл новых мест того же
  класса — решил проверить остальные поля `Audience` на предмет «описано, но
  не читается». Поле `estimate_required` (докстринг: «обязана ли смета быть
  посчитана до копейки — комиссия спросит») стояло `True` у соцконтракта с
  момента F1, но НИГДЕ в кодовой базе не читалось: промпт финансового раздела
  прямо запрещает модели отказываться считать («Отказ считать недопустим»,
  проверено тестом `test_finance_section_must_demand_concrete_numbers` для
  ВСЕХ аудиторий), но это была просьба без проверки — непустой ответ без
  единой суммы в рублях проходил как валидный раздел ровно так же, как и
  честно посчитанная смета. Для соцконтракта смета — единственное, ради чего
  платят (это и есть весь смысл `estimate_required`): недосчитанная смета —
  не раздел похуже, а недоставленная услуга (принцип 3).
  Правка: `generate_section` в `app/report_engine.py` — для секции `finance`,
  когда `audiences.get(purpose).estimate_required`, тело раздела обязано
  содержать «₽» или «руб» (без учёта регистра), иначе `ReportEngineError`
  (тот же путь, что и для пустого раздела, — «Обновите страницу»). Проверка
  НЕ распространяется на аудитории без `estimate_required` (сейчас — все,
  кроме соцконтракта): для них финансовый раздел — один из многих, а не весь
  смысл покупки, ужесточать без решения владельца незачем.
  **Проверено:** новый тест на пустом (без сумм) ответе для соцконтракта
  падает на откаченном коде (`git stash`); симметричный тест подтверждает,
  что тот же ответ для business по-прежнему проходит (проверка узкая, не
  всем аудиториям сразу). Один существующий тест
  (`test_purpose_reaches_the_model_prompt`) использовал фейковое тело без
  сумм для соцконтракта — не о том была его цель (проверял, что персона
  доезжает до промпта), поправил фейковое тело на содержащее «₽». 632 теста
  в сьюте зелёные (было 629).
  **Чего не проверял:** ловит ли эта проверка реальный отказ модели считать
  на живых ключах — в dev их нет, путь проверен инъекцией `_post`, как и
  весь класс тестов вокруг `report_engine`.
- [x] **E9. `Audience.reader` заполнено для всех трёх аудиторий, но нигде не
  читалось.** Найдено 2026-07-29 при поиске по образцу E8 (поле-декларация
  без действия). Запись сама предлагала решение сессии: либо `reader`
  начинает реально влиять на текст промпта явной строкой, либо поле
  удаляется как избыточное. Решено 2026-07-29: `persona` описывает РОЛЬ
  модели («ты — жёсткий аналитик венчурного фонда»), но нигде не называет
  явно, кто на самом деле прочитает написанное — это разные вещи (можно
  писать от лица аналитика, ни разу не подумав о том, что текст увидит
  именно комиссия соцзащиты, а не сам заявитель). Добавлена явная строка
  «Твой текст прочитает: {reader}. Пиши так, чтобы это было полезно именно
  ему, а не абстрактному читателю» в `_core_prompt` и `_section_prompt`
  (`app/report_engine.py`), сразу после persona.
  **Проверено:** новый тест `test_reader_reaches_both_core_and_section_prompts`
  — для всех трёх `PURPOSES` строка `reader` присутствует в обоих промптах;
  падает на откаченном коде (`git stash`), проходит после правки. Полный
  сьют — см. журнал (51).
  **Чего не проверял:** влияет ли эта строка на реальный текст модели
  (меняется ли тон между «пишу для комиссии» и «пишу для жюри») — в dev
  нет LLM-ключей, проверено только тем, что строка физически доезжает до
  промпта, не тем, что модель на неё реагирует.

---

## Журнал работ

Новые записи — сверху. Формат: что сделано · что проверено · что осталось.

### 2026-07-29 (51) — E9: `Audience.reader` не влиял на промпт

**Контекст цикла.** PR #17 (A8, F4-F8, тест на F8, E8) ещё открыт, без
комментариев от владельца — продолжил на той же ветке. `git status` чистый,
`HEAD` на коммите E8. Блок A закрыт кроме A16 (ждёт решения владельца о том,
считать ли балл отчёта и балл бесплатной проверки одной величиной или
разными — не бралось в работу повторно по той же причине, что и в прошлом
цикле: это решение не сессии). Стартовый прогон — 632 теста зелёные.

**Что сделано.** E9 из прошлого цикла сам предлагал решение: либо `reader`
входит в промпт явной строкой, либо поле удаляется как избыточное. Решил в
пользу первого — `persona` и `reader` описывают разные вещи (роль модели vs.
кто реально читает результат), и не выводятся друг из друга: можно писать
от лица «жёсткого аналитика венчурного фонда», ни разу явно не подумав о
том, что текст в итоге увидит комиссия соцзащиты, а не сам заявитель. Строка
«Твой текст прочитает: {reader}. Пиши так, чтобы это было полезно именно
ему, а не абстрактному читателю» добавлена в `_core_prompt` и
`_section_prompt` (`app/report_engine.py`) сразу после persona — единое
место для обоих видов вызова (ядро + разделы).

**Что проверено.** Новый тест `test_reader_reaches_both_core_and_section_prompts`:
для всех трёх `PURPOSES` строка `reader` присутствует в тексте промпта обоих
видов вызова. Подтверждено, что тест падает на откаченном коде
(`git stash push -- app/report_engine.py`, тест красный, `git stash pop`).
Полный `python -m pytest tests/` — все тесты зелёные (было 632, стало 633).

**Чего не проверял.** Меняет ли эта строка реальный текст, который пишет
модель (тон/акценты между «для комиссии» и «для жюри конкурса») — в dev нет
LLM-ключей, доступна только проверка, что строка физически доезжает до
промпта, не то, что модель на неё реагирует иначе. Живую UI-часть не менял
в этом цикле — правка целиком внутри `report_engine.py`, Playwright не
запускал (нет визуальных изменений).

**Что осталось.** В блоке E не закрыты E1 и E5 (нужна живая модель — ключей
в dev нет, см. записи) и E3 (ждёт одобрения Яндекса на `direct:api`, спека
готова). Остальные пункты блока E — `[x]`. Следующий цикл — либо
кастдев-проход (блок A эффективно закрыт), либо A16 (если владелец наконец
ответит в PR #17).

### 2026-07-29 (50) — E8: `Audience.estimate_required` было полем без действия

**Контекст цикла.** PR #17 (A8, F4-F8, тест на F8) ещё открыт — продолжил на
той же ветке. Блок A закрыт кроме A16 (ждёт владельца, в PR по-прежнему нет
комментариев). Прошлый цикл систематически прошёл по классу «кэш не знает
про аудиторию» и новых мест не нашёл — в конце предположил, что если
следующий кастдев-проход снова найдёт дефект того же духа, стоит
подозревать не отдельные места, а более общий пробел. Решил не ждать
следующего кастдев-прохода, а сразу проверить оставшиеся поля `Audience` на
предмет «описано в дата-классе, но нигде не читается» — тот же общий
вопрос, просто с другой стороны.

**Что сделано.** `estimate_required: bool = False` (докстринг: «обязана ли
смета быть посчитана до копейки — комиссия спросит») стояло `True` у
соцконтракта с момента F1 (26-07-28) и не читалось НИГДЕ в кодовой базе —
чистое поле-декларация. Промпт финансового раздела уже прямо запрещает
модели отказываться считать («Отказ считать недопустим», это подтверждает
существующий тест для всех трёх аудиторий), но раньше это было просьбой без
проверки: непустой ответ модели без единой суммы в рублях проходил как
валидный раздел совершенно так же, как честно посчитанная смета. Для
получателя соцконтракта смета — это и есть весь смысл покупки (принцип 3):
недосчитанная смета — не более скудный раздел, а недоставленная услуга.

Правка в `generate_section` (`app/report_engine.py`): для секции `finance`,
когда `audiences.get(purpose).estimate_required`, тело обязано содержать
«₽» или «руб» — иначе `ReportEngineError` тем же путём, что и пустой раздел
(«Обновите страницу — обычно со второго раза собирается»). Проверка
специально узкая: только для аудиторий с `estimate_required=True` (сегодня
единственная — соцконтракт). Для business/student финансовый раздел один
из многих, а не единственная причина платить — ужесточать его без решения
владельца не стал.

**Что проверено.** Новый тест на пустом (без сумм) ответе для соцконтракта
падает на откаченном коде (`git stash`) — подтверждает, что дыра была
настоящей. Симметричный тест подтверждает, что тот же пустой ответ для
business по-прежнему проходит — проверка не расползлась шире задуманного.
Один существующий тест (`test_purpose_reaches_the_model_prompt`) звал
`generate_section("finance", ..., purpose=PURPOSE_SOCIAL_CONTRACT)` с
дефолтным фейковым телом без единой суммы — сам тест проверял, что персона
доезжает до промпта, а не качество тела, поэтому просто дал фейку тело с
суммой, логику теста не менял. Полный `python -m pytest tests/` — 632
зелёных (было 629).

**Чего не проверял.** Ловит ли эта проверка реальный отказ модели считать
на живых ключах — в dev их нет, путь проверен инъекцией `_post`, тем же
способом, что и весь класс тестов report_engine до этого цикла.

**Что осталось.** A16 и A8 (порог применён, живая проверка впереди) — за
владельцем. Остальной бэклог — за владельцем или ждёт LLM-ключей/токенов.
Прошёл `grep` по остальным полям `Audience`: `plan_first`, `weak_demand`,
`switch_label`, `persona`, `viability`, `label`, `slug` реально читаются
вне `audiences.py`. **Нашёл ещё один похожий кандидат, не стал чинить в
этом же цикле** — поле `reader` («кто читает результат — от этого зависит
вся оптика разбора») заполнено для всех трёх аудиторий, но нигде не
читается за пределами `audiences.py`: только два теста проверяют его
непустоту как часть общей проверки «запись описана полностью», ни один
код-путь не подставляет его в промпт или на витрину. В отличие от
`estimate_required`, это не давало ложного впечатления работающей защиты —
скорее избыточная документация человеку-читателю поверх `persona`, которая
реально уходит в промпт. Оставляю как задачу на будущее: либо решить, что
`reader` должен реально влиять на текст (например, войти в промпт явной
строкой «Читатель: …»), либо удалить как избыточное.

### 2026-07-29 (49) — Систематический прогон по классу «кэш не знает про аудиторию» + закрыт долг честности F8

**Контекст цикла.** PR #17 (A8, F4-F8) ещё открыт — продолжил на той же
ветке. Блок A закрыт кроме A16 (ждёт владельца, в PR по-прежнему нет
комментариев). Прошлый цикл (F8) сам себе оставил пометку: четыре цикла
подряд (F4-F8) находили один и тот же класс дефекта («состояние, посчитанное
один раз для одной аудитории, не инвалидируется при смене оптики, или
размазано по файлу, который её не должен знать») по одному кастдев-проходом
— и предложил вместо пятого прохода поискать оставшиеся места целенаправленно.

**Что сделано.** Прошёл по всем местам в `app/main.py`, где `purpose`
читается, кэшируется или подставляется в шаблон (`_sections_meta`,
`example_page`, `report_page`, `_owner_preview`, `_tier_summary_html`,
`_example_link`, `/api/funnel`), сверяя каждое с тем же вопросом: не
показывает ли оно что-то, посчитанное для ОДНОЙ аудитории, когда текущая —
другая. Новых мест того же класса **не нашёл** — F4 (кабинет), F5 (пример),
F6 (заострение), F7 (состав тарифа), F8 (образец) закрыли всё, что реально
кэшируется или ветвится по аудитории на сегодня. Проверил заодно соседние
гипотезы (общий rate-limit на все ручки сразу, а не по эндпоинту; сброс
всей карты rate-limit при 10000 IP) — оба оказались осознанными, явно
задокументированными в коде компромиссами для нынешнего масштаба, а не
случайными дырами; трогать не стал, доказательств, что они уже мешают, нет.

По дороге нашёл **долг честности за собой**: журнал F8 (прошлый цикл)
утверждал «старые записи с плоским форматом sample_json деградируют
безопасно», но это не было проверено тестом — только прочитано в коде и
предположено. Написал тест
`test_old_flat_sample_format_degrades_to_a_fresh_one_instead_of_crashing`:
кладёт в `DemandCheck.sample_json` буквально старый (домиграционный)
плоский JSON, дёргает `/report/{id}` и проверяет, что страница не падает,
показывает СВЕЖИЙ образец под текущую оптику (а не старый плоский текст) и
пересчитывает модель ровно один раз, не при каждом визите. Тест прошёл на
уже закоммиченном коде — значит утверждение в журнале F8 было верным, но
теперь это факт, а не предположение.

**Что проверено.** Новый тест на старом формате: 9 тестов класса
`TestFreeSampleSellsTheReport` зелёные. Полный `python -m pytest tests/` —
629 зелёных (было 628, ровно +1 новый тест, кода не менял).

**Чего не проверял.** Ничего сверх того, что уже написано в F4-F8 —
живая генерация LLM по-прежнему недоступна в dev.

**Что осталось.** A16 и A8 (порог применён, живая проверка впереди) — за
владельцем. Остальной бэклог — за владельцем или ждёт LLM-ключей/токенов.
Класс «кэш без аудитории» похоже закрыт полностью — если следующий
кастдев-проход снова найдёт такой же дефект, стоит уже подозревать не
отдельные места, а архитектурный пробел (например, отсутствие единой
обёртки «кэшируй по purpose» для всего, что зависит от аудитории).

### 2026-07-29 (48) — F8: бесплатный образец отчёта не обновлялся при смене оптики

**Контекст цикла.** PR #17 (A8, F4-F7) ещё открыт — продолжил на той же
ветке. Блок A закрыт кроме A16 (ждёт владельца). Прошлый цикл (F7) снова
нашёл проблему кастдев-проходом по ПЛАТНОМУ пути (не бесплатному, который
F4-F6 уже покрыли) — прошёлся тем же путём ещё раз, теперь глядя конкретно
на кэширование.

**Что сделано.** `_ensure_sample()` — бесплатный образец платного разбора
на `/report/{id}` (балл, объяснение, один настоящий раздел целиком; C1/E5,
существует РОВНО затем, чтобы убедить купить, — это прямо написано в
докстринге функции) — считался «один раз на проверку и кэшировался
навсегда» одной плоской записью в `DemandCheck.sample_json`, а не на
аудиторию. Человек, открывший `/report/{id}` под одной оптикой (образец
сгенерировался под неё и лёг в кэш), а потом переключивший оптику на `/r/`
(`POST /api/demand/{id}/purpose`, механизм F3), при следующем визите видел
ЗАСТРЯВШИЙ старый образец — скажем, венчурный разбор фаундера на витрине,
куда он зашёл как получатель соцконтракта. Ровно тот класс дефекта, что и
F5 (пример отчёта не проверял аудиторию совпадения), только другой кэш —
третий раз за четыре цикла один и тот же паттерн: артефакт, посчитанный
один раз, не инвалидируется при смене оптики.

Правка: `sample_json` теперь хранит `{purpose: sample}` вместо одного
плоского объекта. `_ensure_sample` проверяет наличие ключа ТЕКУЩЕЙ
`rec.purpose` в кэше — если его там нет (первый раз для этой аудитории),
генерирует и дописывает под этим ключом, не трогая уже посчитанные для
других аудиторий. Аудиторий всего три — переключение туда-обратно не
может стоить владельцу больше одной генерации на каждую: то, что уже
считали, повторно не считаем. Старые (домиграционные) плоские записи
`sample_json` не совпадают ни с одним ключом-аудиторией — деградируют
безопасно, просто перегенерируются один раз под текущую оптику вместо
крэша на парсинге.

**Что проверено.** Новый тест
`test_sample_regenerates_after_a_switch_then_stays_cached_per_audience`
падает на откаченном коде (`git stash`) — воспроизвёл ровно сценарий
находки: сгенерировал под business, переключил на social_contract,
убедился, что старый текст утекает в ответ. Все 7 существующих тестов
`TestFreeSampleSellsTheReport` (включая «строится и кэшируется один раз на
ту же аудиторию», «покупатель не платит за образец», «соцконтракту нельзя
показывать венчурный разбор даже в образце») остались зелёными без
изменений — поведенческий контракт «один вызов модели на ту же аудиторию»
не сломан, только добавлена размерность «на какую именно». Полный
`python -m pytest tests/` — 628 зелёных (было 627).

**Чего не проверял.** Живую генерацию образца настоящей моделью — в dev
нет LLM-ключей, путь проверен инъекцией `generate_core`/`generate_section`,
тем же способом, что и весь остальной класс тестов до этого цикла.

**Что осталось.** A16 и A8 (порог применён, живая проверка впереди) — за
владельцем. Остальной бэклог — за владельцем или ждёт LLM-ключей/токенов.
Четыре цикла подряд (F4-F8, пять правок) один и тот же паттерн — состояние,
посчитанное один раз для одной аудитории, не инвалидируется при смене
оптики или размазано по файлу, который не должен её знать. Возможно, стоит
явно поискать, не осталось ли в кодовой базе ЕЩЁ мест того же класса
(`grep` по всем полям, кэширующим что-либо, связанное с `purpose`), вместо
того чтобы находить их по одному кастдев-проходом.

### 2026-07-29 (47) — F7: состав тарифа на `/r/` называл секцию не так, как отчёт

**Контекст цикла.** PR #17 (A8, F4-F6) ещё открыт — продолжил на той же
ветке. Блок A закрыт кроме A16 (ждёт владельца). Прошлый цикл (F6) оставил
пометку: следующему стоит попробовать кастдев-проход ПЛАТНЫМ путём, а не
только бесплатным — F4-F6 всё смотрели до оплаты. Сделал это.

**Что сделано.** `_tier_summary_html()` — блок «Что входит в тариф» прямо у
кнопки оплаты на `/r/` (C2) — строил названия секций из статического
`ALL_SECTIONS`: `titles = dict(ALL_SECTIONS)`, то есть всегда брал ДЕФОЛТНУЮ
(business) формулировку названия, игнорируя `SECTION_SPECS.by_audience`.
Для финансового раздела дефолт — «Финансовая модель», а для соцконтракта
`by_audience` переопределяет его в «Смета и расчёты для комиссии» — и
именно так секция называется в самом отчёте после оплаты (`section_title(key,
purpose)` это уже умел, просто `_tier_summary_html` его не звал). Получатель
соцконтракта на `/r/` перед оплатой ищет глазами «смету» — ради неё и
платит, это прямо написано в докстринге функции — а видел «Финансовая
модель» и мог не опознать в списке то, за чем пришёл, хотя после оплаты
получил бы ровно нужный раздел под другим именем. Витрина и отчёт обязаны
называть одну и ту же секцию одинаково.

Правка: `_tier_summary_html(purpose="business")` теперь строит словарь
названий через `section_title(k, purpose)` (уже импортирован в `main.py`,
уже понимает `by_audience`) вместо статического `dict(ALL_SECTIONS)`.
`_fill_server_values` уже принимал `purpose` с прошлого цикла (F5) — просто
прокинул его в вызов `_tier_summary_html(purpose)`.

**Что проверено.** Новый тест
`test_tier_breakdown_names_sections_the_way_this_audience_will_see_them`
падает на откаченном коде — проверено `git stash`. Два СУЩЕСТВУЮЩИХ теста
(`test_summary_follows_the_engine_not_a_copy`,
`test_section_without_a_group_still_reaches_the_showcase`) патчили для
проверки «движок ↔ витрина не разъезжаются» только `main.ALL_SECTIONS` —
с новой реализацией название читается из `report_engine.SECTION_SPECS`,
и оба патча пришлось расширить (иначе падали с «неизвестная секция»,
а не проверяли то, что задумано). Живьём: поднял `uvicorn` на отдельной
scratch-БД (не трогая закоммиченный `sozdatel.db` — в прошлый раз задел его
случайно ручной проверкой, вернул `git restore`), создал по одной проверке
на `business` и `social_contract`, curl `/r/{id}` — группа «Деньги»
показывает «финансовая модель» и «смета и расчёты для комиссии»
соответственно. Полный `python -m pytest tests/` — 627 зелёных (было 626).

**Чего не проверял.** Такие же ли расхождения были у двух других секций с
`by_audience`-переопределением титула («Почему клиенты вернутся именно к
вам» — retention, «Когда пересмотреть план» — pivot/exit): фикс общий и
математически должен закрыть все три сразу (`titles` строится для ЛЮБОГО
ключа из `ALL_SECTIONS` одинаково), но глазами конкретно эти две не смотрел
и отдельного теста на них не написал.

**Что осталось.** A16 и A8 (порог применён, живая проверка впереди) — за
владельцем. Остальной бэклог — за владельцем или ждёт LLM-ключей/токенов.
Кастдев платного пути дал результат уже дважды подряд не по бесплатному
(F7 сейчас, до этого F4/F5 по кабинету и примеру) — возможно, стоит ещё
раз пройти именно оплату/вебхук ЮКассы третьей аудиторией, если в
следующем цикле снова не найдётся задачи из бэклога.

### 2026-07-28 (46) — F6: заострение идеи всегда писалось «за фаундера»

**Контекст цикла.** PR #17 (A8, F4, F5) ещё открыт — продолжил на той же
ветке. Блок A закрыт кроме A16 (ждёт владельца). Остальной бэклог блокирован
на владельце (C1, C4, D5) или на LLM-ключах/токенах (E1, E5, B9-остаток, E3).
Третий цикл подряд кастдев-прохода по трём витринам: F4 и F5 нашли, что
кабинет и пример отчёта не знали про третью аудиторию — на этот раз проверил
СВОБОДНЫЙ (бесплатный, до всякой оплаты) шаг заострения идеи, единственный
движок, который F1-F3 не тронули вовсе.

**Что сделано.** `app/offer_engine.py` — единственное место в кодовой базе,
где ещё жил хардкод «Фаундер»: системный промпт открывался «Фаундер приносит
сырую идею», пользовательское сообщение — «Идея фаундера: …», и это
происходило абсолютно для ЛЮБОГО посетителя, включая пришедшего с
`/social-contract` или `/students`. Хуже: `purpose` даже не доезжал до
вызова — три независимых места теряли его порознь:
`/api/sharpen`/`/api/offers` в `app/main.py` звали `sharpen_idea(data.idea)`
без параметра, хотя `IdeaIn.purpose` был доступен; а `static/result.html`
вообще не клал `purpose` в тело запроса на `/api/sharpen`, хотя ПЕРЕМЕННАЯ
`PURPOSE` уже была в скрипте (использовалась только для цели Метрики
двумя строками ниже). Заострение — первый и для большинства единственный
до оплаты вызов модели в воронке: получатель соцконтракта и студент читали
венчурную рамку позиционирования ровно там, где остальной сервис давно
умеет говорить на трёх языках (принцип 4).

Правка: `sharpen_idea(idea, *, purpose="business", …)`; системный промпт
собирается через `_system_prompt(purpose)`, подставляя `Audience.switch_label`
из реестра («Проверяю идею для своего дела» / «Готовлю обоснование для
соцзащиты» / «Делаю студенческий проект») — переиспользует уже существующее
поле, не изобретает новую копию текста персон. И `/api/sharpen`, и
`/api/offers` передают `data.purpose`; `result.html` кладёт в тело запроса
`purpose: PURPOSE`.

**Что проверено.** Два новых теста
(`test_purpose_changes_who_the_model_thinks_it_is_writing_for`,
`test_sharpen_passes_purpose_through`) падают на откаченном коде — проверено
`git stash`. Два существующих теста были жёстко завязаны на буквальный текст
«Идея фаундера» в пользовательском сообщении — обновлены на новый нейтральный
префикс «Идея:», сама персона теперь живёт в системном промпте, а не в
пользовательском вводе. Живьём на 390px через Playwright (`page.route`
перехватывает `/api/sharpen` до сети): реальный DOM-клик по `#sharpen-btn`
на `/r/` со студенческой проверкой (`purpose="student"` в БД) отправляет
`{"purpose":"student"}` в теле запроса. Полный `python -m pytest tests/` —
626 зелёных (было 624).

**Чего не проверял.** Различается ли реально КАЧЕСТВО трёх заострений на
живой модели — только то, что верная персона доезжает до промпта, не то,
что модель по ней хорошо пишет (в dev нет LLM-ключей, тот же принцип, что
и в E1/F3 про платный отчёт).

**Что осталось.** A16 и A8 (порог применён, живая проверка впереди) — за
владельцем. Остальной бэклог — за владельцем или ждёт LLM-ключей/токенов.
Отдельных задач у сессии не осталось; следующему циклу, возможно, стоит
попробовать кастдев-проход самим ПЛАТНЫМ путём (заказ/оплата отчёта),
а не только бесплатным — там ещё не смотрели третьей аудиторией.

### 2026-07-28 (45) — F5: пример отчёта мог утечь не в свою аудиторию

**Контекст цикла.** PR #17 (A8 + F4) ещё открыт, не смержен — продолжил на
той же ветке. Блок A закрыт, кроме A16 (ждёт решения владельца — коротко
объяснил в чате в прошлый раз, подтверждения выбора пока нет). Остальной
бэклог блокирован либо на владельце (C1, C4, D5), либо на LLM-ключах (E1,
E5, B9-остаток), либо на внешнем токене (E3). Продолжил кастдев-проход по
трём витринам, начатый в прошлом цикле (F4).

**Что сделано.** Механика публичного примера (C1) не знала про аудитории:
`_example_link()` подставляла одну и ту же ссылку на `/example` на витрины
`/social-contract`, `/students` и на `/r/` при любой выбранной оптике,
не спрашивая, для кого пример собран. Пример — ровно один на весь сервис
(так решили в F2, чтобы два «примера» не разъехались, как разъезжались
копии цен), и если бы владелец опубликовал пример по бизнес-идее (обычный
путь — сначала появится проверка своего дела), получатель соцконтракта
или студент увидели бы венчурный разбор как обещание того, что получат
сами, — прямое нарушение принципа 4 (одна аудитория — одна оптика) и
принципа 3 (продаём то, что реально отдаём). Сейчас это не проявлялось на
проде только потому, что C1 всё ещё не наполнен: владелец не публиковал
пример, баг ждал первой публикации молча.

Правка: `_example_link(text, purpose)` сравнивает аудиторию страницы с
аудиторией DemandCheck, из которого пример собран (`ReportPurchase.check_id
-> DemandCheck.purpose`), и не отдаёт ссылку при несовпадении.
`_fill_server_values`/`_audience_landing` теперь принимают и передают
`purpose`/`audience`; `/r/{id}` передаёт `rec.purpose` — актуальную оптику,
в том числе после переключения на самой странице (F3-остаток).

**Что проверено.** Два новых теста (`test_examples_do_not_leak_across_audiences`,
`test_example_link_appears_for_its_own_audience`) падают на откаченном коде
— проверено `git stash`. Существующий `test_showcases_link_the_example_once_it_exists`
пришлось сузить: он проверял именно протекание примера на `/social-contract`,
то есть закреплял баг как ожидаемое поведение — тот же урок, что был с
`account.html` в F4. Полный прогон `python -m pytest tests/` — 624 зелёных
(было 622), включая браузерный `test_mobile.py`.

**Чего не проверял.** Живую публикацию настоящего примера на проде — в dev
нет LLM-ключей, путь проверен только конструированием `ReportPurchase` через
`_built_report`/`monkeypatch`, как и раньше в `TestPublicReportExample`.

**Что осталось.** A16 и A8 (порог применён, проверка на живых кампаниях
впереди) — за владельцем. Остальной бэклог — за владельцем или ждёт
LLM-ключей/токенов; отдельных задач у сессии не осталось.

### 2026-07-28 (44) — F4: кабинет и прогрев статики не знали про третью аудиторию

**Контекст цикла.** PR #16 (F1-F3, A8) уже смержен владельцем — ветка
перезапущена от `main`, единственный немерженный коммит (A8) перенесён,
открыт новый PR #17. Блок A закрыт полностью, кроме A16 — он ждёт решения
владельца (объяснил коротко в чате, предложил вариант, владелец пока не
подтвердил выбор; менять балл самовольно нельзя, роадмап прямо это
запрещает). Весь остальной открытый бэклог блокирован либо на владельце
(C1, C4, D5), либо на LLM-ключах (E1, E5, B9-остаток), либо на внешнем
токене (E3). Провёл кастдев-проход по трём витринам вместо задачи из
бэклога.

**Что сделано.** Два бага одного семейства, оба — тот же класс дефекта,
что и F1 («аудитория размазана и хардкожена в файле, который не должен её
знать»), только F1 их не покрыла, потому что смотрела на другие файлы:

1. `static/account.html` решала, куда вести за следующей идеей, бинарной
   веткой `purpose === 'social_contract' ? '/social-contract' : '/'`.
   Студент, сохранивший проверку, попадал на витрину для фаундеров.
   Хуже — существовавший тест `test_cabinet_routes_social_contract_back_to_its_own_landing`
   прямо проверял наличие этой строки, то есть закреплял баг как
   ожидаемое поведение. Правка: `/api/account/me` отдаёт `home` из
   реестра (`audiences.for_page(purpose)["home"]`), кабинет использует
   `d.home` и про `purpose` больше не знает.
2. `_lifespan` прогревал `social-contract.html` — файл, который F2
   удалила в тот же день, заменив на `audience-landing.html`.
   Исключение поймано и залогировано (принцип 7), сервис не падал, но
   каждый рестарт писал трассировку в лог, а сама `audience-landing.html`
   (обслуживает и `/social-contract`, и `/students` — обе новые витрины
   под рекламу с завтрашнего дня) прогревом не покрывалась вовсе. Список
   вынесен в константу `PREWARM_STATIC_PAGES`, чтобы тест мог проверить
   его на диске.

**Что проверено.** Оба новых теста (`test_home_url_for_each_audience`,
`test_every_prewarmed_name_exists_on_disk`) падают на старом коде — на
первом убедился откатом файлов, на втором временной подменой имени в
списке прогрева. `python -m pytest tests/` — 622 зелёных (было 620).
`tests/test_mobile.py` — 22 браузерных теста зелёные отдельным прогоном
(в общем прогоне один раз словил ложные падения из-за собственного
параллельного `uvicorn`+Chromium, оставленного вручную запущенным —
не регрессия, воспроизвёл и опроверг перезапуском). Живой `uvicorn`:
до правки лог прогрева пишет `FileNotFoundError` трассировку при
каждом старте, после — старт чистый.

**Что осталось.** A16 и A8 (порог уже применён, но проверка на живых
кампаниях — впереди) числятся за владельцем. Остальной бэклог — за
владельцем или ждёт LLM-ключей/токенов, отдельных задач у сессии не
осталось.

### 2026-07-28 (43) — A8 решён владельцем + найден и закрыт баг деградации отчёта

**Что сделано.** Владелец снизил порог «сигнала» перед началом рекламы: было
`SIGNAL_RATE=8%`/`DEAD_RATE=4%`, стало `SIGNAL_RATE=4%`/`DEAD_RATE=2%` —
серая зона той же относительной ширины (была 4-8%, стала 2-4%), не схлопнута
в точку. Правка в одном месте (`app/main.py`), витрины берут числа через
`__SIGNAL_PCT__`/`__DEAD_PCT__` (B5) — обновились сами. Заодно у пресетов
`PRESET_OFFERS` и у дефолтов в `offer_engine._validate`/`create_project`
жёстко зашитые 0.08/0.04 заменены на актуальные — были дублем-литералом
ровно того класса, который B5 и должно было выкорчевать, но не поймало,
потому что это примеры значений, а не ссылки на константу.

**Побочная находка при прогоне тестов.** `generate_core`/`generate_section`
в `report_engine.py` ловили `httpx.HTTPError`/`TimeoutException`, но не
`llm_adapter.LLMAdapterError` — свою же ошибку, которую `llm_adapter`
бросает при не-200 ответе модели (обрыв связи ловился, явный отказ API —
нет). В `dev`-окружениях без сетевого доступа к Яндексу это никогда не
проявлялось: транспортная ошибка (`httpx.ConnectError`) — подтип
`httpx.HTTPError`, ловилась. Именно в этом окружении сеть до Яндекса
дотягивается (через прокси) и настоящий 401 от тестового ключа вскрыл дыру:
страница отчёта падала вместо честной деградации (принцип 7). Добавил
`llm_adapter.LLMAdapterError` в оба except. Без этой находки было бы 15
ложных падений тестов, никак не связанных с A8 — заодно чиню то, что могло
уронить платную страницу на бою при любом настоящем отказе Яндекса (квота,
невалидный ключ, технический сбой), а не только при обрыве сети.

**Что проверено.** Полный прогон `python -m pytest tests/` — 620 тестов
зелёных (был 1 тест, завязанный на старые пороги 4-8% для примера серой
зоны, — поправил числа в примере, логику не трогал).

**Что осталось.** Порог по-прежнему теоретическая калибровка, не измерение —
проверить на первых живых кампаниях. A16 ждёт решения владельца. По блоку A
других задач не осталось.

### 2026-07-28 (42) — F3 (остаток): оптику стало видно и можно сменить

**Что сделано.** Закрыл гап, который сам же создал два цикла назад: ручка
`POST /api/demand/{id}/purpose` появилась, а кнопки к ней не было. Человек,
попавший не на ту витрину, читал результат чужими глазами и мог только начать
всё сначала. Хуже — на `/r/` вообще ничего не говорило, ЧЬИМИ глазами он
читает, хотя от этого зависит многое: что стоит главным действием, что мы
отвечаем при слабом спросе и какой персоной модель напишет платный разбор.

Теперь над заголовком тихая строка: «Разбор под задачу: Проверяю идею для
своего дела · Не то? …» с кнопками остальных аудиторий. Спрос при смене не
пересчитывается — это цифры Яндекса, они от аудитории не зависят; страница
просто пересобирается сервером под новую оптику. Если аудитория одна, блока
нет вовсе: выбирать не из чего, и строка только мешала бы.

**Что проверено.** 6 тестов, 3 падали на старом поведении. Ключевой — что
после смены цифры частотности ОСТАЛИСЬ те же (иначе смена оптики молча
переписывала бы данные), и что платный разбор, собранный после смены, берёт
новую персону: смена бессмысленна, если движок её не увидит. Отдельный сторож
на случай единственной аудитории. Живьём на 1000 и 390 px: строка читается,
переключение туда и обратно работает, ни одного сырого слота, ничего не шире
экрана. Вся сьюта: 620 зелёных (было 614).

**Что решил НЕ делать и почему.** Смотрел, не нужны ли студенту свои
переопределения разделов отчёта (`by_audience`), как они есть у соцконтракта.
Вывод: почти нет. Соцконтракту они нужны, потому что там другой читатель
(комиссия) и другая цель (выплата); у студента читатель — преподаватель,
который задаёт по сути те же вопросы, что и фаундер себе. Выдумывать различия
ради симметрии не стал. Если владелец на живом прогоне увидит, что какой-то
раздел звучит не для студента, — заведём точечно.

**Чего НЕ проверял.** Качество студенческого разбора на живой модели: ключей
LLM в dev нет. Проверено, что нужная персона доезжает до промпта, а не то,
что модель отвечает по ней хорошо.

**Что осталось.** По F3 — живая проверка разбора владельцем. По блоку A —
A8 и A16, оба ждут его решения. Своих задач у сессии в бэклоге не осталось.

### 2026-07-28 (41) — F3: третья аудитория и общий шаблон витрины

**Что сделано.** Появилась витрина `/students` и аудитория «студент» в
реестре. Заодно обе аудиторские витрины переехали на общий шаблон
`static/audience-landing.html`: тексты приходят из реестра, вёрстка одна.
Прошлый цикл я откладывал это решение — сливать две страницы, чтобы потом
добавить третью, дороже, чем добавить третью и вынести общее. Третья
появилась, общее вынесено.

**Оптика студента.** Персона — не венчурный аналитик, а научный руководитель,
который готовит к защите: «сделай так, чтобы работа выдержала вопросы —
откуда цифры, почему заплатят, что мешает повторить». Балл — не про
амбициозность, а про то, можно ли по разбору действительно начать и защитить
решение. Цены единые для всех аудиторий: решение владельца, разные цены
обидят тех, кто не попал в льготную группу.

**Нулевой спрос — главный вопрос этой аудитории — решён без вранья.** Прятать
слабый вердикт нельзя: принцип 1 не обсуждается, и принцип 2 тоже — вердикт
имеет право сказать «нет». Но A4 уже установила правило: вердикт СООБЩАЕТ
находку и не предписывает следующий шаг, потому что шаг у аудиторий разный.
Теперь это записано данными: `Audience.weak_demand`. Фаундеру — «переформулируйте,
не тратьте рекламный бюджет»; студенту — «это тоже результат для работы, если
сказать о нём прямо: либо назовите идею словами, которыми её ищут, либо
объясните, откуда придут клиенты, если не из поиска». Тот же вопрос отдельно
разобран в FAQ витрины.

Ветка `IS_SOCIAL_CONTRACT` из скрипта страницы результата убрана — она
означала «аудиторий ровно две». Данные для страницы отдаёт
`audiences.for_page()`.

**Что проверено.** 9 новых тестов, 8 падали на старом поведении. Десять старых
пришлось обновить — они ссылались на удалённый `social-contract.html` или
предполагали две аудитории; **один из них поймал реальную потерю**: при
переносе текстов в реестр я потерял оговорку «Обоснование сметы **в тарифе
"Бизнес-план"**», то есть витрина начала обещать смету без уточнения, что в
дешёвом тарифе её нет (принцип 3). Возвращена. Вся сьюта: 614 зелёных
(было 605). Живьём на 1000 и 390 px: обе витрины, переключатель со всеми
тремя аудиториями, одинаковые цены, ни одного сырого слота.

**Чего НЕ проверял, и это важно.** Что студенческая оптика даёт хороший
разбор на живой модели — ключей LLM в dev нет. Это ровно то же ограничение,
что висит на двух других аудиториях (E1): промпт написан, результат никто не
видел. Прежде чем лить рекламу на `/students`, владельцу стоит прогнать
владельческим прогоном студенческую идею и посмотреть глазами.

**Что осталось.** F3 — живая проверка разбора и кнопка смены оптики прямо на
`/r/` (ручка есть, кнопки нет). По блоку A — A8 и A16, ждут владельца.

### 2026-07-28 (40) — F2: витрина соцконтракта перестала быть тупиком

**Что сделано.** Витрина `/social-contract` существовала только как адрес для
объявления. С сайта на неё было не попасть: ни с главной, ни из подвала, ни со
страницы результата. Человек, которому нужно обоснование для комиссии,
приходил на главную, читал про венчурную проверку идеи и уходил, не узнав, что
мы и про него тоже. Обратное так же верно: пришедший по объявлению не мог уйти
на витрину фаундера, если ошибся, — единственной ссылкой был логотип, который
молча уводил на «другой» сайт.

Теперь на обеих витринах строка «Вы к нам зачем» со ссылками на остальные
аудитории. Собирается сервером из реестра (F1), в страницах только слот:
копия разметки в каждой витрине разъехалась бы ровно так же, как разъезжались
цены и названия тарифов. Текущая аудитория показана, но не ссылка — вести
человека туда, где он уже стоит, значит тратить его клик.

Плюс `POST /api/demand/{id}/purpose`: сменить оптику разбора на уже посчитанной
проверке. Человек мог прийти не с той витрины — это обычное дело, если он нашёл
нас поиском, а не по объявлению, — и гонять его через проверку заново незачем:
спрос посчитан, меняется только то, чьими глазами читать результат.

**Отменено прежнее решение, и это важно записать.** Тест прямо требовал: «на
главной не должно быть ссылки на `/social-contract`, чтобы не отпугнуть
массового посетителя». Опасение верное — сужать позиционирование главной не
надо. Но запрет был слишком широким и стоил нам целой аудитории. Компромисс
закреплён новым тестом: заголовок, подзаголовок и описание пути на главной
по-прежнему не упоминают соцконтракт; ссылка живёт только в переключателе.
Причина отмены записана в докстринг теста, чтобы следующая сессия не откатила
её вслепую (как это уже случалось в A12).

**Что проверено.** 8 новых тестов, 7 падали на старом поведении; один старый
переписан вместе с обоснованием. Отдельный сторож требует, чтобы каждая
аудитория из реестра появлялась в переключателе сама — иначе третья витрина
молча не попадёт в навигацию. Другой запрещает нашим внутренним словам
(«аудитория», «оффер», ключи вроде `business`) попадать в подписи: человек
говорит про себя иначе. Живьём на 1000 и 390 px: переход с главной на витрину
соцконтракта и обратно работает, ничего не шире экрана. Вся сьюта: 605 зелёных
(было 597).

**Чего НЕ делал и почему.** Общую вёрстку витрины из реестра. Сливать две
страницы в один шаблон, чтобы потом добавить третью, дороже, чем добавить
третью и вынести в шаблон то, что реально повторится. Единственное, что уже
повторялось бы, — сам переключатель, — вынесено. Ещё не сделано: переключатель
оптики на самой странице `/r/` (ручка есть, кнопки нет) — это к F3, когда
станет ясно, как студенческая оптика обходится с нулевым спросом.

**Что осталось.** F3 — аудитория «студент» и нерешённый вопрос про нулевой
поисковый спрос. По блоку A — A8 и A16, оба ждут решения владельца.

### 2026-07-28 (39) — F1: аудитория стала первоклассной сущностью

**Что сделано.** Владелец решил делать витрины под разные аудитории — студенты,
соцконтракт, фаундеры, возможно ещё одна. Прежде чем добавлять третью, свёл
описание аудитории в одно место: оно было размазано по четырём файлам и
четырьмя разными способами.

Персона для модели и критерий балла — двумя словарями в `report_engine`.
Переопределения разделов — ключом `"social"` прямо внутри `SECTION_SPECS`, то
есть «аудиторий ровно две». Ветка `IS_SOCIAL_CONTRACT` — в скрипте страницы
результата. Русская метка «фаундеры / соцконтракт» — копией в `desk.html`.

Это тот же дефект-класс, который я ловил весь день: копия правила в двух
местах молча разъезжается. На нём уже разъезжались цены (B5), названия тарифов
(B7), правило этапа (A13) и адрес отчёта (E6). На трёх аудиториях он сработал
бы гарантированно.

Появился `app/audiences.py`: одна запись — адрес витрины, метка для воронки,
кто читает результат, персона, что означает балл, обязательна ли смета.
Движок отчёта спрашивает персону и критерий у реестра, переопределения
разделов лежат под ключом аудитории (`by_audience`), воронка отдаёт русские
названия сервером, панель владельца их принимает.

**Что проверено.** 8 тестов, 7 падали на старом поведении. Среди них два
сторожа против возврата копий: в движке не должно остаться `_PERSONA` и
`_VIABILITY_SPEC`, а в разделах — ключа `"social"`. Отдельно закреплено, что
переезд не потерял смысл оптики: «Смета и расчёты для комиссии» против
«Финансовая модель» на том же разделе. Вся сьюта: 597 зелёных (было 589).
Живьём: `/api/funnel` отдаёт метки, панель владельца рисуется как раньше на
1000 и 390 px.

**Что НЕ изменилось — и это главное.** Поведение обеих существующих аудиторий
осталось прежним. Это перенос, а не правка оптики: видимых изменений для
пользователя в этом цикле нет, и выдавать их за улучшение продукта я не буду.

**Чего НЕ проверял.** Что промпты после переезда дают тот же текст на живой
модели: ключей LLM в dev нет. Проверено, что персона и критерий из реестра
попадают в собранный промпт, а не что модель отвечает так же.

**Что осталось.** F2 — витрины из реестра и переключатель аудитории
(`/social-contract` до сих пор недоступен с сайта). F3 — студенты, после того
как владелец посмотрит живой отчёт. По блоку A — A8 и A16, ждут владельца.

### 2026-07-28 (38) — B9 шаг 5: сторож моно-шрифта закрыл последние две страницы

**Что сделано.** Цикл небольшой и честно об этом говорит: закрыл последнее
белое пятно, которое сам же три цикла подряд записывал в «осталось» — под
сторожем моно-шрифта не было `/desk` и `/example`.

`/example` оказался чист. `/desk` — восемь нарушений: «Этап 4 из 7 — Заявки»
целиком моно (правило разрешает моно только самой метке), подписи под числами
«визитов», «заявок», «конверсия», текстовый вердикт «СЕРАЯ ЗОНА», надпись
«следующий шаг», статус заказа «оплачено» и тип проекта «проверка спроса ·
Создатель». Всё это текст, а не числа. Починено по местам; метка этапа
разделена так же, как в кабинете, — моно осталось на «Этап N из 7».

Панель владельца за ключом, поэтому в тесте она отдельным шагом: ключ кладётся
в `sessionStorage` и страница перезагружается — так же, как это делает живой
человек. Через `?key=` в адресе панель не открывается, ключ она читает только
из хранилища.

**Что проверено.** Сторож падал с полным списком до правок — 8 нарушений на
`/desk`, ноль на `/example`. После — зелено, вся сьюта 589. Живьём на 1000 и
390 px: метка этапа читается, подписи под числами обычным шрифтом, ничего не
шире экрана.

**Заодно починена сама карточка B9 в бэклоге:** два цикла назад абзац про
четвёртый шаг вклинился между «Осталось:» и его пунктом, и список читался как
каша. Порядок восстановлен.

**Чего НЕ делал.** Ничего сверх правила: цвета, размеры и раскладка `/desk`
не тронуты.

**Что осталось.** По блоку A — A8 и A16, оба ждут решения владельца. По C4 —
цвета проверочной страницы. По B9 — только рамка длины абзацев в карточках
рисков, и она упирается в ключи LLM (идёт вместе с E5). Больше своих задач у
сессии в бэклоге нет: следующий цикл — кастдев-проход.

### 2026-07-28 (37) — A20: починки не доезжали до живых страниц

**Что сделано.** Два последних цикла закончились одинаковой припиской: «уже
запущенные проверочные страницы останутся со старым шаблоном». Сначала это
выглядело оговоркой, теперь видно, что это отдельный дефект — и он
обесценивает обе предыдущие правки.

`landing_html` собирается и кладётся в БД в момент запуска. Значит A18 (убрала
8 секунд белого экрана на платном трафике) и C4 (убрала запрещённую пилюлю)
**не дошли ни до одной уже работающей страницы**. Обновить их владелец не мог:
никакой ручки нет, перезапуск требует руками собрать POST `/api/launch` с
полным JSON варианта. Хуже — он не мог даже узнать, что страница отстала.

Тихо пересобирать всё при старте я не стал, и это осознанно: страница может
стоять под работающей рекламой, а менять её во время проверки значит менять
то, что измеряешь. Об этом же плейбук просит самого покупателя. Поэтому
решение принимает владелец, а система лишь показывает состояние: у проекта
хранится отпечаток шаблона, `/desk` показывает предупреждение и кнопку
«Пересобрать страницу», подтверждение прямо говорит, что люди увидят
обновлённую версию.

**Что проверено.** 8 тестов, 7 падали на старом поведении. Отдельно закреплено,
что записи БЕЗ отпечатка (всё, что уже запущено на проде) считаются
устаревшими — иначе правка молчала бы ровно про те страницы, ради которых
делалась. И что переименование переживает пересбор: имя живёт на проекте, а не
в варианте, иначе владелец получил бы обратно машинное название.
Живьём на 1000 и 390 px, на странице, состаренной руками до вида «как на
проде»: предупреждение видно, кнопка на месте, до пересбора страница отдаёт
внешний шрифтовой хост, после — нет, предупреждение исчезает. Вся сьюта:
589 зелёных (было 581).

**Чего НЕ проверял.** Миграцию `ALTER TABLE ... ADD COLUMN template_hash` на
живом PostgreSQL: доступа к продовой БД нет. В dev (SQLite) колонка создаётся
вместе с таблицей, поэтому ветка миграции там не исполняется вовсе — она
написана по образцу восьми предыдущих в том же блоке.

**Что это значит для владельца.** После деплоя все существующие проекты
покажутся в `/desk` устаревшими — это правда, они собраны до правок. Нажать
«Пересобрать страницу» стоит хотя бы для тех, по которым идёт реклама: там
лежит тот самый белый экран из A18.

**Что осталось.** По блоку A — A8 и A16, оба ждут решения владельца. По C4 —
цвета. По B9 — рамка длины абзацев в карточках рисков (ключи LLM). Под
сторожем моно-шрифта нет `/desk` и `/example`.

### 2026-07-28 (36) — C4 частично: с проверочной страницы убраны пилюля и моно

**Что сделано.** Взял из C4 то, что не требует решения владельца. Карточка
задачи прямо говорит: бейдж-пилюлю «убрать в любом случае». Плюс правило про
моно-шрифт одно на весь сайт, и оно не про вкус.

Зонд по отрисованной странице нашёл ровно два нарушения: пилюля-бейдж
«результат · черновик готов» (скруглённый по высоте прямоугольник с цветной
рамкой) и моно-шрифт на текстовых подписях — «Семьи 30–45 лет, ремонт в
квартире», «Ваше окно», «готово за секунды» и та же надпись в бейдже. Пилюля
заменена обычной подписью, моно снят.

Главное, как обычно, не правка, а то, что `/l/` наконец встала под сторожа.
Их два: общий поиск пилюль по всем страницам (скруглённый по высоте элемент с
заливкой или рамкой и текстом внутри — на остальных страницах чисто) и уже
существующий сторож моно-шрифта, расширенный на проверочную страницу.

**Ловушка, на которую я попался, и она важная.** Оба новых сторожа позеленели
с первого запуска. Это было враньё: в посеве мобильного прогона проект
`sweep1` создавался с `landing_html="<h1>тест</h1>"` — заглушкой вместо
настоящей страницы. Сторожа честно проверяли пустой `<h1>` и ничего не
находили. Посев переведён на настоящий `render_landing`, после чего оба теста
упали с точным списком нарушений. **Третий раз подряд** тот же урок (A13, A19):
заглушка обязана совпадать с реальностью, иначе она не проверяет, а успокаивает.

**Что проверено.** Оба сторожа падали на старом шаблоне и на настоящем рендере
— проверено до правки. Живьём на 1000 и 390 px: ноль внешних запросов,
заголовок IBM Plex Sans, форма заявки принимает контакт, зонд по пилюлям и
моно чист. Вся сьюта: 581 зелёный (было 579).

**Чего НЕ трогал.** Цвета: оранжевый акцент и зелёный «ответ» остались. Это
единственное, что ещё отличает страницу от дизайн-системы, и ровно тот случай,
где «страница представляет чужой бизнес, а не Создателя» может быть верным
ответом. Решение владельца, не сессии.

**Напоминание про прод (из A18, действует и здесь):** `landing_html`
сохраняется в БД в момент запуска проекта. Уже запущенные проверочные страницы
сохранят и пилюлю, и моно, пока их не перезапустят под тем же `idea_id`.

**Что осталось.** По блоку A — A8 и A16, оба ждут решения владельца. По C4 —
цвета. По B9 — рамка длины абзацев в карточках рисков (ключи LLM). Под
сторожем моно-шрифта до сих пор нет `/desk` и `/example`.

### 2026-07-28 (35) — A19: движки показывали человеку имена своих полей

**Что сделано.** Прошлый цикл закончился тем, что дефект в третий раз подряд
отсиделся в `app/`, пока сторожа смотрели `static/`. Поэтому начал не с новой
страницы, а с самого слепого угла: расширил сторож B5 на
`app/landing_template.html` (цен и названий тарифов там не оказалось — дыра
закрыта на будущее) и прошёл сплошняком по строковым литералам всех модулей
`app/`.

Нашёлся четвёртый дефект той же породы, и он живее предыдущих.

Текст исключений обоих движков уходит на экран **дословно**: `/api/sharpen`
отдаёт `str(e)`, `result.html` рисует его как есть; у отчёта тот же путь через
`__GEN_ERROR__`. Докстринг `sharpen_idea` при этом прямо обещает «бросает
OfferEngineError с человеческим текстом». Обещание не выполнялось:

  · «нужно ровно 3 **оффера**» — запрещённое слово, на бесплатном шаге прямо
    перед решением платить;
  · «в **оффере** нет поля demo_left_label», «pains должен содержать 3 блока»,
    «direct_queries: 5-12 фраз» — имена полей из нашего JSON;
  · у отчёта «недостаточно top_risks», «пустой раздел finance» — это читает
    человек, который уже отдал 990–2990 ₽.

Случай не экзотический: модель отдаёт три объекта по одиннадцать обязательных
полей, и промах по одному полю — обычная осечка.

У обоих исключений появился `.tech`: техническая причина уходит в лог, на
экран идёт одна человеческая фраза. И раз осечка случайная — движок теперь
**переспрашивает**, как он уже делал для испорченного JSON и таймаута. До
правки человек упирался в тупик с первого раза.

**Что проверено.** 7 тестов, все падали на старом поведении. Отдельный сторож
разбирает `app/*_engine.py` через AST и не даёт ни одному сообщению об ошибке
нести запрещённое слово или имя поля. Живьём на стенде, где модель отвечает
валидным JSON без одного поля: человек читает «ИИ вернул неполный ответ.
Попробуйте ещё раз — обычно со второго раза получается», кнопка снова активна,
а в логе лежит «в варианте нет поля h1 (attempt 1/2)» — видно и что
переспросили. Вся сьюта: 579 зелёных (было 572).

**Ловушка, на которую я попался.** Первые три теста позеленели сразу — и это
было враньё: моя заглушка `_post` возвращала anthropic-образный ответ, движок
падал в ветку «битый JSON» и до проверок структуры не доходил вовсе. Форма
ответа должна совпадать с настоящим провайдером по умолчанию
(`_yandex_response`). Тот же урок, что в A13: заглушка обязана совпадать с
реальностью, иначе она не проверяет, а успокаивает.

**Чего НЕ проверял.** Осечки модели смоделированы подменой `llm_adapter.call`,
а не реальным ответом Яндекса: ключей в dev нет. Проверялось, что движок
делает с неполным ответом, а не то, как часто он приходит.

**Что осталось.** По блоку A — A8 и A16, оба ждут решения владельца. C4: шрифты
приведены (A18), остались цвета и бейдж-пилюля («убрать в любом случае» —
можно без владельца). По B9 — рамка длины абзацев в карточках рисков (ключи
LLM). Под сторожем моно-шрифта нет `/desk`, `/example` и `/l/`.

### 2026-07-28 (34) — A18: страница, на которую льют платную рекламу, ждала Google

**Что сделано.** Пошёл смотреть `/l/` — единственную страницу, которую видят
посторонние люди и на которую заказчик тратит свой рекламный бюджет. Собирался
заняться разрешённой частью C4 (бейдж-пилюля), а нашёл в шапке шаблона тег
стилей на внешний шрифтовой хост.

Это ровно то, что B6 вычищала по всему сайту. Она прошла по `static/`, а
шаблон проверочной страницы лежит в `app/` — **третий раз подряд тот же слепой
угол**: так же отсиделось слово «лендинг» (A14) и запрещённые слова в
`app/main.py` (A17). И промах пришёлся на самую дорогую страницу из всех.

Померил честно, а не «по общим соображениям». Если хост просто отказывает,
всё быстро — но из России он обычно не отказывает, а молчит. Смоделировал
молчание 8 секунд: заголовок появился через **8,07 с**, `first-contentful-paint`
не наступил ни разу — всё это время белый экран. После правки тот же сценарий
даёт **0,72 с** и ноль внешних запросов.

Считать это косметикой нельзя. Человек платит нам 1490 ₽ и 3–5 тысяч Яндексу,
посетитель уходит с белого экрана — а мы по этой конверсии выносим вердикт
«спроса нет» и говорим это заказчику как факт о его рынке. Порча идёт прямо в
продукт.

Manrope/Onest/JetBrains Mono заменены на IBM Plex, который у нас уже лежит и
отдаётся своим роутом: новых файлов не добавилось, внешних запросов не
осталось. Побочно закрылась часть C4 — страница переехала на шрифты
дизайн-системы; цвета и бейдж-пилюля там пока свои.

**Что проверено.** 5 тестов, 4 падали на старом поведении. Один из них общий:
в отрисованной странице не должно остаться ни одного `http://`/`https://`.
Другой сверяет использованные семейства с тем, что реально лежит в
`static/fonts/fonts.css` — иначе можно убрать чужой запрос и остаться без
шрифта. Живьём на 1000 и 390 px: ноль внешних запросов, заголовок набран IBM
Plex Sans, форма заявки принимает контакт. Вся сьюта: 572 зелёных (было 567).

**Ловушка, на которую я попался и которую надо помнить.** Первый замер после
правки показал те же 8 секунд. Причина не в правке: `landing_html` рендерится
и **сохраняется в БД в момент запуска** проекта. Стенд отдавал страницу,
собранную до фикса. **На проде это значит то же самое:** уже запущенные
проверочные страницы останутся со старым тегом, пока их не перезапустят под тем
же `idea_id`. Новые получат исправленную сразу.

**Чего НЕ проверял.** Молчание хоста смоделировано задержкой в Playwright, а
не реальной недоступностью Google из России. Числа сравнимы между собой (до и
после, один сценарий), но это не замер с настоящего мобильного интернета.

**Что осталось.** По блоку A — A8 и A16, оба ждут решения владельца. C4 стала
уже: шрифты приведены, остались цвета и бейдж-пилюля («убрать в любом
случае» — можно делать без владельца). По B9 — рамка длины абзацев в карточках
рисков (ключи LLM). Под сторожем моно-шрифта до сих пор нет `/desk`,
`/example` и `/l/`.

### 2026-07-28 (33) — C5: лист для комиссии был озаглавлен не тем, что купили

**Что сделано.** Блок A стоит на решениях владельца, у B9 остался пункт под
ключи LLM — снова кастдев-проход. На этот раз по тому, что вообще ни разу не
проверялось: **как разбор выглядит на бумаге**. Для аудитории соцконтракта это
не мелочь — комиссии нужен лист, и «Скачать PDF» для неё способ доставки, а не
украшение.

Собрал полный бизнес-план на 21 раздел и посмотрел его в печатной эмуляции и
настоящим PDF. Механика печати оказалась в порядке: шапка, оглавление, витрина
тарифов и кнопки скрыты, все разделы уходят на лист, ничего не обрезано.
Отдельно проверил оглавление — пять пунктов в нём без ссылок оказались
заголовками групп, а не битыми якорями; ложная тревога.

А вот сам документ был озаглавлен **«Отчёт по идее»** — независимо от того, что
человек купил. Включая тариф, который мы сами называем «Бизнес-план», так
продаём и так пишем в чеке. Комиссия получала бумагу, название которой не
совпадает ни с чеком, ни с тем, зачем её принесли. И **даты на листе не было
вовсе** — документ без даты для комиссии не документ, притом что дата у нас
готовая, это день оплаты.

Заголовок теперь берётся из `REPORT_PRICES`, а не пишется в шаблоне: третья
копия названий тарифов разъехалась бы ровно так же, как уже разъезжались цены.
Под ним печатная строка выходных данных — тариф, дата, адрес сайта; на экране
она скрыта, там от неё пользы нет. Тизер до оплаты остался «Отчётом по идее» и
без даты: называть неоплаченную страницу купленным тарифом значит обещать то,
чего человек не получил.

**Что проверено.** 7 тестов, 5 падали на старом поведении. Отдельным тестом
закреплено, что заголовки берутся из прайса по всем тарифам сразу, и отдельным
— что тизер до оплаты бизнес-планом не зовётся. Сторож незаполненных слотов
поймал два новых слота, пока я не зарегистрировал их — сработал как надо.
Живьём: заголовок «Бизнес-план» на экране и на листе, строка «Бизнес-план по
идее · подготовлен 28 июля 2026 г. · projectsozdatel.ru» видна только в печати,
на 390 px страница не едет. Снят PDF формата A4. Вся сьюта: 567 зелёных
(было 560).

**Чего НЕ проверял.** Тексты разделов на стенде — заглушка одинаковой длины,
не вывод живой модели: ключей LLM в dev нет. Проверялась структура листа, а не
читаемость настоящего разбора на бумаге. Печать смотрел эмуляцией Chromium;
как это ляжет в чужом браузере или на реальном принтере, не проверял.

**Что осталось.** По блоку A — A8 и A16, оба ждут решения владельца. По B9 —
рамка длины абзацев в карточках рисков (ключи LLM). Отдельно: под сторожем
моно-шрифта до сих пор нет `/desk`, `/example` и `/l/`.

### 2026-07-28 (32) — B9 шаг 4: моно-шрифтом был набран обычный текст, включая целую страницу

**Что сделано.** Взял пункт, замеченный в прошлом цикле: метка этапа в кабинете
набрана моно целиком, вместе с названием этапа. Но проверять это глазами по CSS
бессмысленно — шрифт задаётся в одном месте, а виден совсем в другом. Поэтому
написал зонд, который обходит отрисованную страницу и у КАЖДОГО текстового узла
смотрит вычисленный `font-family`.

Нашлось 24 места, и картина оказалась шире одной метки:

  · **вся страница `/contacts` набрана моно** — «Индивидуальный предприниматель»,
    «Белкин Борис Ильич», адрес почты, названия всех трёх документов. Наследство
    старой «чертёжной» системы, которое пережило переход на светлую;
  · «Этап 3 из 7 — Тест на реальных людях» в кабинете и в плейбуке: правило
    разрешает моно самой метке, а не названию этапа;
  · подписи под числами — «визитов», «заявок», «конверсия»;
  · текстовые вердикты «РАНО СУДИТЬ» и «СЕРАЯ ЗОНА»;
  · заголовки шагов в плейбуке — «Что увидите», «Если отклонили»;
  · строка «Редакция от 21 июля 2026 г.» на всех трёх юридических страницах.

Всё это правило `CLAUDE.md` запрещает прямым текстом: моно — только для чисел
и метки этапа. Починено по местам; там, где число и слово стоят рядом
(«0 / 40 визитов», «Этап 3 из 7 — Тест на реальных людях»), моно оставлен
ровно на числе, а слово вынесено в отдельный узел.

**Что проверено.** Сторож написан ДО правок и падал с полным списком —
19 нарушений на публичных страницах плюс 5 в кабинете (кабинет за входом,
поэтому он в тесте отдельным шагом с логином). После правок зелено. Вся сьюта:
560 зелёных (было 559). Живьём на 1000 и 390 px: `/contacts`, кабинет и
страница проекта — скриншоты сняты, горизонтального скролла нет, ИНН и ОГРНИП
остались моно, как и положено числам.

**Чего НЕ проверял.** Зонд обходит десять страниц из списка. Страницы, которых
в списке нет (`/desk`, `/example`, `/l/`, `/report` без покупки), под сторожем
не стоят — `/desk` намеренно: это владельческая панель, и правило про кашу
шрифтов там менее критично, но нарушения в ней могут быть.

**Что осталось.** По B9 — только рамка длины абзацев в карточках рисков, она
упирается в ключи LLM (идёт с E5). По блоку A — A8 и A16, оба ждут решения
владельца.

### 2026-07-28 (31) — A17: покупателю диктовали следующий шаг словами владельца

**Что сделано.** Блок A стоит на решениях владельца, у B9 остался только
пункт под ключи LLM — поэтому кастдев-проход, на этот раз по ПЛАТНОМУ пути.
Прошёл его целиком: заказ теста на реальных людях, оплата (довёл заказ до
`paid` тем же способом, что и вебхук ЮКассы, с автозапуском проекта), вход в
кабинет по ссылке из письма, карточка проекта, страница `/p/`, плейбук Директа.

Дефект нашёлся в кабинете, в строке «следующий шаг». `_smoke_card` собирает её
один раз для обеих панелей — владельческой и покупательской. Это осознанно,
один язык на двоих. Но язык был владельческий:

  · «Запустить Директ на страницу — инструкция: `/guide/direct`» — человеку,
    заплатившему 1490 ₽, показывали путь в адресной строке. Не ссылка, обычный
    серый текст: чтобы открыть инструкцию, её надо перепечатать руками;
  · «Сигнал есть → идея в очередь на MVP» — очередь владельца, не покупателя;
  · «Спроса нет → идею в архив» — архив тоже владельческий;
  · «Серая зона → второй **оффер** на том же **трафике**» — «оффер» запрещён
    во всех текстах для пользователя, и это не стилистика, а принцип 5.

**Сторож на запрещённые слова это пропускал**, потому что сканирует `static/`,
а строка живёт в `app/main.py`. Та же дыра, что нашлась в A14: «лендинг»
отсиделся в `app/landing_template.html`. Поэтому новый тест проверяет не файл,
а сам вывод `_smoke_card` — по всем пяти состояниям, через которые проходит
проект. Отдельным тестом закреплено, что состояний действительно пять: сторож
бесполезен, если ветви на самом деле не разошлись.

Ссылка на инструкцию вынесена в отдельное поле `next_link`. HTML в `next_step`
класть нельзя — строка уходит в разметку экранированной, и именно поэтому путь
изначально оказался голым текстом.

**Что проверено.** 6 новых тестов, 5 падали на старом поведении. Два старых
теста ждали прежних формулировок — обновлены. Живьём на 1000 и 390 px: строка
читается, «Пошаговая инструкция →» кликается и действительно открывает плейбук
(проверил переходом, а не только атрибутом `href`). Вся сьюта: 559 зелёных
(было 553).

**Чего НЕ проверял.** Оплату довёл до конца сам, повторив шаги вебхука
(`status = paid` + автозапуск проекта) — настоящей кассы в dev нет, вебхук от
ЮКассы не приходил. Проверялось, что показывает кабинет по оплаченному
заказу, а не то, что ЮКасса присылает именно такой вебхук.

**Что нашёл попутно и НЕ трогал.** Метка этапа в кабинете набрана моно-шрифтом
целиком — `.stage-txt` красит и «Этап 3 из 7», и название «Тест на реальных
людях», хотя `CLAUDE.md` разрешает моно только для чисел и самой метки.
Добавлено в B9.

**Что осталось.** По блоку A — A8 и A16, оба ждут решения владельца. По B9 —
моно-шрифт в метке этапа и рамка длины абзацев в карточках рисков (последнее
упирается в ключи LLM).

### 2026-07-28 (30) — B9 шаг 3: карточка заострения склеивала два поля движка

**Что сделано.** Начал с оставшегося пункта B9 про типографику — и закрыл его
без правки: скан всех страниц показал, что кавычки-ёлочки и длинное тире уже
единообразны, а короткое тире стоит только в числовых диапазонах, где оно и
уместно. Выдумывать сторож ради сторожа не стал.

Поэтому пошёл кастдевом за фаундера — и впервые дошёл до шага заострения не
кнопкой «Пропустить». Прежние проходы его пропускали, поэтому дефект дожил.

Движок отдаёт боль двумя полями: `h2` — название, `p` — объяснение, каждое
самостоятельное предложение (на посадочной `/l/` они и рисуются отдельными
элементами). Карточка склеивала их через « — » в одну строку, и объяснение
приносило с собой свою заглавную букву:

> Ателье срывают сроки — Обещают три недели, шьют полтора месяца

Заглавная после тире посреди строки — не опечатка модели, а наш шаблон: так
склеилась бы любая пара. И это последний бесплатный экран перед решением
платить.

Теперь название боли и объяснение — разные строки, как «Для кого» рядом.
Пунктуацию приводит сервер (`_polish_offers`), там же, где живёт нормализация
подписей из прошлого цикла; название боли не трогаем, это заголовок.
Нормализация повешена на оба пути выдачи вариантов — `/api/sharpen`
(покупатель) и `/api/offers` (владелец).

**Что проверено.** 4 текстовых теста (2 падали на старом поведении) плюс
браузерный сторож на двух ширинах: ответ `/api/sharpen` подменяется прямо в
браузере через `page.route` — в dev ключей LLM нет, а проверяем мы отрисовку,
а не движок. Сторож смотрит не подстроку, а геометрию: объяснение обязано
стоять НИЖЕ названия, а не в одну строку с ним. Проверил, что он работает:
откатил `result.html` — обе ширины падают. Вся сьюта: 553 зелёных (было 547).
Живьём на 1000 и 390 px, скриншот снят.

**Чего НЕ проверял.** Варианты заострения на стенде — заглушка: настоящий
`sharpen_idea` требует ключей LLM. Проверялось, как карточка рисует пару
полей, а не что именно модель в эти поля кладёт.

**Что осталось.** По B9 — только рамка длины абзацев в карточках рисков, и
она упирается в ключи LLM (идёт вместе с E5). По блоку A оба пункта ждут
решения владельца: A8 (порог 8%) и A16 (какой из двух баллов главный —
расписан двумя вариантами).

### 2026-07-28 (29) — B9 шаг 2: у шкалы «Спрос» вообще не было подписи

**Что сделано.** Блок A стоит на решениях владельца (A8 — порог 8%, A16 —
какой из двух баллов главный), поэтому взял следующий шаг B9: единообразие
подписей.

Собрал стенд на РЕАЛЬНОЙ форме данных — и оказалось, что дефект крупнее
пунктуации. `check_demand` кладёт шкале спроса `note: ""`: три остальные шкалы
объясняет модель, а спрос считается по данным, и объяснять его было некому.
На экране из четырёх ячеек одна стояла голой — с числом и названием, но без
строчки под ними. Голой была самая важная: спрос единственный посчитан по
реальным цифрам Яндекса и он же потолок общего балла (B3 была ровно про то,
что числа обязаны объяснять себя).

**Фикстуры это прятали.** В посеве мобильного прогона у «Спроса» подпись
была — «1 200 запросов в месяц», — чего настоящий движок не делает. Тот же
урок, что в A13: заглушка обязана совпадать с реальным поведением, иначе она
не проверяет, а успокаивает. Посев исправлен на пустую строку.

Второй дефект — пунктуация. Модель пишет фрагменты как придётся: «Рынок
растёт.» с точкой рядом с «Начать можно одной» без. В сетке из четырёх ячеек
это читается как небрежность.

Оба лечатся одним местом — `_polish_scores` на выдаче страницы, а не при
записи: тогда чинятся и уже сохранённые проверки, и правило живёт в одном
месте на одном языке, а не копией в шаблоне. Подпись к спросу собирается из
его же частотности (`_demand_caption`, никаких новых чисел — принцип 1), нет
частотности — так и написано «Частотность недоступна». Пунктуация разведена
по контексту: в ячейке это подпись под числом и точка не нужна
(`_as_caption`), в тизере отчёта те же заметки идут отдельными абзацами и там
точка обязательна (`_as_sentence`).

**Что проверено.** 6 тестов, все падали на старом поведении — проверено до
правки. Живьём на 900 и 390 px: четыре ячейки читаются одинаково («480
запросов в месяц» / «Много частных мастеров» / «Рынок растёт» / «Начать можно
одной»), в тизере все пять абзацев заканчиваются точкой. Разделитель тысяч —
неразрывный пробел, тот же, что даёт `toLocaleString('ru-RU')` выше по
странице. Вся сьюта: 547 зелёных (было 541).

**Чего НЕ проверял.** Тексты заметок на стенде — руками составленные образцы
с разной пунктуацией, а не вывод живой модели: ключей LLM в dev нет.
Проверялось, что нормализация делает с такими строками, а не то, какие строки
модель присылает на самом деле.

**Что осталось.** По B9 — рамка длины абзацев в карточках рисков (нужны ключи
LLM, идёт с E5) и сторож на единые кавычки и тире. По блоку A — оба пункта
ждут решения владельца, A16 подробно расписан двумя вариантами.

### 2026-07-28 (28) — B9: «ровно» стало машинной проверкой, а не глазом

**Что сделано.** Владелец опубликовал первый пример отчёта (C1 закрыта с его
стороны) и прислал скриншот с претензией: блоки одинакового размера, а текст
в них уходит вниз на разную глубину и вообще выглядит случайно.

Воспроизвёл на стенде с настоящей вёрсткой отчёта и померил рамки карточек на
четырёх ширинах. Претензия верная, но причина не та, что кажется. Блок рисков
верстался через `grid-template-columns: repeat(auto-fit, minmax(220px,1fr))`,
то есть число колонок подбиралось под ширину окна. На 1200 px выходило четыре
колонки, на 390 — одна, и оба варианта смотрелись прилично. А на 900 px (обычная
ширина окна на ноутбуке, ровно то, что на фото) выходило **три карточки в ряд
и четвёртая одна под ними**, с двумя третями пустоты справа. Дефект жил ровно
в промежутке — поэтому точечные скриншоты его и не показывали.

Починено тем, что рисков всегда чётное число: 2 в быстром разборе, 4 в
бизнес-плане (`_risk_count`). Значит две колонки дают полные ряды при любом
тарифе, и подбор колонок под окно тут не нужен вовсе.

Главное в цикле — не сама правка, а сторож. `test_card_grids_stay_even` не
знает списка селекторов: он ищет сетки на странице сам — `display:grid`, три и
более видимых ребёнка, все одной ширины (последнее отсекает раскладочные сетки
вроде «30px 1fr» под номер и текст). Дальше две проверки: в одном ряду карточки
одной высоты, и в последнем ряду не может быть одинокой карточки, если
предыдущие ряды заполнены. Шесть страниц × четыре ширины. Новый блок попадает
под проверку без правки теста.

**Что проверено.** Сторож найден работающим ДО правки: он падал с точным
адресом дефекта — «отчёт на 900px: .risks — карточка-сирота в последнем ряду
(ряды по [3, 1])», и отдельно то же самое на `/example`, то есть на витрине.
После правки — 2×2, высоты в ряду совпадают, на 1200/700/390 тоже чисто.
В посев мобильного прогона добавлены `viability_score` и четыре риска
намеренно РАЗНОЙ длины: на ровном содержимом такая проверка ничего не значит.
Вся сьюта: 541 зелёный (было 537). Снят скриншот отчёта на 900 px.

**Чего НЕ проверял.** Тексты рисков на стенде — заглушка, скопированная со
скриншота владельца, а не вывод живой модели: ключей LLM в dev нет. Проверялась
геометрия сетки на реалистичных по длине текстах, а не то, какой длины тексты
приходят от Яндекса на самом деле.

**Что нашёл попутно и НЕ трогал.** Заведена **A16** — на том же экране
бесплатная проверка говорит «Общая оценка идеи — 4/10», а платный отчёт прямо
под ней «65/100». Два числа об одной идее, разные шкалы, разный вывод, ни
слова о том, почему они расходятся. Это видно на опубликованном примере.
Не взял в работу сознательно: починка требует выбора, который принадлежит
владельцу, — либо балл отчёта ограничивается спросом так же, как бесплатный
(для соцконтракта это, скорее всего, неверно, см. принцип 4), либо это две
разные величины и их надо назвать по-разному. Подробности и оба варианта — в
карточке A16.

**Что осталось по B9.** Пунктуация в текстах от модели (в тизере рядом «Рынок
растёт» без точки и «Спрос небольшой, но устойчивый.» с точкой), рамка длины
абзацев в промпте рисков (идёт вместе с E5, нужны ключи LLM), сторож на
единые кавычки и тире.

### 2026-07-28 (27) — B8: подпись к частотности указывала «справа», а на телефоне число снизу

**Что сделано.** PR #15 смержен, ветка перезапущена от main. Блок A закрыт
(в нём остался только A8 — решение владельца о пороге 8%), поэтому взял
верхнюю открытую задачу бэклога: B8.

Когда Вордстат подсказывает формулировку ходовее той, что угадала LLM, число
в строке частотности относится к подсказке, а не к фразе, которую человек
читает. Мы намеренно не подменяем фразу молча (принцип 1, см. `_best_related`)
и объясняем это подписью. Но подпись была написана через положение на экране:
«цифра справа про неё». На узком экране ряд перестраивается в столбик, число
уходит вниз — указание било мимо. Это не косметика: строка существует ровно
затем, чтобы человек не приписал число не той фразе, а на телефоне она этого
не делала.

Починено порядком, а не словами-заплаткой: ряд обёрнут в `.freq-item`,
подпись вынесена ОТДЕЛЬНОЙ строкой после числа и не называет сторон совсем —
«Число посчитано по более ходовой формулировке, которую подсказал Вордстат:
"…"». Теперь она стоит рядом со своим числом при любой раскладке, и следующая
правка вёрстки не сможет её рассинхронизировать.

Попутно тот же обман нашёлся на другом экране: в ветке слабого спроса текст
звал максимальную частотность «самой популярной из ВАШИХ формулировок», хотя
максимум мог прийти из подсказки Вордстата. Заменено на «из проверенных».

**Что проверено.** 3 текстовых теста (2 падали на старом поведении) плюс
браузерный сторож на ДВУХ ширинах — 1000 и 390 px: подпись ниже своего числа
и выше следующей строки частотности, слова «справа» в ней нет. Проверил, что
сторож работает: откатил `result.html` — оба параметра падают, вернул —
зелено. Текстовых проверок тут мало (урок A11: разметку строит скрипт, а
раскладку CSS — подстрока в шаблоне ничего не докажет). В посев мобильного
прогона добавлена вторая формулировка: с одной строкой проверка «подпись не
уехала в чужой ряд» не имела смысла. Вся сьюта: 537 зелёных (было 532).
Живьём на обеих ширинах — скриншоты сняты, разделители рядов на месте,
горизонтального скролла нет.

**Чего НЕ проверял.** Настоящий ответ Вордстата с `topRequests` — в dev нет
ни OAuth-токена, ни ключа Cloud Search API, `matched_phrase` приходит из
заглушки кастдев-стенда. Проверялось отображение поля, а не то, что Яндекс
его отдаёт.

**Что осталось.** Открытый бэклог целиком за владельцем: A8 (порог 8%),
C1 (опубликовать пример отчёта с прода), C4 (`/l/` в старой дизайн-системе —
сначала решить, наследство это или выбор), D5 (DKIM в панели reg.ru),
E1/E5 (качество отчёта — нужны ключи LLM), E3 (автопилот Директа — нужен
токен `direct:api`). Своих задач у сессии в бэклоге не осталось: следующий
цикл — снова кастдев-проход.

### 2026-07-27 (26) — A15: почтовый сканер съедал ссылку входа раньше человека

**Что сделано.** Кастдев-проход аудиторией соцконтракта: от рекламной
посадочной `/social-contract` через бесплатную проверку и ленту `/r/` до
кабинета. Сама воронка отработала чисто — оптика под комиссию на месте,
бизнес-план стоит главным действием, смета названа в составе тарифа,
JS-ошибок нет. Дефект нашёлся на возврате: **вход в кабинет по ссылке из
письма.**

Токен гасился прямо в GET-обработчике `/account/verify`. Но ссылки из писем
открывают не только люди: mail.ru, Яндекс.Почта, корпоративные шлюзы и
антивирусы проходят по ним сами, до адресата, проверяя на вредоносность.
Воспроизвёл: первый GET (сканер) — 307 и вход, второй GET (человек) — 400
«Ссылка недействительна». Человек просит новую — сканер съедает и её. Круг
замкнут: покупатель, отдавший 990–2990 ₽, не может открыть купленное, и
кабинет для него закрыт навсегда. Вторым слоем — ответ сканеру нёс
`Set-Cookie` на 180 дней, то есть сессия покупателя выдавалась машине.

Вход перенесён на `POST /account/verify`. GET показывает страницу
подтверждения и токен не трогает; сканеры ходят GET и HEAD, форму не
отправляют. Лишний клик здесь не потеря — страница называет почту, в чей
кабинет человек входит. Отказ («ссылка устарела») раньше отдавался голым
`<p>` без doctype и стилей: человек, потерявший доступ к оплаченному, видел
чёрный Times New Roman на белом. Теперь это страница в дизайн-системе с
кнопкой «Запросить новую ссылку» и напоминанием проверить «Спам».

Токен передаётся в query действия формы, а не полем: разбор тела формы
потребовал бы `python-multipart`, а лишняя зависимость на проде — это риск
того, что приложение не поднимется после ▶.

**Что проверено.** 9 новых тестов (`TestMagicLinkSurvivesMailScanners`),
7 из них падали на старом поведении — проверено до правки. Ключевой:
GET по ссылке, затем POST — вход проходит. Плюс: сканеру не достаётся
`Set-Cookie`. Обновлены 13 мест в тестах, где сессия заводилась через GET, и
два браузерных теста — теперь они жмут кнопку, как человек. Обе страницы
входа добавлены в мобильный прогон. Вся сьюта: 532 зелёных (было 523).
Живьём на 1000 и 390 px: отказ, подтверждение с названной почтой, вход
после «сканерского» GET, горизонтального скролла нет.

**Чего НЕ проверял.** Что именно делают со ссылками конкретные почтовые
провайдеры Бориса — в dev нет SMTP, писем никто не шлёт. Утверждение
«сканеры не отправляют формы» — это общее свойство таких обходчиков, а не
измеренный факт про mail.ru; но даже если какой-то из них POST'ит, хуже
прежнего не станет: сейчас ссылку гасил ЛЮБОЙ GET.

**Что осталось.** Заведена B8: на `/r/` подсказка Вордстата говорит «цифра
справа про неё», а на телефоне цифра снизу — строка показывает не туда.
Не взял в этот цикл (одно улучшение за цикл), чинится словами.
Остальной бэклог по-прежнему за владельцем: A8 (порог 8%), C1 (пример
отчёта с прода), C4 (`/l/` в старой дизайн-системе), D5 (DKIM),
E1/E5 (нужны ключи LLM), E3 (токен `direct:api`).

### 2026-07-27 (25) — E7: битая ссылка вела на главную с сырыми слотами

**Что сделано.** Блок A закрыт, поэтому цикл ушёл в кастдев-проход по
сценарию «а что человек видит, когда всё сломалось». Поднял копию приложения
с намеренно мёртвой LLM (все вызовы модели бросают `LLMAdapterError`) и
прошёл по путям.

Сама деградация LLM оказалась в порядке: `/api/demand` и `/api/sharpen`
отвечают 400 с человеческой русской фразой, кнопка на главной
разблокируется, страница платного отчёта показывает «Не получилось собрать
отчёт: … Обновите страницу через минуту» и пишет владельцу. Менять там
нечего.

А вот 404 оказался сломан. `/r/{чужое}` и `/report/{чужое}` возвращали
`_static("index.html")` напрямую, минуя `_fill_server_values`, — и главная
показывала человеку сырые слоты шаблона: «Больше `__SIGNAL_PCT__` — идея
живая, меньше `__DEAD_PCT__` — меняем предложение. Раньше `__CLICK_TARGET__`
визитов не судим». Это первая страница сервиса для того, кто пришёл по
ссылке от знакомого.

Починено одним местом — `_lost_page()`: главная со всеми подставленными
значениями, статус 404 сохранён, плюс записка сверху («Мы не нашли эту
проверку. Ссылка могла быть неполной — мессенджеры часто обрезают длинные
адреса. Опишите идею ниже: проверим спрос заново, это бесплатно»). Молчаливая
подмена страницы главной была вторым дефектом того же места: человек шёл
смотреть конкретную проверку и не понимал, куда попал. Форма проверки
остаётся под запиской — тупика из битой ссылки делать незачем.

**Что проверено.** 6 новых тестов (`TestBrokenLinkLandsOnAWorkingPage`),
из них 3 падали на старом поведении — проверено до правки. Общий сторож
`_slots()` вырезает `<script>` и ищет `__ИМЯ__` в тексте: он покрывает не
только этот случай, но и любой будущий слот, добавленный в `index.html` без
подстановки. Вся сьюта: 523 зелёных. Живая проверка в браузере на 1000 и
390 px: записка на месте, «Больше 8% … меньше 4% … Раньше 40 визитов»
подставлены, горизонтального скролла нет, на обычной `/` записки нет
(`#lost-note` не существует).

**Чего НЕ проверял.** Мёртвая LLM моделировалась подменой `llm_adapter.call`,
а не реальным отказом Яндекса: коды ошибок живого API в dev недоступны, ключей
нет. Вывод «деградация LLM в порядке» относится к обработке
`LLMAdapterError` — то, что все реальные сбои Яндекса превращаются именно
в неё, здесь не проверялось.

**Что осталось.** Бэклог целиком упирается в владельца: A8 (порог 8% — его
решение), C1 (пример отчёта с прода), C4 (`/l/` в старой дизайн-системе),
D5 (DKIM в панели reg.ru), E1/E5 (качество отчёта — нужны ключи LLM),
E3 (автопилот Директа — нужен токен `direct:api`).

### 2026-07-27 (24) — A14: проверочная страница обещала за покупателя

**Сделано.** Кастдев по последнему неохваченному месту — `/l/{idea_id}`.
Это страница, ради которой человек платит нам 1490 ₽ и Яндексу ещё 3–5
тысяч; её видят посторонние люди, пришедшие из рекламы.

В шаблоне оказались зашиты обещания, которых владелец идеи не давал:

| Что стояло | Чем плохо |
|-----------|-----------|
| «Ранним — **50% на первый месяц**» | скидку на чужой бизнес придумали мы и показывали живым людям; «первый месяц» вообще предполагает подписку |
| «Запускаемся скоро», «продукт готовится к запуску» | рамка «продукта ещё нет». Человек, искавший «пошив штор на заказ», хочет заказать сегодня — и уходит |
| «**лендинг** собирает ранний список» | запрещённое слово в тексте для пользователя |

Второй пункт хуже, чем кажется на первый взгляд: **мы меряем не идею, а
свою рамку.** Если посетитель ушёл, потому что ему предложили «ранний
доступ» вместо услуги, конверсия просядет — и мы вынесем вердикт «спроса
нет» по ошибке собственного шаблона. Для аудитории соцконтракта (пекарня,
груминг, ателье) это ровно тот случай.

Стало нейтрально и без обещаний за владельца: кнопка «Оставить заявку»,
подпись «Оставьте контакт — напишем вам с условиями и сроками. Без спама и
звонков», подтверждение «Заявка принята». Согласие на обработку данных и
ссылка на политику остались — без них страница не имеет права собирать
контакты, и модерация Директа её не пропустит.

**Сторожа не было вовсе.** Запрет на «оффер»/«лендинг» проверялся по
`static/*.html`, а шаблон лежит в `app/` — единственная страница, которую
видят посторонние, была вне проверки. Теперь покрыта.

**Проверено.** 517 тестов зелёные (было 511). Новый класс
`TestLandingPromisesNothingOnTheOwnersBehalf` (6); на старом тексте падают
4 из 6. Живьём: запустил проект через `/api/launch`, открыл его страницу с
UTM-меткой на 1000px и 390px, оставил заявку — и проверил сквозь, что
события дошли: `/api/verdict/` показал 2 визита, 1 заявку и этап «Тест на
реальных людях». То есть механика, за которую платят, работает целиком.

**Честно про охват правки.** `landing_html` рендерится и сохраняется в
момент запуска. Уже запущенные проекты остаются со старым текстом; новый
подхватится только при повторном запуске того же `idea_id`. Массового
перерендера я не делал — это тронуло бы боевые записи.

**Осталось.** Завёл C4: страница `/l/` живёт в старой дизайн-системе
(оранжевый акцент, пилюля-бейдж — прямо запрещена в `CLAUDE.md`). Отчасти
защитимо: страница представляет чужой бизнес, а не Создателя. Сначала
решить, сознательный это выбор или наследство.

Бэклог для сессии по-прежнему пуст: A8 (решение владельца), D5 (DKIM),
C1 (прогон на проде), E5/E1 (LLM-ключи), E3 (OAuth Директа).

### 2026-07-27 (23) — A13: оплаченный проект назывался «Идеей»

**Сделано.** Кастдев по неохваченному куску — тому, что покупатель получает
**за 1490 ₽**. Прошёл весь платный путь живьём: проверка спроса → заострение
→ выбор варианта → оплата → вебхук → автозапуск → кабинет → `/p/`.

Автозапуск отработал: проект собрался, страница отрисовалась, контакт
привязан, в кабинете всё видно. А вот метка этапа врала.

Человек только что заплатил, мы собрали ему проверочную страницу — и
кабинет пишет **«Этап 1 из 7 — Идея»**, а `/p/` добавляет подпись
«Идея — набираем первые визиты», противоречащую сама себе. Причина в
`_smoke_card`: `stage = 1 if views > 0 else 0`. Правило осталось от старой
шкалы, где нулевой этап звался «Оффер»; после объединения шкалы (0 «Идея»,
1 «Спрос», 2 «Тест на реальных людях») оно стало показывать человеку, что он
в самом начале — ровно в тот момент, когда он заплатил за третий этап.

Стало: 2 «Тест на реальных людях», пока визитов меньше цели, и 3 «Заявки»,
когда их хватило на вердикт. Правило теперь одно — `_smoke_stage`;
**вторая копия жила прямо в `static/project.html`** (`d.views > 0 ? 1 : 0`)
и уже разъехалась с сервером. Страница берёт с сервера и этап, и длину
шкалы, так что следующее изменение шкалы её не сломает.

**Ложная тревога, которую поймал на себе.** Сначала показалось, что
автозапуск вообще не срабатывает: заказ оплачен, проекта нет. Уже собирался
записывать это как дыру — но проверил и оказалось, что виновата **моя
собственная заглушка** заострения: она отдавала 5 полей вместо 11, а
`LAUNCH_REQUIRED_FIELDS` требует все. Настоящий `sharpen_idea` их валидирует
(см. `offer_engine._validate`), так что на проде такого нет. Заглушку
привёл в соответствие с валидатором — иначе следующий проход снова наступит
на те же грабли. **Урок: заглушку надо сверять с валидатором, а не с
собственным представлением о данных.**

**Проверено.** 510 тестов зелёные (было 509). Переписаны два теста,
фиксировавших старое правило (`stage == 0` / `stage == 1`), добавлены два
новых на корректные переходы и браузерный
`test_paid_project_is_not_labelled_as_just_an_idea`. Проверил, что сторож
рабочий: с возвращённым `1 if views else 0` падают оба теста этапов.

Живьём: оплаченный проект в кабинете и на `/p/` — «Этап 3 из 7 — Тест на
реальных людях», семь делений шкалы, три закрашены; 1000px и 390px.

По ходу прочитал плейбук `/guide/direct` в составе `/p/` — шаги читаются,
ключевые фразы подставляются из выбранного варианта, ссылка с меткой на
месте. Замечаний нет.

**Осталось.** Бэклог для сессии по-прежнему пуст: A8 (решение владельца),
D5 (DKIM), C1 (прогон на проде), E5/E1 (LLM-ключи), E3 (OAuth Директа).
Не охваченной кастдевом осталась сама страница `/l/{idea_id}` — то, что
видит посетитель рекламы.

### 2026-07-27 (22) — A12: ненайденную частотность выдавали за приговор идее

**Сделано.** Бэклог кончился — всё оставшееся упирается во владельца (ключи,
токены, панель reg.ru, решения), — поэтому кастдев-проход. Начал не с
идеального сценария, а с того, который **вероятнее всего и есть на проде
прямо сейчас**: Вордстат не отдаёт частотность. OAuth-токена у владельца нет,
облачный путь может не отвечать — и тогда `check_demand` возвращает
`count = None` по всем фразам.

Прошёл этот путь живьём и увидел вот что: напротив каждой из трёх формулировок
страница писала **«почти не ищут»**.

Это ложь. В докстринге `wordstat_best` прямым текстом: `count=None` = «оба
пути недоступны/без данных — штатная деградация». Ноль и «не смогли получить»
это разные вещи, и второе не даёт права на вывод о рынке. Вердикт под
цифрами при этом говорил правду («данные недоступны»), то есть страница
противоречила сама себе — а человек читает сначала три крупных строки, а
потом уже мелкий текст.

Масштаб: пока Вордстат молчит, это видит **каждый** посетитель. Идея может
быть отличной, а мы её хороним на пустом месте.

Второй симптом того же: финал продавал как ни в чём не бывало. Живой тест за
1490 ₽ плюс 3–5 тысяч Яндексу, разбор за 990–2990 ₽ — на основании проверки,
которой не было. A11 закрывала это для `weak`, но `unknown` — другой случай:
не «идея слабая», а «мы не измерили».

Стало:

| Было | Стало |
|------|-------|
| «почти не ищут» | «нет данных у Яндекса» |
| «Дальше — соберём страницу…» | «Проверка не состоялась — цифр Яндекса сейчас нет» |
| главное действие — платное | главное действие — «Проверить ещё раз», бесплатно |
| «разбор на данных, которые мы честно посчитали» | «…цифр спроса сейчас нет, раздел будет построен без них» |

**Отменил прежнее решение — и вот почему.** Тест
`test_no_data_wording_reads_as_finding_not_error` фиксировал обратное:
когда-то «нет данных» заменили на «почти не ищут», потому что первое
читалось как «сайт сломан». Жалоба верная, но лечится она словами, а не
враньём в другую сторону: «нет данных **у Яндекса**» называет источник и
потому не читается как наша поломка. Плюс рядом теперь есть вердикт и блок
«Проверка не состоялась», которых тогда не было. Причина отмены записана
прямо в тесте, чтобы следующая сессия не вернула всё обратно вслепую.

**Проверено.** 509 тестов зелёные (было 501). Новый класс
`TestUnmeasuredDemandIsNotSoldAsMeasured` (7) и браузерный
`test_unmeasured_demand_stops_selling_in_a_real_browser`, который заодно
проверяет обратное: с настоящими цифрами блок не появляется и живой тест
остаётся главным. Со снятой правкой падают 2 из 8, включая браузерный.

Живьём: отдельный сервер, где Вордстат молчит, — 1000px и 390px, обе
аудитории. Заодно убедился, что деградация вокруг работает как задумано:
шаги «Конкуренты» и «Оценка идеи» без данных не рисуются вовсе.

**Осталось.** Бэклог по-прежнему пуст для сессии: A8 (решение владельца),
D5 (DKIM в панели reg.ru), C1 (нужен прогон на проде), E5/E1 (нужны
LLM-ключи), E3 (нужен OAuth-токен Директа). Следующий цикл — снова кастдев;
неохваченными остались `/p/`, `/guide/direct` и сама страница `/l/`.

### 2026-07-27 (21) — E4: идеи стало видно сравнимыми. И регрессия от E6

**Сначала о плохом — сам себе сломал.** Проверяя, что трогать в кабинете, я
посмотрел, какие ссылки он отдаёт, и нашёл собственную регрессию из прошлого
цикла. E6 перевела адреса на неугадываемый `public_id`, но в
`/api/account/me` остались ссылки с порядковым номером. Для проверки,
привязанной к кабинету, это ещё работало через редирект, а вот для **заявки
на живой тест** — нет: проверку под заявкой человек мог и не сохранять, тогда
`DemandCheck.contact` пуст, совпадения нет, и кабинет отдавал **404 на его же
заявке**. Воспроизвёл скриптом до правки: `/r/1` → 404. Теперь адрес берётся
из `public_id` самой проверки, а если проверки уже нет — ссылки нет вовсе:
ссылка в никуда хуже её отсутствия.

Урок на будущее: правка, меняющая форму адреса, обязана пройтись по **всем**
местам, которые этот адрес собирают, включая ответы API. Тесты E6 проверяли
страницы и не смотрели на кабинет.

**Сделано по задаче.** Человек с пятью проверенными идеями видел пять
одинаковых строк «Бесплатная проверка спроса» и не мог сказать, какая
сильнее, не открыв каждую. Теперь строка несёт цифры, по которым идею уже
судили:

> **7**/10 · **2 400** запросов/мес · слабое место — конкуренция

и строки отсортированы от сильной к слабой. **Порядок и есть ответ** на
вопрос «во что вкладываться» — отдельного экрана сравнения не понадобилось.
Подпись «От самой сильной идеи к самой слабой» появляется только когда строк
больше одной: над единственной она была бы шумом.

Ничего нового не считаем и не обещаем — всё это уже посчитано на бесплатной
проверке. Проверки без балла (Вордстат промолчал) уходят вниз, но остаются
видимыми: сравнивать их не с чем, а прятать чужую работу нечестно. Битый
`result_json` не прячет карточку — без цифр, но строка на месте.

**Проверено.** 501 тест зелёный (было 491). Новые классы
`TestIdeasCanBeCompared` (7) и `TestCabinetLinksSurvivedTheAddressChange` (2),
плюс браузерный `test_cabinet_ranks_ideas_so_they_can_be_compared`. Проверил,
что сторожа рабочие: со снятой сортировкой и возвращённой числовой ссылкой
падают 3 из 9.

По ходу поймал ловушку прогона: токен входа одноразовый, и второй тест,
который логинится тем же токеном, молча видел бы страницу гостя. Каждому
теперь свой токен, это записано в посеве комментарием.

Живьём: кабинет с пятью идеями (баллы 7/5/3/1 и одна без балла) на 1000px и
390px. Порядок верный, цифры читаются, вбок не едет.

**Осталось.** В блоке E — E5 и E1 (нужен живой прогон модели, ключей в dev
нет) и E3 (ждёт OAuth-токен Директа). Сравнение охватывает только «просто
проверенные» идеи: те, из которых уже вырос отчёт или заявка, живут в своих
разделах выше и в сортировку не попадают — для человека, который уже
заплатил, вопрос «какая сильнее» обычно уже решён.

### 2026-07-27 (20) — E6: чужую идею больше не прочитать перебором

**Сделано.** Задача лежала в бэклоге с пометкой «решение не очевидно —
сначала решить, чем жертвуем». Решилось: **ничем.** Требовать вход в кабинет
действительно сломало бы обмен ссылкой (на `/r/` есть кнопка «Поделиться
результатом», это задуманная функция), но обмену не мешает **неугадываемый
адрес**. Кто получил ссылку — открывает без всякого входа; кто ссылки не
получал — не подберёт её перебором. Тот же приём, что уже закрыл платный
отчёт в A9.

`DemandCheck.public_id` — случайный, 12 символов, с индексом и бэкфиллом при
старте (проверки, сделанные раньше, иначе остались бы доступны по номеру).
Страницы переехали на `/r/{public_id}` и `/report/{public_id}`.

**Дыра была на ДВУХ страницах, а не на одной.** Про `/r/` знал бэклог, но
тизер `/report/{id}` показывает текст идеи ровно так же — и по номеру
читался так же. Чинить пришлось обе, иначе правка была бы косметической.

Старые ссылки не сломаны у тех, кто имел право: порядковый номер в адресе
по-прежнему принимается у владельца по ключу и у хозяина проверки по сессии
кабинета, и им отдаётся редирект на канонический адрес — закладка чинится
сама. Постороннему с номером — 404. Редирект сохраняет query, иначе на нём
терялись бы владельческий прогон (`?preview=full`) и токен покупателя
(`?t=...`).

Номер записи остался внутри API — заказ отчёта, сохранение в кабинет, выбор
формулировки, дозаказ разделов. Там свои проверки доступа, и в адресную
строку он больше не попадает.

**Проверено.** 491 тест зелёный (было 478). Новый класс
`TestIdeaIsNotReadableByGuessing` — 13 тестов; проверил, что ловят дыру: со
снятой проверкой падают 7 из 13. Отдельно покрыто, что публичной ссылкой
по-прежнему может воспользоваться кто угодно (иначе я бы «починил» задачу,
сломав её смысл), что два адреса не совпадают, что чужая сессия номер не
открывает и что `_report_link` (письмо покупателю и кабинет) ведёт на новый
адрес, а не на закрытый номер.

Живьём: прошёл путь целиком на 1000px и 390px — после проверки адрес вида
`/r/xPyKVRudjz6F`, кнопка «Посмотреть отчёт» ведёт на `/report/` с тем же
адресом. Затем отдельной сессией **перебрал номера 1–3 на обеих страницах**:
текста идеи нет нигде.

**Правка бухгалтерии.** Запись B7 из прошлого цикла лежала в блоке E, хотя
задача из блока B. Перенёс: бэклог берут сверху вниз, и запись не в своём
блоке ломает порядок.

**Осталось.** В блоке E — E4 (идеи нельзя сравнить между собой), E5 и E1
(нужен живой прогон модели, ключей в dev нет), E3 (ждёт OAuth-токен Директа).

### 2026-07-27 (19) — D4: почта перестала быть чёрным ящиком

**Сделано.** Задача пришла не из бэклога, а от владельца: он настраивал почту
и написал «столько всего, что разбегаются глаза». Это и есть диагноз. Почта —
единственная подсистема, которая **ломается молча**: не ушло письмо со ссылкой
входа или об оплате, и об этом не узнаёт никто, включая нас. Настройка при
этом живёт в чужой панели и состоит из четырёх переменных окружения, любую из
которых можно ввести неверно.

Появился прибор — тот же приём, что уже выручил с Вордстатом (`/api/diag/yandex`):

- **`GET /api/diag/mail?key=...&to=...`** (owner-only) — что задано, чего не
  хватает и уходит ли письмо на самом деле.
- **Блок «Почта» в `/desk`** — то же самое человеческим языком, с полем и
  кнопкой «Отправить». Читать сырой JSON владельцу негде.

Главное в нём — не сам факт проверки, а **перевод ошибки**. Получить в ответ
`SMTPAuthenticationError (535, b'5.7.8 ...')` значит остаться ровно там же, где
был. Теперь ответ выглядит так: «Сервер не принял логин или пароль. Логин — это
адрес ящика целиком (вида info@вашдомен.ru), пароль — от самого ящика, не от
панели reg.ru», а техническая строка идёт следом, в скобках. Разобраны шесть
частых причин: неверный логин/пароль, несуществующий сервер, закрытый порт,
таймаут, несовпадение шифрования, отказ по адресу получателя.

**Ловушка, найденная по дороге.** Код умел ровно один способ подключения —
SSL с первого байта, порт 465. Провайдеры дают на выбор два, и reg.ru
показывает оба; владелец, вписавший 587, получил бы невнятную ошибку SSL при
полностью верных логине и пароле, и искал бы её в пароле. Теперь способ
выбирается по порту (`mailer._transmit`), и STARTTLS поднимается **до**
логина — иначе пароль ушёл бы в сеть открытым.

**Честность в мелочи.** Сначала блок писал зелёным «настроена», как только
переменные заданы. Это неправда: заданные переменные и уходящие письма —
разные вещи, и зелёная надпись рядом с красной ошибкой противоречила бы сама
себе. Стало: «переменные заданы» нейтральным, и только после реально ушедшего
письма — зелёное «письмо уходит».

**Проверено.** 478 тестов зелёные (было 455). Новые классы:
`TestMailerSpeaksBothPorts` (3), `TestOwnerCanDiagnoseMail` (14),
`TestDeskShowsMailState` (5) и браузерный `test_owner_sees_mail_state_in_the_desk`.
Проверил, что сторож рабочий: с возвращённым «только SSL» падают 2 из 3
портовых тестов. Отдельным тестом закрыто, что пароль не возвращается ручкой
ни при каких условиях и что ручка не отвечает пятисотой при любой ошибке.

Живьём: три состояния на отдельных серверах — переменные не заданы, письмо
уходит, неверный пароль — на 1000px и 390px. Тексты читаются, вбок не едет,
ошибок в консоли нет.

**Не проверено (и здесь нельзя).** Настоящего SMTP в dev нет: отправка везде
подменена. Что письмо реально доходит до ящика и не попадает в спам, покажет
только прод. Ровно для этого блок и сделан — чтобы владелец увидел это сам,
одной кнопкой, а не по жалобе покупателя.

**Осталось.** У домена есть SPF (`include:_spf.hosting.reg.ru`), но **нет
DKIM** — владелец проверил. Без подписи письма с нового домена системно
рискуют уйти в спам у mail.ru и Яндекса. Это настройка в панели reg.ru, не
код; заведено в блок D как D5.

### 2026-07-27 (18) — B7: состав тарифов снова можно прочитать

**Сделано.** В блоке A остался только A8 (решение владельца), поэтому взял
верх блока B — находку прошлого цикла.

C2 в своё время поставила состав тарифов прямо к кнопке оплаты, и это было
правильно: человек с `/social-contract` идёт за сметой, а «Финансовой модели»
в тарифе за 990 ₽ нет вовсе. Но тогда разделов было восемь. После
посекционной переработки (E5) их стало 21, и полный тариф на витрине
превратился в это:

> всё это и ещё кто платит, почему сейчас, чем отличаться, что мешает
> скопировать, как берём деньги, цена, экономика одной продажи, финансовая
> модель, с чего начать без вложений, где брать клиентов, первые десять
> клиентов, план запуска, что оформить по закону, риски и как их снижать,
> когда остановиться, как проверить дёшево.

Шестнадцать строчных фрагментов через запятую в момент, когда человек
достаёт деньги. Блок, созданный помогать решению, решению мешал.

Стало — пять строк по группам `SECTION_GROUPS`, имя группы жирным:

| Группа | Что в ней |
|--------|-----------|
| Идея и рынок | кто платит, почему сейчас |
| Конкуренты | чем отличаться, что мешает скопировать |
| **Деньги** | как берём деньги, цена, экономика одной продажи, **финансовая модель** |
| Запуск | с чего начать без вложений, где брать клиентов, первые десять клиентов, план запуска, что оформить по закону |
| Риски и проверка | риски и как их снижать, когда остановиться, как проверить дёшево |

Смысл группировки не в красоте: «Финансовая модель» перестала тонуть в
середине списка и находится по слову «Деньги». Для получателя соцконтракта
это ровно та строка, ради которой он платит, — и вся суть C2.

Дешёвый тариф остался простым перечислением: пять пунктов читаются одной
строкой, дробить их значило бы сделать хуже там, где было хорошо. Число
разделов («и ещё 16») считается, а не пишется руками — набор секций уже
менялся однажды.

**Поймал на себе по ходу.** Первая версия раскладки обходила
`SECTION_GROUPS` — и секция, не попавшая ни в одну группу, молча исчезала бы
с витрины. Это ровно тот разъезд движка и витрины, против которого весь блок
и написан. Поймал существующий тест C2 (он подменяет набор секций и требует,
чтобы витрина назвала новую); добавил явный сбор «бесхозных» ключей и
отдельный тест на них.

**Проверено.** 455 тестов зелёные (было 447). Новый класс
`TestTierListIsReadableAtTheDecisionPoint` — 8 тестов; проверил, что они ловят
старое поведение: с возвращённой простынёй падают 5 из 8. Здесь, в отличие от
A11, JS-развилки нет — блок собирает сервер, поэтому питоновские проверки
сторожат настоящее поведение.

Живьём: обе аудитории × 1000px и 390px. На узком экране группы читаются,
вбок не едет, ошибок в консоли нет.

**Осталось.** Вторая половина задачи — описывать тарифы **выдачей, а не
объёмом** («3 названные компании-аналога», «риск-реестр», как у dimeadozen).
Она пересекается с остатком E5 (усилить голос разделов по живому прогону) и
делается вместе с ним, когда у владельца дойдут руки до прогона на проде: в
dev нет LLM-ключей.

### 2026-07-27 (17) — A11: слабый спрос перестал продавать себе в убыток

**Сделано.** Блок A был закрыт (кроме A8, где решение за владельцем), поэтому
цикл начался с кастдев-прохода. Чтобы пройти путь по-настоящему, а не по
тестам, поднял обёртку над приложением, где подменены ровно три внешних
вызова — Вордстат, заострение и генерация разделов, — а всё остальное
настоящее: маршруты, вёрстка, БД. Прошёл ленту `/r/` шаг за шагом обеими
аудиториями, отдельно — на идее, которую почти не ищут.

Там и нашлось. Идея с вердиктом «в поиске эту идею почти не ищут» и оценкой
**1/10** приводила ровно к той же кнопке, что идея на 6/10: «Оставить заявку
на живой тест» за 1490 ₽ плюс 3–5 тысяч Яндексу. Ни слова о том, что мы
только что показали цифры, означающие обратное.

Это не вкусовщина, а проверяемая арифметика. Живой тест гоняет рекламу по
**тем же** запросам, а вывод мы делаем по `CLICK_TARGET` = 40 визитам. Порог
`weak` — меньше `THRESHOLD_NICHE` = 300 запросов в месяц. Набрать 40 визитов
оттуда означало бы, что по объявлению кликает больше 13% всех, кто вообще
ищет эту фразу. Мы продавали тест, который по нашим же числам не наберёт
статистики, — прямо против принципа 2 («смысл сервиса — чтобы человек НЕ
потратил деньги зря»).

Стало: при `verdict.level == "weak"` финальный шаг ведёт бесплатное действие —
«Прежде чем платить, попробуйте другую формулировку», с названной причиной
(«ищут 30 раз в месяц, а судить нужно по 40 визитам — набрать их из поиска
будет нечем»). Оба платных блока опускаются в альтернативы. У самой кнопки
живого теста — прямая оговорка, что тест рискует не набрать визитов, а
рекламный бюджет всё равно потратится. Шапка перестаёт обещать «Этап 3 —
соберём проверочную страницу»: это было то же противоречие, только выше по
экрану. Кнопки при этом **не спрятаны**: наше дело сказать правду, а не
решить за человека.

Число берётся из `CLICK_TARGET` через серверную подстановку, а не пишется
руками, — порог уже разъезжался с витриной (B5).

**Проверено.** 447 тестов зелёные (было 437). Восемь текстовых в
`TestWeakDemandStopsSelling` и **два настоящих браузерных** в
`tests/test_mobile.py`.

Про тесты отдельно, это важнее самой правки. Сначала я написал только
текстовые проверки — и они остались зелёными, когда я временно отключил
логику. Разметка блока лежит на странице всегда, показывает его скрипт, и
подстроки в HTML такого не видят. Пришлось довести до браузерного теста,
который ходит по ленте и смотрит, что видно и какими классами разведены
блоки: он падает, как только логику выключаешь. В докстринге текстового
класса это записано, чтобы следующая сессия им не обманулась.

Живьём: два сервера (слабый спрос и нормальный) × две аудитории × 1000px и
390px — восемь прогонов. Предупреждение появляется только при слабом спросе,
при нормальном порядок блоков прежний (у фаундера главный — живой тест, у
соцконтракта — бизнес-план). Вбок не едет, ошибок в консоли нет.

**Осталось.** Тем же проходом найдено и **не сделано**: B7 — состав тарифа
«Бизнес-план» на витрине превратился в простыню из 16 строчных фрагментов
через запятую (C2 ставила его, когда разделов было 8, после E5 их 21). Блок,
созданный помогать решению, теперь решению мешает. Заведено в блок B.

В блоке A остаётся только A8 — верен ли порог 8%; это решение владельца.

### 2026-07-27 (16) — A10: покупатель наконец получает письмо

**Сделано.** После оплаты письмо уходило владельцу, а человеку, который
только что отдал 990–2990 ₽, — ничего. Формально он получал фискальный чек от
ЮКассы, но чек — это чек, а не ссылка на продукт. Единственным следом покупки
была вкладка в браузере: закрыл — и ищи. Отдельно скверно то, что разбор из
21 раздела собирается минутами, и нигде не было сказано, что вкладку **можно**
закрыть — по коду можно, каждый раздел дописывается в базу сразу.

Появился `mailer.notify_buyer`: тот же контракт, что у владельческого —
никогда не бросает, потому что человек уже заплатил и сбой SMTP не имеет
права стать ошибкой на его экране. Отличий два: адрес приходит параметром, а
не из окружения, и контакт может оказаться телефоном — для этого есть
`looks_like_email`, который спрашивает заранее вместо того, чтобы пытаться и
падать (контакт для чека 54-ФЗ телефон разрешает).

Флаг `buyer_notified` заведён **отдельно** от `paid_notified` на обеих
моделях заказа. Один флаг на двоих означал бы, что удачное письмо владельцу
гасит письмо покупателю, — ровно та ошибка, которую разводили в A2.

Письмо отвечает на три вопроса, которые возникают сразу после оплаты:

| Вопрос | Что в письме |
|--------|--------------|
| что куплено | тариф, сумма, номер заказа, идея |
| где это лежит | прямая ссылка с токеном из A9 |
| что если ссылка потеряется | вход в кабинет по той же почте, без пароля |

Плюс две вещи по месту: в письме про разбор — что страницу можно закрыть и
сборка продолжится; в письме про живой тест — повтор оговорки про отдельный
рекламный бюджет (A7 закрывала её на витрине, но письмо — последнее место,
где об этом можно промолчать). Письмо уходит и на заявку без оплаты: её
доводит владелец руками, и подтверждение человеку нужно тем более.

На странице отчёта строка сборки теперь говорит, что вкладку можно закрыть, а
плашка возврата с оплаты — где разбор будет лежать потом. **Про письмо
страница говорит только если оно действительно ушло** (`buyer_notified`):
обещать письмо, которого не было, хуже, чем не обещать ничего.

**Проверено.** 437 тестов зелёные (было 418). Новые классы
`TestBuyerHearsFromUs` (16) и `TestMailerKnowsWhatItCanSend` (3). Проверил,
что они ловят дыру: с временно отключённой отправкой 7 из них падают.
Покрыто в том числе то, что письмо уходит один раз на заказ (вебхук ЮКассы
приходит повторно), что телефон-контакт не ломает оплату, что упавший SMTP не
ломает оплату, и что при недоступном ящике покупателя владельческий флаг
всё равно ставится, а покупательский — нет.

Оба письма **прочитал глазами**, а не только по подстрокам: на этом нашлась
фраза «даже если ссылка выше потеряется» в письме про живой тест, где
никакой ссылки выше нет, — исправлено и покрыто тестом.

Живьём: uvicorn на 8601, Playwright на 1000px и 390px. Плашка возврата с
оплаты и строка сборки читаются, вбок не едет, ошибок в консоли нет. По ходу
убрал повтор: две соседние плашки дважды говорили «страницу можно закрыть» —
эта мысль осталась в одном месте.

**Не проверено (и проверить тут нельзя).** Настоящей отправки письма не было:
в dev нет SMTP-ключей, весь путь проверен инъекцией `mailer.send`. Что письмо
реально доходит до ящика и не улетает в спам — увидит только владелец на
проде, после того как задаст `SOZDATEL_SMTP_*`.

**Осталось.** В блоке A остаётся только A8 (порог 8% — решение владельца, не
сессии). Следующая по приоритету — C1: механика публичного примера готова,
но сам пример пуст, пока владелец не соберёт хороший отчёт на проде и не
нажмёт «Опубликовать как пример».

### 2026-07-27 (15) — A9: оплаченный отчёт больше не открывается перебором

**Сделано.** Блок A был закрыт (кроме A8, где решение за владельцем), поэтому
цикл начался с кастдев-прохода по платному пути. Нашлась дыра, которую никто
не искал: страница `/report/{check_id}` адресуется **порядковым номером
бесплатной проверки**. Набрав 41 вместо 42, посторонний получал чужой
бизнес-план за 2990 ₽ целиком — вместе с чужой идеей и чужой сметой.

Проверено это было не рассуждением: скрипт создал оплаченную покупку с
меткой «СЕКРЕТНЫЙ ВЫВОД» и запросил страницу без ключа, без сессии и без
чего-либо ещё. Метка оказалась в HTML, `UNLOCKED_TIER` — `"full"`.

Чтение было ещё не худшим. `POST /api/report/{id}/section` пускал
постороннего **запускать генерацию по чужой покупке**: в dev вызов дошёл до
`llm_adapter` и упал только на отсутствии ключа. На проде это наши деньги за
вызовы модели, которые может тратить кто угодно.

Теперь у каждой покупки есть `access_token`, а отчёт открывают три двери, и
все три — у настоящего покупателя:

| Дверь | Когда работает |
|-------|----------------|
| ключ владельца | как везде на `/desk` |
| ссылка с токеном | оплата возвращает человека уже с ней; она же в кабинете |
| сессия кабинета | вошёл своей почтой — открывает отчёт вообще без токена |

Посторонний видит тизер и строку «Этот отчёт уже оплачен. Откройте его по
ссылке, на которую вас вернула оплата, — или войдите в личный кабинет с той
же почтой». Не 403: свою же ссылку легко открыть в другом браузере, и глухая
ошибка человеку ничего не объяснит (принцип 7).

Три вещи всплыли по ходу и починены здесь же:
- **Постороннему на чужой оплаченной проверке генерировался бесплатный
  образец** — вызов модели ради того, кому и продавать нечего: отчёт по этой
  идее уже продан.
- **Рядом с «уже оплачен» стояла кнопка оплаты.** Для покупателя, потерявшего
  ссылку, это прямой путь заплатить за одно и то же дважды. Витрина в этом
  состоянии гасится.
- **`/example` дозаказывал недостающие разделы из браузера посетителя.** Если
  пример опубликован неполным, каждый зритель оплачивал бы нам вызовы модели
  по чужой покупке. Страница примера теперь показывает ровно то, что
  опубликовано.

Плюс `<meta name="referrer" content="same-origin">`: раз в адресе лежит
токен, любая будущая ссылка наружу утащила бы его в `Referer`.

Покупки, оформленные до этой правки, получают токен при старте приложения —
иначе прямая ссылка перестала бы работать у тех, кто уже заплатил.

**Проверено.** 418 тестов зелёные (было 401). Новый класс
`TestPaidReportIsNotPublic` — 17 тестов. Главное: **проверил, что они ловят
дыру**, а не проходят вхолостую — с временно отключённой проверкой доступа
7 из них падают, включая «посторонний читает чужой отчёт» и «посторонний
тратит наши деньги на модель».

Живьём: uvicorn на 8591 с засеянной оплаченной покупкой, Playwright на 1000px
и 390px. Посторонний — «400 000 ₽» из сметы нет на странице, есть плашка,
витрина оплаты убрана; покупатель по ссылке с токеном — весь отчёт на месте.
Ошибок в консоли нет, вбок не едет.

**Осталось.** Тем же проходом найдено и **не сделано**: A10 — покупателю
после оплаты не приходит ни одного письма от нас (владельцу приходит,
покупателю — только фискальный чек ЮКассы), при том что отчёт собирается
минутами и нигде не сказано, что вкладку можно закрыть. Заведено в блок A
как следующая задача. Ещё завёл E6: результат бесплатной проверки `/r/{id}`
тоже читается перебором — но там ссылкой делятся намеренно, и решение
не очевидно, сначала надо решить, чем жертвуем.

### 2026-07-27 (14) — D2 + D3: воронка владельца из своей же БД

**Сделано.** У Метрики (D1) есть два изъяна, из-за которых её одной мало:
её настраивают руками в чужом интерфейсе, она теряет людей на
блокировщиках — и она ничего не знает про деньги. Поэтому появился второй,
независимый прибор: `GET /api/funnel` (owner-only) считает воронку прямо
по нашим таблицам `DemandCheck` / `ReportPurchase` / `LiveTestOrder`.

Восемь шагов, от первого касания до денег:

| Шаг | Что считает |
|-----|-------------|
| Проверок спроса | человек описал идею и получил результат |
| Заострили идею | выбрал одну из трёх формулировок |
| Сохранили в кабинет | оставил почту, чтобы вернуться |
| Дошли до витрины отчёта | открыли страницу отчёта и увидели образец |
| Заказали отчёт | нажали «Получить отчёт», включая неоплаченные |
| Оплатили отчёт | деньги получены |
| Заказали тест на людях | заявка на живой тест, включая неоплаченные |
| Оплатили тест на людях | деньги получены |

Каждый шаг **называет, что именно он считает** — это тот же принцип, что в
B3: число без определения приглашает сделать неверный вывод. Рядом с числом
— разбивка «фаундеры / соцконтракт» (это и есть серверная половина D3, без
неё не понять, какая рекламная кампания окупается), «дошли N% с прошлого
шага» и полоса доли от первого шага. Период переключается: за всё время /
7 дней / 30 дней. Внизу — «Получено денег: N ₽ — только подтверждённые
оплаты».

Две ловушки, из-за которых цифры могли бы врать, закрыты явно:
владельческие прогоны отчёта (`status == "preview"`, инструмент E1) в
воронку не попадают вовсе — иначе собственная проверка качества выглядела
бы как заказ; а деньги суммируются только по `status == "paid"`, не по
выставленным счетам. Заказы отчёта и живого теста наследуют аудиторию своей
проверки спроса — сами по себе они `purpose` не хранят.

**Проверено.** 401 тест зелёный. Новый класс `TestOwnerFunnel` (11 тестов):
без ключа владельца — 401; все восемь шагов на месте и каждый объясняет
себя; числа отражают реальные строки БД; неоплаченный заказ считается
заказанным, но не оплаченным; разбивка по аудиториям; заказ отчёта берёт
аудиторию проверки; владельческий прогон — не продажа; выручка только по
подтверждённым оплатам; фильтр периода сужает окно; `/desk` рисует воронку.

Вживую: uvicorn на 8571 с засеянной БД (12 проверок 7/5 по аудиториям,
4 заказа отчёта + 1 владельческий прогон, 2 заявки на живой тест),
Playwright/Chromium на 1000px и 390px. `/api/funnel` вернул ожидаемые
12 → 8 → 6 → 5 → 4 → 3 → 2 → 1 и выручку 8 460 ₽ (2990 + 990 + 2990 +
1490) — владельческий прогон в заказы не попал. Вбок не едет ни на одной
ширине, ошибок в консоли нет, переключение периода работает.

**Починено по ходу.** На 390px колонка чисел стояла `white-space:nowrap`
и отжимала название этапа в две строки — «Дошли до витрины отчёта»
ломалось посередине. Медиазапрос ≤560px разворачивает строку в один
столбец: сначала название и что считаем, потом число и разбивка под ним.

**Осталось.** В блоке D закрыто всё. Следующая по приоритету — E5, шаг 3:
усилить голос разделов по результатам живого прогона; в dev это сделать
нельзя, нужны ключи LLM (только у владельца). Владельцу по-прежнему нужно
завести десять целей в интерфейсе Метрики (D1) и задать
`SOZDATEL_OWNER_EMAIL` — без него письма об оплатах молча не уходят.

### 2026-07-27 (13) — D1: середина воронки стала видимой

**Сделано.** Целей Метрики было три: `demand_started` на двух витринах и две
оплаты. Между «начал проверку» и «заплатил» приборов не было вовсе. Для
рекламы это значит две плохие вещи: непонятно, на каком шаге отваливается
оплаченный клик, и **Директ оптимизируется на клики, а не на покупателей** —
автостратегии учатся по целям, а целей середины у нас не существовало.

Стало десять целей, реестр `METRIKA_GOALS` в `app/main.py`:

`demand_started` → `demand_done` → `sharpen_used` → `check_saved` →
`report_viewed` → `example_viewed` → `report_order_started` →
`live_test_ordered` → `report_paid_quick` / `report_paid_full`

**Каждая цель несёт `purpose`** — business или social_contract. Без этого
нельзя понять, какая кампания окупается: обе аудитории идут по одним и тем же
шагам. Это закрывает клиентскую половину D3.

**Заодно убрана копипаста, которая гарантировала пропуски.** Каждая страница
носила свою копию проверки `if (window.SOZDATEL_YM_ID && typeof ym ===
'function')`, и новая страница просто забывала её написать. Теперь вместе со
счётчиком отдаётся `sozGoal()` — единственный вход, глушит ошибку внутри
(счётчик не имеет права ломать страницу). Тест запрещает звать `reachGoal` в
обход.

**Сторож против расхождения с Метрикой.** Имена целей владелец заводит руками
в интерфейсе, и разъехавшееся имя — это молча пустой отчёт, по которому будет
сделан неверный вывод. Два теста в обе стороны: каждая объявленная цель
реально отправляется из кода, и ни одна отправляемая цель не лежит вне
реестра. Плюс проверка, что имена — валидные идентификаторы (пробел или
кириллица там обернутся молчащей целью).

**Проверено.** 390 тестов зелёные (8 новых). Живьём в браузере с подменённым
`ym`: на `/r/` уходит `demand_done {purpose: social_contract}`, на `/report/`
— `report_viewed` с тем же purpose. То есть цель реально отправляется, а не
просто написана в коде.

**Что должен сделать владелец.** Завести эти десять целей в интерфейсе
Яндекс.Метрики (тип «JavaScript-событие», идентификатор — имя из реестра) и
задать `YANDEX_METRIKA_ID` в окружении. Без счётчика вставки нет вовсе и
цели никуда не уходят — код к этому готов, но данных не будет.

**Осталось.** D2 — вид на воронку у владельца из нашей же БД, без зависимости
от Метрики. И серверная половина D3 — разбивка `/desk` по аудитории.

### 2026-07-27 (12) — бесплатная часть отчёта стала продавать

**Сделано (шаг 4 из 4 по E5).** До этого бесплатная часть страницы отчёта
была пересказом цифр со страницы спроса: частотность, конкуренты, заметки по
шкалам — всё это человек уже видел на `/r/`. **Ни строчки сгенерированного
анализа.** То есть перед решением отдать 990–2990 ₽ оценить качество того, что
покупаешь, было физически не по чему.

Теперь бесплатно отдаётся настоящий кусок платного разбора:

- балл жизнеспособности с объяснением, почему именно такой;
- названные риски — с заголовком-тезисом и механикой провала;
- раздел **«Резюме» целиком**, тот же, что в платном отчёте.

Дальше — подпись «Так написан весь разбор» и запертые разделы, каждый со
своим вопросом. Это ровно та схема, которой пользуется dimeadozen: отдать
инсайт, продать глубину.

**Почему именно резюме.** Это самый ценный раздел, и поэтому он в образце:
показывать в качестве примера что-то слабое бессмысленно, а человек не купит
разбор, о котором не может судить.

**Цена вопроса и как она ограничена.** Образец — два вызова модели, и платит
за них владелец. Поэтому: считается ЛЕНИВО (только когда человек реально
открыл страницу отчёта, то есть думает о покупке), кэшируется навсегда в
`DemandCheck.sample_json`, и покупателю не считается вовсе — у него весь
разбор и так открыт.

**Проверено.** 382 теста зелёные (7 новых). Отдельно закрыто: образец
собирается один раз и второй визит бесплатен; у соцконтракта образец идёт в
своей оптике; сбой генерации образца не роняет страницу (принцип 7 — остаётся
прежняя витрина из цифр); образец не открывает платный тариф — `UNLOCKED_TIER`
и `TIER_KEYS` у постороннего пусты. Живьём на 1000px и 390px.

**Осталось по E5.** Шаг 3 — голос: усилить ТЗ разделов по результатам живого
прогона. Это требует ключей LLM, которых в dev нет, и потому ждёт владельца.
Плюс описать тарифы выдачей, а не объёмом.

### 2026-07-27 (11) — отчёт: посекционная генерация, 8 разделов → 21

**Почему вне очереди.** Владелец прислал восемь скриншотов dimeadozen.ai и
сказал прямо: «у нас ужасный отчёт, он и 10 рублей не стоит». Это перебивает
бэклог: мы продаём за 990–2990 ₽ то, что не стоит этих денег.

**Диагноз оказался архитектурным, а не про формулировки промпта.** Весь отчёт
был ОДНИМ вызовом модели с бюджетом 12 000 токенов на всё сразу: балл,
резюме, риски и восемь разделов «по 2-5 абзацев». Кириллица дорога в токенах
— это ~25-30 тысяч знаков на весь документ, около страницы на раздел, причём
модель под таким прессом систематически недодаёт последним. У конкурента
вызов на каждый раздел, отсюда и «up to 30 minutes for some sections», и
объём. Одним запросом такого не выдать физически.

**Сделано (шаг 1 из 4).**

- **Раздел = отдельный вызов** со своим бюджетом (2 600 токенов) и своим ТЗ.
  Суммарно на порядок больше, чем прежние 12 000 на весь отчёт.
- **8 разделов → 21**, сгруппированы в пять блоков: Идея и рынок · Конкуренты ·
  Деньги · Запуск · Риски и проверка. Появилось то, чего не было как класса:
  экономика одной продажи, цена, монетизация, первые десять клиентов, когда
  остановиться, как проверить дёшево. И то, чего нет у конкурента, а в России
  нужно: **что оформить по закону** — самозанятость или ИП, налоги, разрешения.
- **У каждого раздела свой вопрос**, на который он обязан ответить. Общий
  промпт «напиши про рынок» даёт пересказ идеи; вопрос «сколько остаётся с
  одной продажи после всех расходов на неё» даёт разбор. Тест требует, чтобы
  вопрос был вопросом, а требования — не короче 80 знаков.
- **Страница собирается на глазах.** Открытие стоит ОДНОГО вызова (ядро: балл,
  объяснение, названные риски), разделы дозаказываются по одному и появляются
  по мере готовности. Сверху строка «Разбор собирается: готово 2 из 21. Можно
  читать уже готовые разделы». Держать человека минуты на белом экране нельзя.
- **Запертый раздел продаёт вопросом**, а не фразой «полный разбор в отчёте».
- Оптика соцконтракта прошла насквозь: заголовки и ТЗ переопределяются под
  комиссию («Что мешает скопировать» → «Почему клиенты вернутся именно к вам»).

**Что закрыто по дороге.** Сбой одного раздела не выглядит как сбой всего
отчёта — остальные читаются. Готовый раздел не пересобирается (каждый повтор
— деньги за вызов). Дешёвый тариф не может вытянуть разделы полного. Порядок
разделов в хранилище остаётся читаемым, даже если дозаказ шёл вразнобой.

**Проверено.** 375 тестов зелёные (19 новых). Живьём на отдельной базе: 21
раздел с группами, заголовки под соцконтракт, открытие страницы стоит одного
вызова модели, строка прогресса показывает «готово 2 из 21», вёрстка снята на
1000px и 390px, вбок не едет.

**Чего проверить НЕЛЬЗЯ и что честно осталось.** Качество самого текста — в
dev нет ключей LLM. Архитектура и ТЗ починены, но стал ли текст лучше, покажет
только прогон владельца через превью. Шаги 3-4 (голос по результатам прогона,
продающее бесплатное превью, описание тарифов выдачей) — впереди.

**Ошибка в процессе, стоит помнить.** При переписывании тестов я срезал блок
по `class TestReportFlow` и снёс 26 тест-классов из 59; заметил по счётчику
тестов, восстановил из git и переделал по точным границам. Резать тестовый
файл по имени класса, не проверив, что оно встречается один раз, нельзя.

### 2026-07-27 (10) — C3: договор описывает то, что мы действительно продаём

**Задача оказалась шире формулировки.** C3 говорила «политика возврата не
сформулирована». На деле политики не было потому, что **не было самой услуги
в договоре: вся оферта описывала только «живой тест идеи» за 1490 ₽.** Отчёт
за 990/2990 ₽ — тот самый продукт, который идёт под рекламу на соцконтракт —
отсутствовал целиком: ни в определениях, ни в предмете, ни в таблице цен, ни
в сроках, ни в возврате, ни в ответственности. Человек платил бы 2990 ₽ по
договору, который эту услугу не упоминает.

**Что добавлено в оферту.** Определение «отчёт по идее»; предмет с оговоркой,
что расчёты построены на допущениях; обе цены тарифов; срок («формируется
автоматически при первом открытии страницы отчёта»); возврат; ответственность.

**Отдельно — про соцконтракт.** Лендинг обещает цифры, которые «выдержат
вопросы комиссии». В договоре теперь прямо сказано, что отчёт **не гарантирует
принятие решения третьими лицами, в том числе органами социальной защиты**.
Без этой строки обещание витрины читается как гарантия одобрения выплаты — а
это самая уязвимая аудитория из всех, что у нас есть.

**Возврат.** Если отчёт не сформирован — услуга не оказана, возврат полный.
После того как отчёт сформирован и открыт — по тому же основанию (ст. 26.1),
которое договор уже применял к живому тесту. Формулировка зеркалит логику,
уже бывшую в документе, а не вводит новую политику. Плюс срок рассмотрения и
способ возврата, которых не было. Условия вынесены к кнопке оплаты на `/r/` и
`/report/`: в оферту по своей воле не заходят.

**Дыра в собственном стороже.** Цена «1 490 ₽» лежала в оферте зашитой копией,
и проверка из B5 её **пропускала** — регулярное выражение искало `1490`, а в
тексте разряды разделены пробелом. Сторож теперь сначала схлопывает
разделители разрядов, и на это есть свой тест: ловит «1 490 ₽» и «1490 ₽», не
ловит «21490 ₽» и не принимает «990» внутри «2 990».

**Проверено.** 359 тестов зелёные (8 новых). Живьём: таблица цен в оферте
отдаёт 1490 / 990 / 2990 подстановкой, слотов не осталось, оговорка про
возврат видна у кнопки на `/r/`; оферта снята на 1000px и 390px, таблица не
едет вбок.

**Требует владельца.** Юридический текст писал я, а договор — его: ИП Белкин
Б. И. Формулировки надо прочитать глазами и при сомнениях показать юристу.
Особенно раздел про возврат: там я держался логики, уже бывшей в документе,
но это всё равно обязательство перед потребителем.

**Осталось.** Блок C закрыт (C1 ждёт публикации примера на проде). Дальше
блок D — измеримость: середина воронки не меряется, вида на воронку у
владельца нет, разбивки по аудиториям нет. Это то, без чего нельзя понять,
окупается ли реклама. В блоке A — только A8. Незакрытым висит вопрос про
вход по почте и паролю.

### 2026-07-27 (9) — C2: состав тарифов виден там, где человек достаёт деньги

**Сделано.** Закрыта C2. На `/r/` у самой кнопки теперь стоят оба тарифа —
название, цена и что именно входит:

- «Быстрый разбор — 990 ₽»: резюме проекта, спрос и рынок, конкуренты и
  позиционирование, вердикт;
- «Бизнес-план — 2990 ₽»: всё это и ещё целевая аудитория, **финансовая
  модель**, риски, план запуска.

**Почему это не косметика.** Человек с `/social-contract` приходит ровно за
обоснованием сметы, а секции `finance` в дешёвом тарифе нет вовсе. Раньше на
экране решения стояло только «от 990 ₽», состав открывался лишь следующим
экраном — то есть купить не тот тариф было проще, чем нужный. Блок меняет
роль на главный вместе со всем предложением бизнес-плана, так что обе
аудитории видят разницу до оплаты.

**Список берётся из движка, а не пишется руками.** Состав собирается из
`ALL_SECTIONS`/`QUICK_KEYS`; отдельный тест подменяет набор секций и требует,
чтобы витрина назвала новую. Это та же дисциплина, что в B5, и проверка «цены
не зашиты в статику» тоже осталась зелёной — C2 не протащила обратно то, что
B5 закрыла.

**Мелочь по языку.** Заголовок секции «План запуска — по этапам» внутри
перечисления через запятую читался двусмысленно из-за тире — в списке берётся
часть до него.

**Проверено.** 351 тест зелёный (6 новых). Живьём обе аудитории на 1000px и
390px: у фаундера блок стоит в «Или получите отчёт…», у соцконтракта — в
главном блоке над кнопкой; вбок ничего не едет.

**Осталось.** C3 (политика возврата) — последняя в блоке C, и для холодного
рекламного трафика она про споры о деньгах. Дальше блок D: середина воронки
по-прежнему не измеряется, а это то, без чего нельзя оценить рекламу. В блоке
A — только A8, решение владельца. C1 ждёт публикации примера на проде.
Незакрытым висит вопрос про вход по почте и паролю.

### 2026-07-27 (8) — C1: место для примера отчёта есть, наполнить его может только прод

**Сделано.** Механика публичного примера. `/example` показывает настоящий
собранный отчёт целиком и бесплатно, с честной подписью: «Это настоящий
отчёт, собранный сервисом — тариф "…", открыт целиком. Ваш будет по вашей
идее и вашим цифрам спроса». Владелец публикует его кнопкой прямо на странице
отчёта, рядом с уже существующей панелью владельческого прогона.

**Почему пример не написан руками.** Это был самый быстрый путь и он же
самый вредный: рукописный текст всегда выходит глаже машинного, и человек
заплатил бы за увиденное, а получил бы другое — прямое нарушение принципа 3.
Поэтому публикуется только то, что реально отдал движок; собрать это можно
владельческим прогоном из прошлого цикла.

**Ссылка на витринах появляется сама.** `/r/` и `/social-contract` спрашивают
у сервера, есть ли пример, и показывают ссылку только если он опубликован.
Звать «посмотрите пример» на пустую страницу хуже, чем не звать вовсе
(принципы 3 и 7).

**Что закрыто по дороге.** Пример ровно один — при публикации нового старый
снимается, иначе две «витрины правды» разъехались бы так же, как разъезжались
копии цен (B5). Пустой отчёт опубликовать нельзя (409). Публиковать может
только владелец (401). На самой странице примера нет ни панели владельца, ни
ссылок на прогон.

**Проверено.** 345 тестов зелёные (10 новых). Живьём весь путь на отдельной
базе: до публикации `/example` отдаёт 404 и ссылок на витринах нет → после
`POST /api/example/publish` страница открывается, ссылки появились и на `/r/`,
и на `/social-contract`. Страница добавлена в браузерный прогон узкого экрана
и проверена на 390px.

**Честно: задача НЕ закрыта.** Пример пуст, пока владелец не соберёт хороший
отчёт на проде и не нажмёт кнопку. В dev нет LLM-ключей — взять настоящий
текст неоткуда. Статус `[~]`, не `[x]`.

**Осталось.** C2 (разница тарифов не видна там, где принимается решение),
C3 (политика возврата). В блоке A — только A8, решение владельца. Незакрытым
висит вопрос про вход по почте и паролю.

### 2026-07-27 (7) — B6: сайт больше ничего не просит у Google

**Сделано.** Закрыта B6. IBM Plex Sans (400/500/600/700) и IBM Plex Mono
(500/600), подмножества cyrillic+latin — 12 файлов, 341 КБ — лежат в
`static/fonts/` и отдаются роутом `/fonts/{name}`. Все 13 страниц переведены
на `/fonts/fonts.css`; ссылок на `fonts.googleapis.com` и `fonts.gstatic.com`
в проекте не осталось.

**Вред был больше, чем записано в самой задаче.** Запись говорила: «текст
спасает `display=swap`, приезжает чужая типографика». Это неверно.
`display=swap` управляет поведением, когда правила `@font-face` уже
загружены; сам `<link rel=stylesheet>` в `<head>` блокирует отрисовку
страницы целиком. При недоступном домене человек видел не «другой шрифт», а
**белый экран до сетевого таймаута**. Мы собираемся платить за клик из
Директа — и теряли бы его на пустой странице.

**Замер, а не рассуждение.** Браузер с полностью отрезанным интернетом
(разрешён только `127.0.0.1`): `document.fonts.check('16px "IBM Plex Sans"')`
→ `true`, загружено 8 начертаний, заблокированных внешних запросов — ноль.
Без блокировки: **0,13 с до `load` вместо ~13 с**, которые страница раньше
ждала таймаута (эти 13 с и были причиной, по которой прогон вёрстки в B4 стоил
2,5 минуты).

**Почему роут, а не `StaticFiles`.** Монтирование `static/` открыло бы наружу
HTML-шаблоны с неподставленными слотами. Роут отдаёт только имена из белого
списка, собранного при старте; обход каталога закрыт тестом.

**Мелочь, которую стоит знать.** Знака `₽` в подмножествах IBM Plex нет — он
и раньше рисовался запасным системным шрифтом. Не регрессия переноса.

**Лицензия.** IBM Plex — SIL OFL 1.1, распространение вместе с сайтом
разрешено. Полный текст и инструкция по обновлению — в `static/fonts/`.

**Проверено.** 336 тестов зелёные (7 новых: ни одна страница не ходит к
Google, ни одна не осталась без локальных стилей, каждый упомянутый в CSS
файл реально отдаётся, кириллица покрыта, обход каталога закрыт). Живьём —
главная на 1000px и 390px с отрезанной сетью, шрифт настоящий.

**Осталось.** Блок B закрыт целиком. Дальше блок C, первым C1 — примера
отчёта нет нигде. Инструмент, чтобы его собрать, появился прошлым циклом.
В блоке A остаётся A8 (решение владельца). Незакрытым висит вопрос про вход
по почте и паролю.

### 2026-07-27 (6) — владелец может посмотреть платный отчёт, не оплачивая его

**Зачем.** Прямой запрос владельца: «надо убедиться, что бизнес-план
нормальный, но я не хочу за это платить 2990». До этого единственным способом
увидеть платный продукт было купить его у себя же — поэтому промпты и
правились вслепую (E1).

**Сделано.** `/report/{id}?key=<ключ владельца>&preview=full` собирает полный
тариф бесплатно: та же функция генерации, тот же промпт, та же оптика по
`purpose`, то же заострение. При входе с ключом на странице отчёта видна
панель со ссылками на оба тарифа и честной подписью, что человек видит
сейчас: «отчёт не куплен», «показан владельческий прогон» или «оплаченный
отчёт покупателя».

**Что здесь можно было сломать и чем закрыто.**

- **Утечка платного продукта.** Прогон владельца создаёт запись со статусом
  `preview`, и если пустить её в общую выдачу, автор той же проверки получил
  бы бизнес-план за 2990 ₽ даром. `_best_report_purchase` пускает `preview`
  только при `include_preview`, который выставляется исключительно для
  владельца. Отдельный тест проверяет и страницу, и `/api/report/{id}/status`.
- **Порча статистики продаж.** Нулевая покупка выглядела бы в `/desk` как
  «ожидает оплаты». Превью исключены из `/api/orders`.
- **Лишние вызовы модели.** Повторное открытие берёт уже собранный отчёт.
- **Ложная тревога владельцу.** Письмо о сорванной доставке шлётся только
  для оплаченных заказов — свой же прогон о себе не пишет.

**Проверено.** 329 тестов зелёные (9 новых). Живьём проверены все три
состояния панели, невидимость её для постороннего и то, что превью не
открывает отчёт без ключа. **Не проверено и не могло быть:** как выглядит сам
сгенерированный бизнес-план — в dev нет LLM-ключей, страница честно
показывает «Сервер не настроен: не задан YANDEX_API_KEY». Оценка качества —
за владельцем на проде.

**Что это открывает.** C1 (публичный пример отчёта) упирался в то, что
хорошего примера просто негде было взять. Теперь его можно собрать, вычитать
и обезличить.

### 2026-07-27 (5) — B5: цена на витрине и цена в кассе больше не могут разойтись

**Сделано.** Закрыта B5 — и это третий подряд случай одного и того же класса
ошибки, поэтому в этот раз чинили не находку, а сам класс.

Что было зашито в HTML второй копией:

- **цены и названия обоих тарифов** на витрине `/social-contract` (990/1490,
  2990/3990, «Быстрый разбор», «Бизнес-план»). Смена прайса в коде оставила
  бы витрину продавать по старой цене, а списалась бы новая;
- «От 990 ₽» в описаниях `/social-contract` и на `/r/`;
- **порог сбора данных в оферте** — «до получения 40 посещений». Это условие
  договора: разъехавшись с `CLICK_TARGET`, оно стало бы неисполнимым как
  написано;
- название тарифа в тексте отчёта и в подсказке на `/r/`;
- значение по умолчанию `40` в разметке `/p/` (его и так затирает JS из API —
  заменено на прочерк, чтобы неверное число не мелькало).

Всё это теперь идёт через `_fill_server_values()`, общий для витрин, `/r/`,
`/report/` и оферты.

**Главное — сторож, а не разовая уборка.** Пять тестов не дают классу
вернуться: сумма с «₽», название тарифа в кавычках-ёлочках, порог вердикта,
рекламный бюджет — ничего из этого не должно встречаться в исходниках
статики; плюс обратная защита — ни один слот не имеет права доехать до
браузера неподставленным, иначе человек увидит на экране `__FULL_LABEL__`.

**Две тонкости в самих проверках** (если будете расширять): искать суммы надо
вместе с «₽», иначе голое число ловится где угодно; а «8%» без границы слева
находится внутри «58%» из CSS-градиентов.

**Ложных находок не осталось, но две проверки сознательно узкие.** «Бизнес-план
для социального контракта» в заголовке страницы — название услуги, а не
ссылка на тариф, поэтому ищем только форму в кавычках-ёлочках. И `score >= 40`
в `report.html` — совсем другой порог, к `CLICK_TARGET` отношения не имеет,
поэтому число ищется только рядом со словами «визит»/«посещен».

**Проверено.** 320 тестов зелёные. Живьём: витрина соцконтракта отдаёт те же
990/1490 и 2990/3990 и те же названия, но уже подстановкой; оферта — «до
получения 40 посещений»; ни на одной из четырёх страниц не осталось
неподставленных слотов (проверено `curl`); карточки тарифов отсняты на 1000px
и 390px — вёрстка не изменилась.

**Осталось.** Блок B закрыт, кроме новой B6 (шрифты с `fonts.googleapis.com`).
Дальше блок C, и первым — C1: примера отчёта нет нигде, это кастдев-находка
самого владельца и, похоже, самое дорогое из оставшегося. В блоке A остаётся
A8 — решение владельца. Незакрытым висит его вопрос про вход по почте и паролю.

### 2026-07-27 (4) — B4: узкий экран проверяет машина, а не глаз

**Сделано.** Закрыта B4. Появился `tests/test_mobile.py` — прогон 13 страниц
на 390px в настоящем Chromium, три проверки на каждой:

1. страница не скроллится вбок;
2. ни один видимый элемент не выходит за экран;
3. ни один плейсхолдер не обрезан.

Покрыты обе витрины, `/r/` обеими аудиториями и с раскрытой лентой, отчёт,
кабинет до и после входа, `/p/`, `/desk`, все юридические страницы. Модуль
входит в обычный `python -m pytest tests/`; там, где нет Playwright или
Chromium, пропускается целиком — основной набор не должен зависеть от
браузера.

**Что нашёл.** Один настоящий дефект: **на главной обрезан плейсхолдер поля
ввода идеи** — пример занимает три строки (72px) в поле высотой 62px, то есть
последняя строка срезана на первом же экране воронки. Поле стало выше на
узких экранах; заодно высокое поле само подсказывает, что можно писать не
одну фразу. То же поле на `/social-contract` проверку проходило, но высоту
подняли и там — компонент один.

**Два ложных срабатывания, из-за которых проверки пришлось делать точнее.**
Оба стоит помнить, если будете расширять прогон:

- лента разделов в отчёте намеренно скроллится вбок внутри себя
  (`overflow-x:auto`) — это нормальный приём для широкого блока, дефект
  только когда вбок едет сама страница. Теперь пропускается всё, что лежит
  внутри скроллера;
- `textarea` переносит плейсхолдер по строкам, `input` — нет. Мерить их
  одинаково нельзя: у первого проверяем высоту, у второго ширину.

**Почему прогон сначала шёл 2,5 минуты.** Страницы тянут CSS шрифтов с
`fonts.googleapis.com` рендер-блокирующим тегом; без внешней сети каждая
загрузка ждала таймаут ~13 секунд. В прогоне внешние запросы теперь режутся —
**156 → 16 секунд**. Побочная находка занесена как B6: из России этот домен
часто недоступен, а мы ставим его в `<head>` (принцип 8).

**Честная оговорка про метод.** Без внешнего CSS браузер берёт запасной
шрифт, метрики текста слегка другие. Находки вида «не влезает на пару
пикселей» стоит перепроверять глазами. С другой стороны, у страниц стоит
`display=swap` — первые мгновения реальный человек тоже видит запасной шрифт.

**Проверено.** 314 тестов зелёные, из них 5 — браузерные. Починенная главная
отснята на 390px: плейсхолдер помещается целиком.

**Осталось.** B5 (зашитые в статику копии серверных названий), новая B6
(шрифты), затем блок C — примера отчёта по-прежнему нет нигде (C1). В блоке A
остаётся только A8, это решение владельца. Незакрытым висит его вопрос про
вход по почте и паролю.

### 2026-07-27 (3) — A7: воронка обещает ровно то, что записано в оферте

**Сделано.** Закрыта A7. Оферта прямо говорит: «Запуск рекламы осуществляется
Пользователем самостоятельно по предоставленной инструкции. Рекламный бюджет
не входит в стоимость услуги.» Воронка при этом обещала обратное в трёх
местах:

- крошка на `/r/`: «Этап 3 — ... и **запустим первую рекламу**»;
- сообщение после оплаты: «Мы запускаем живой тест — **вернёмся с первыми
  цифрами**» (мы никуда не возвращаемся, цифры человек смотрит в кабинете);
- ответ на заявку без оплаты: «**запустим страницу и рекламу** вашей идеи».

Показательно, что главный абзац предложения двумя экранами ниже уже говорил
правду («дальше вы сами запускаете рекламу») — то есть страница
противоречила сама себе.

**Больше, чем было в задаче.** По ходу нашлось то, что опаснее формулировок:
**про рекламный бюджет до оплаты не говорилось нигде.** Человек платил
1490 ₽, а потом узнавал, что нужно ещё 3–5 тысяч ₽ Яндексу — то есть
настоящая цена шага втрое выше названной. На холодном рекламном трафике это
прямой повод требовать возврат, и требование было бы справедливым. Оговорка
теперь стоит под самой кнопкой оплаты, у обеих аудиторий.

**Цифра бюджета — из одного места.** `AD_BUDGET_HINT` подставляется и под
кнопку, и в плейбук: тест падает, если в статике заведётся зашитая копия.
Это тот же приём, что спас пороги вердикта в B3, — второй случай подряд,
когда копия числа в статике оказывалась враньём.

**Проверено.** 309 тестов зелёные (7 новых, включая проверку самой
предпосылки — что оферта действительно говорит именно так). Живьём: `/r/`
обеими аудиториями (`business` и `social_contract`) на 1000px и 390px —
оговорка видна под кнопкой в обоих раскладах, у соцконтракта блок корректно
опущен под бизнес-план. Текст после оплаты и крошка проверены `curl`.

**Осталось.** В блоке A остаётся A8 — порог 8% может быть завышен, но это
решение владельца, не сессии. Дальше B4, B5 и блок C: примера отчёта
по-прежнему нет нигде (C1) — это кастдев-находка самого владельца и, похоже,
самая дорогая из оставшихся. Незакрытым висит его вопрос про вход по почте и
паролю.

### 2026-07-27 (2) — B3: числа объясняют себя, витрина перестала врать о порогах

**Сделано.** Закрыта B3. Главная находка оказалась не про пояснения, а про
прямое расхождение: **главная обещала «выше 2,5% — идея живая», плейбук —
«дождитесь ~100 визитов», а движок считал по 8%, 4% и 40 визитам.** Человек с
3% читал на витрине «идея живая», а в кабинете видел «СПРОСА НЕТ». Это
нарушение принципа 3, и заметить его можно было только сличив две страницы с
кодом глазами.

- Пороги переехали в `CLICK_TARGET` / `SIGNAL_RATE` / `DEAD_RATE` — один
  источник правды. Дефолты модели `SmokeProject` берутся оттуда же, чтобы не
  появилась третья копия в схеме БД.
- `/` и `/guide/direct` отдаются через `_with_thresholds()` — числа
  подставляются из кода. Тест падает, если витрина назовёт другое число.
- Вердикт теста теперь называет порог, с которым сравнил: «50 визитов, из них
  6 заявок — это 12%, при пороге 8% и выше».
- Под «6/10» на `/r/` — строка, откуда взялось число. Отдельный текст, когда
  итог опущен до балла спроса: это ровно тот вопрос, который владелец задал
  на живом прогоне («как при почти нулевом спросе идея получила 6/10?»).

**Найденный по дороге дефект.** В `compute_verdict` жило запрещённое слово
**«оффер»** («попробовать второй оффер») — и этот текст видит покупатель в
`/account` и на `/p/`. Тест на запрещённые слова покрывал только
`demand._verdict`; вторая, очень похожая функция не проверялась никем. Заодно
ушли «в очередь на MVP», «копим клики», «идею в архив» — язык владельца
пульта. Плюс согласование числительных: было «1 заявок».

**Проверено.** 303 теста зелёные (14 новых). Живьём: `/r/` с заниженным
спросом на 1000px и 390px — объяснение балла читается и не ломает вёрстку;
главная и плейбук отдают 8% / 4% / 40 подстановкой (проверено `curl` и
скриншотом). Все четыре текста вердикта распечатаны и прочитаны на числах
1/0, 10/1, 50/6, 50/1, 52/2, 111/5.

**Найдено, не взято в работу.** A7 — хлебная крошка на `/r/` обещает, что
рекламу запустим мы («и запустим первую рекламу»), хотя человек запускает её
сам; это нарушение принципа 3 прямо на пути к оплате. A8 — порог 8% может
быть завышен для холодного трафика, но менять его значит менять вердикт,
который люди уже получали: решение владельца, не сессии.

**Осталось.** Блок A снова не пуст: A7 первым в следующий цикл, A8 — вопрос
Борису. Затем B4, B5 и блок C (примера отчёта по-прежнему нет). Незакрытым
висит вопрос владельца про вход по почте и паролю.

### 2026-07-27 — B1+B2: кабинет перестал быть тупиком на первом входе

**Сделано.** Закрыты B1 и B2 одной правкой — это буквально один экран и одна
проблема. Человек подтверждал почту, попадал в `/account` и видел два пустых
заголовка подряд («Пока нет запущенных проектов.», «Пока нет отчётов по
идее.») — ни объяснения, ни действия.

- Пустые разделы больше не выводятся: заголовок над пустотой занимает место
  и ничего не сообщает.
- Когда пусто всё — экран первого входа: одна мысль («здесь будут ваши
  проверки идей»), одно действие, одна честная сноска «платить ничего не
  нужно».
- Когда содержимое есть — разделы идут от самого продвинутого к раннему
  (проекты → заявки → отчёты → проверки), внизу тихое «Проверить ещё одну
  идею →».
- Обе кнопки ведут на СВОЮ витрину: `/social-contract` для тех, кто пришёл
  оттуда (`purpose` берётся с самой свежей проверки человека). Возвращать
  получателя соцконтракта на витрину для фаундеров — нарушение принципа 4.

**Заодно, по ходу.** Три вещи, найденные только глазами, тестов на них не
было: ссылки-кнопки в кабинете были подчёркнуты (на всех остальных страницах
`text-decoration` снят); на 390px «Открыть →» ломалась в столбик из двух
слов — строки теперь складываются в колонку; экран входа обещал кабинет
только оформившим заказ, хотя после `POST /api/demand/{id}/save` туда входят
и с бесплатной проверкой.

**Ещё.** Название тарифа в кабинете («Полный отчёт») разъехалось с витриной
(«Бизнес-план») — вторая копия в статике. Теперь `tier_label` приходит с
сервера и разъехаться не может.

**Проверено.** 290 тестов зелёные (13 новых). Живьём через Playwright оба
состояния кабинета — пустое и заполненное — на 1000px и 390px.

**Осталось.** B3 (числа без объяснения), B4 (системный прогон мобильной
вёрстки), новая B5 (зашитые в статику копии серверных названий). Дальше блок
C — доверие: примера отчёта по-прежнему нет нигде, это прямая кастдев-находка
владельца. Незакрытым висит его вопрос про вход по почте и паролю.

### 2026-07-26 (вечер, 6) — A6: выбранная формулировка доехала до бизнес-плана. Блок A закрыт

**Сделано.** Закрыта A6, а с ней весь блок A. Человек на `/r/` выбирал одну
из трёх заострённых формулировок — и этот выбор до платного разбора не
доезжал: `generate_report` параметр `chosen_offer` принимал, но `main.py` его
не передавал. Выбор теперь хранится на самой проверке
(`DemandCheck.chosen_offer`), пишется по `POST /api/demand/{id}/chosen` при
клике на карточке и попадает в промпт отчёта.

**Почему выбор лёг на проверку, а не на заказ.** На заказе живого теста он уже
жил (`LiveTestOrder.chosen_offer`) — но отчёт заказывают со страницы
`/report/{check_id}`, которая знает только `check_id` и про заказ ничего не
знает. Через заказ выбор туда не проходит в принципе.

**Заодно — видимость.** На странице отчёта появилась строка «Разбираем
формулировку: …». Без неё покупатель не может отличить учтённый выбор от
проигнорированного: обе картины выглядят одинаково.

**Проверено.** 279 тестов зелёные (9 новых). Отдельно закрыты три способа
сломать платную услугу: отчёт собирается и без выбора (заострение
необязательно), битый JSON в колонке даёт `None`, а не исключение, чужая
сохранённая проверка отвечает 409 — id перебираются, а выбор влияет на то,
что человек получит за деньги. Живьём: `/report/` на 1000px и 390px, блок
читается, разметка `<em>` из заголовка снята.

**Осталось.** Блок A закрыт целиком. Дальше — блок B (понятность и первый
вход) и кастдев-проходы по обеим аудиториям. Отдельно висит незакрытый
вопрос Бориса про вход по почте и паролю вместо magic-link.

### 2026-07-26 (вечер, 5) — A5: третья шкала этапов удалена из репозитория

**Сделано.** Закрыта A5. Удалён `static/portfolio.html` — 130 строк мёртвого
экрана, который никто не открывал с v1.0, но который нёс третью шкалу этапов
(Оффер · Активация · Мост · Оплата · Масштаб) вдобавок к покупательской
`STAGE_NAMES` (7) и `TRACKED_STAGE_NAMES` (8). Имя файла убрано из прогрева
шаблонов в `_lifespan`. Роут `/portfolio` оставлен редиректом на `/desk`:
старые ссылки не ломаем, а сам экран отдавать больше нечем.

**Почему это стоило времени, хотя пользователь файла не видел.** Две шкалы
этапов уже требуют комментария в `CLAUDE.md`, чтобы следующая сессия не
«починила» их слиянием. Третья, вообще ни к чему не подключённая, — это
готовая ловушка: при чтении кода она читается как актуальная.

**Проверено.** 270 тестов зелёные. Новый `test_dead_portfolio_file_is_gone`
не даёт файлу вернуться; существующие `test_portfolio_redirects_to_desk` и
`test_portfolio_page_and_clean_index` подтверждают, что редирект пережил
удаление. Прогрев шаблонов при старте приложения не падает на отсутствующем
имени — сьют поднимает приложение через `TestClient`.

**Заодно.** В `CLAUDE.md` поправлены два устаревших пункта «Известных
проблем»: письма владельцу об оплатах и о сорванной доставке отчёта написаны
(A1, A2), но реальная проблема теперь другая — не задана
`SOZDATEL_OWNER_EMAIL`, и без неё канал молчит целиком.

**Осталось.** Блок A: только A6 — `chosen_offer` принимается
`generate_report`, но `main.py` его не передаёт. После неё блок A закрыт.

### 2026-07-26 (вечер, 4) — A4: вердикт спроса без жаргона и без чужих советов

**Сделано.** Закрыта A4. Переписаны все четыре текста `_verdict()`. Ушли:
«на живом трафике», «продавать придётся в холодную», «частотность». Пришли
формулировки, которые понятны и фаундеру, и самозанятой: «Спрос небольшой,
но он есть: людей мало, и каждый клиент будет на счету»; «В поиске эту идею
почти не ищут. Возможно, люди называют её другими словами — попробуйте
переформулировать. Если формулировка верная, клиентов придётся находить
самому: из поиска они не придут».

**Правило, которое стоит держать в голове следующей сессии.** Вердикт
СООБЩАЕТ находку и не предписывает следующий шаг. Раньше он звал «проверить
на живом трафике» — это и жаргон, и совет мимо половины аудитории:
получатель соцконтракта рекламу может вообще не запускать. Следующий шаг
уже подбирает CTA страницы по `DemandCheck.purpose` (сделано двумя циклами
раньше), и вердикт не должен с ним спорить.

**Проверено.** 269 тестов зелёные (3 новых: нет жаргона ни в одном из
четырёх текстов, нишевый вердикт не тянет в рекламу, разряды числа не
сломались). Живьём все три уровня вердикта на 390px — тексты читаются
целиком, вёрстка держит длинный weak-вариант.

**Осталось.** Блок A: A5 мёртвый `portfolio.html`, A6 `chosen_offer` не
доходит до отчёта. После них блок A закрыт — дальше кастдев-проходы.

### 2026-07-26 (вечер, 3) — A3: `/p/` заговорила с покупателем на «вы»

**Сделано.** Закрыта A3. Инструкция запуска на `/p/{idea_id}` писалась, когда
страницу видел только владелец, и осталась на «ты» («не трогай существующие»,
«Так ты знаешь», «видны только тебе»). Страница давно открыта покупателю из
`/account`, а весь остальной сайт — на «вы». Переписаны все шесть шагов.

Заодно убран внутренний жаргон, который там же и жил: «smoke-тест» →
«короткая проверка», «РСЯ» → «показы в сетях на сайтах-партнёрах», «трафик»
→ «реклама»/«объявления» (принцип 5 — пользователь не обязан знать наш язык).
Поправлен устаревший комментарий в CSS: шкала пути 0..7 → 0..6 после
слияния этапов.

**Проверено.** 266 тестов зелёные (3 новых: «ты»-формы не вернутся, вежливые
формы на месте, жаргон отсутствует). Живьём в браузере на 1000px и 390px:
страница открывается по ключу из URL без диалога, инструкция читается
целиком, вёрстка на узком экране не ломается.

**Осталось.** Блок A: A4 жаргон «на живом трафике» в `_verdict()`, A5
мёртвый `portfolio.html`, A6 `chosen_offer` не доходит до отчёта.

### 2026-07-26 (вечер, 2) — A2: владелец узнаёт об оплатах и заявках сам

**Сделано.** Закрыта A2. `_notify_owner_order()` шлёт владельцу письмо в
четырёх точках: подтверждённая оплата в `yookassa_webhook` (и живой тест, и
отчёт) и заявка без оплаты в `/api/live-test` и `/api/report`. Данные письма
собираются внутри сессии БД, а отправка идёт после — держать транзакцию и
ответ вебхуку ЮКассы на медленном SMTP незачем.

**Ключевая деталь, которую легко сломать в будущем.** Флаг уведомления
разведён на два: `paid_notified` (сказали об оплате) и `fail_notified`
(сказали о сорванной доставке). Один флаг на двоих означал бы, что письмо
об оплате гасит более важное письмо «услуга не оказана» — на это есть
отдельный тест `test_paid_notice_does_not_swallow_failure_notice`.
`ReportPurchase.owner_notified` из вчерашней правки переименован в
`fail_notified` (поле не успело уехать в main, миграция идемпотентная).

**Проверено.** 263 теста зелёные (6 новых): заявка без оплаты доходит по
обоим каналам, повторный вебхук ЮКассы не шлёт второе письмо, упавшая почта
не ломает приём заявки, без `SOZDATEL_OWNER_EMAIL` всё молчит штатно.
Приложение поднято живьём — `/health` и `/` отвечают 200. UI не менялся,
браузерная проверка не требовалась.

**Не проверено (честно).** Реальная доставка письма: SMTP-ключей в
окружении нет, путь закрыт инъекцией. Боевая проверка — только после
деплоя с заданной `SOZDATEL_OWNER_EMAIL`.

**Осталось.** Блок A: A3 «ты» на `/p/`, A4 жаргон в `_verdict`, A5 мёртвый
`portfolio.html`, A6 `chosen_offer` не доходит до отчёта.

### 2026-07-26 (вечер) — A1: сбой доставки платного отчёта больше не немой

**Сделано.** Закрыта A1. `mailer.notify_owner()` — побочный канал письма
владельцу, который никогда не бросает исключение (сбой уведомления не имеет
права уронить страницу и без того расстроенного покупателя). У
`ReportPurchase` появились `gen_error` и `owner_notified`: причина сбоя
видна владельцу, письмо уходит один раз на заказ, сколько бы раз покупатель
ни перезагружал страницу. `/api/orders` теперь отдаёт и покупки отчётов,
`/desk` показывает их отдельной секцией со статусами «сбой доставки» /
«оплачено» / «ожидает оплаты».

**Проверено.** 257 тестов зелёные (6 новых). Живьём в браузере на 1000px и
390px: секция отчётов рисуется, сбой доставки виден красным с текстом ошибки.
По ходу поймал два собственных дефекта — ранний `return` в `loadOrders`
скрывал бы весь блок отчётов, пока нет заявок на живой тест, и обращение к
несуществующей переменной `data` (функция деструктурировала `{orders}`),
из-за чего секция была пустой; оба исправлены и покрыты тестом.

**Не проверено (честно).** Реальная отправка письма: в этом окружении нет
SMTP-ключей, путь проверен только инъекцией. На проде нужно задать
`SOZDATEL_OWNER_EMAIL`, иначе уведомления молча не уходят — это
сознательная деградация, но про переменную легко забыть.

**Обнаружено сверх ожидаемого.** Покупки отчётов не были видны владельцу
вообще нигде: `/api/orders` возвращал только `LiveTestOrder`. То есть
оплата на 2990 ₽ и несостоявшаяся доставка выглядели одинаково — никак.
Это оказалось шире, чем формулировка A1, и вошло в ту же правку.

**Осталось.** Блок A: A2 (уведомления о новых оплатах/заявках — канал уже
есть, осталось позвать), A3 «ты» на `/p/`, A4 жаргон в `_verdict`, A5
мёртвый `portfolio.html`, A6 `chosen_offer` не доходит до отчёта.

### 2026-07-26 — заведён источник правды + аудит

**Сделано.** Создан этот файл: принципы, приоритизированный бэклог (24 задачи
в пяти блоках), журнал. До него автономные сессии правили в основном тексты и
не имели общей памяти между запусками.

**Проверено (аудитом, а не догадками).** Единственное письмо в системе —
magic-link (A1, A2). `/p/` обращается на «ты», хотя открыт покупателю (A3).
Целей Метрики три, середина воронки не меряется (D1). Примера отчёта нет
нигде, хотя владелец отмечал это как блокер доверия (C1). `portfolio.html`
жив на диске с третьей шкалой этапов (A5). `chosen_offer` принимается
движком отчёта, но не передаётся (A6).

**Осталось.** Весь бэклог. Ближайшее — блок A, начиная с A1: деньги взяты,
доставка не состоялась, никто не уведомлён — самый дорогой сценарий отказа
для продукта, на который вот-вот пойдёт платный трафик.

**Контекст сессии.** До этого в тот же день: разведены оптики отчёта
(`business` / `social_contract`), промпту запрещено отказываться считать
смету, `/r/` развёрнута под аудиторию соцконтракта, Вордстат научился брать
частотность из `topRequests` и честно показывать подменённую формулировку.
